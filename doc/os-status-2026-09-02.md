# spec/os.md — the status pass retaken, 2026-09-02

*Five days after `doc/os-status-2026-08-28.md`, at Henri's "Can we
discuss what is implemented again?  maybe write yet another
doc/os-status", the hour after he moved the testing to the far future
and named what comes first: the tree raised up and working, and the
interface levelled so it is easy on the user and understandable
(`vision.md` §"What comes first now").  Only what moved is written; a
property not named here stands where the 08-28 pass left it.
Grounded in what is in the tree; his to correct.*

## What moved

* **13, start by pull; no pull → stop** — *served per node* → **and
  held, and ticked.**  `card:hold.md` (08-29, four passes in one
  sitting at his review): a `<name>.hold` beside the pin is a standing
  pull, a hold not honoured is a bold row, and `resolve.sh --tick` is
  the resolver with no hand on it, carried by a user timer on Ubuntu
  (systemd the implementation, never the dependency — his words).
  Shown by execution today, both ways: the node's own `sitting 10`
  ended three 24-turn loops (`llm/state/stopped`: *the 10 minutes of
  llm are up*), and the hold's tick brought it back inside a minute
  each time.  A pull now has a person-side half that outlives the
  puller.  What the day found: a grant's sitting is shorter than a
  run a person wants, and nothing in the handed loop said so.

* **14, errors are not silent** — *one closed, one opened* → **four
  faces closed in two days, each with a gate, and a rule promoted for
  them.**  `F013`: `launch.sh check` said a grant path "does not
  exist" when the fence hid it — three call sites, the third verdict
  printed.  `F014`: a door that "did not answer" deleted curl's own
  reason unread.  `F015`: an account said *thinking off* on a request
  that carried no knob.  `F016`: a banner written over an empty reply,
  seventeen times in three seconds.  The rule behind all four is in
  `board/README.md` since 09-01 — **a check has three verdicts: ✓, ✗,
  and "not from this seat"** — and its next faces came from where it
  said they would.  Also closed: a red that vanishes on retry is now
  counted (`card:flake.md`: the failure ledger and the shake, `F001`,
  `F002`).  Still open in this property: `F010`, the material a node
  is asked to reason from is cut and the node is never told.

* **12, designed for AI use without threatening security** — *its one
  gap has a number* → **minds are admitted at doors, with tools under
  keep, and the first admission is written.**  `doors/` (08-29): a
  model is admitted by a file in the person's words — url, model, a
  key under the home never in the tree, `admitted` by whom and when;
  `openrouter`, `anthropic`, and since today `llm`, the node at its
  own port.  `tools  read ls grep` on a door gives a mind the tree
  read-only, one process under keep per call, every call a `C:` line
  the person sees (`card:tools.md`, 09-01: 96 arms, the knob is what a
  single read returns).  A kept led turn exists (`lead.sh --kept`:
  the kernel refuses a board write); a door turn runs unkept, on the
  person's side, and he said today that is fine for measurement and
  the kept form is the mechanism in practice.  `card:model-acceptance.md`
  has its first sheet: gemma4 admitted as a task shape — reads when
  reading is the ask, not when reading is the way to the ask — and
  refused for leading.  The session's own home is a tmpfs with
  `~/.claude` read-only (`card:lost-write.md`), so a session cannot
  read the person's keys and cannot lose its memory to a vanished
  write.  Open: `card:private.md` (a mind's thinking on display, no
  place of its own), and the leader's reach as a grant row.

* **2, testing installation should not be hard** — *carded* →
  **check from the right seat.**  `launch.sh NODE check` inside the
  fence now says `· not checked from here` instead of a ✗ for what
  the fence hides (`F013`), so a check no longer condemns a node from
  the wrong seat.  `install.sh --check` reads the installed set back
  by hash.  The gap is unchanged and carded: `card:lander.md` — a
  commit through the gate is not in force until the person's one
  `sudo`, and `launch.sh` drifted from its installed copy three times
  in one sitting on 08-28.

* **9, may crash but not hang** — *plus one cord* → **plus a
  heartbeat and a shake.**  The runner's watch file (08-28): a held
  lock with a stale heartbeat is read as *the cords are cut* and
  stopped, escalating past a program that ignores TERM — the 25-minute
  overrun reproduced and closed.  Under load the launcher stopped a
  busy program as idle at exactly the window (`F001`); measured by the
  shake, 8 of 10, fixed on the launcher's own tick reads.

* **8, open where left** — *the canvas* → **the canvas with the
  person's hand in it.**  `tools/panel.py`: pins, holds, hold and
  unhold from the panel, a death on the andon's timeline from the
  runner's own stop line (canvas day two, an Opus draft landed with
  two corrections only the parser could give).  `hold-mirror` shelved:
  what the person's side learns about a held thing has nowhere of its
  own to be written.

* **11, hard to attack from outside** — unchanged in mechanism;
  **two edges named.**  A door's key goes to curl on stdin, never an
  argument line, and is asserted absent from every account and
  proposal; the harness rewrites a session's own command text before
  it runs (`card:rewritten-command.md`), so a session cannot trust
  that what it wrote is what executed — the heredoc route is refused.

* **4a, every part testable** — *the suite, gates, mutation* → **and
  the instrument audited.**  `tools/mutate.sh`'s every gate row had
  read "refused" from a dud commit that could not sign (`F002`);
  fixed, and a row reached `COMMITTED — suite: red` for the first
  time.  765 tests collect from the root today — with 27 collection
  errors, every one under `bench/`: the `SWE-bench` and `simple-evals`
  checkouts' own tests, foreign repositories the tree's suite does not
  run and `pytest` from the root does.  *(First written as "all
  SWE-bench's"; a count run before the commit said two were
  simple-evals', and the sentence is corrected rather than the count
  trusted.)*

* **1, auditable and bounded on its own interface** — unchanged as an
  OS property; **what grew is the governance beside it.**  A rule a
  session writes about sessions carries a mark until the person
  strikes it, and the gate binds the approval to the words he read
  (`test/test_marks.py`); a question says what would answer it and
  only `his call` reaches him (`card:questions.md`); `keeper.md` is
  the person's two acts.  Nine ingestion batches, 90 of 111 kaizens
  read, five rules promoted with their kaizens named (four in `board/README.md`, one in `manifesto.md`).

## The shape, five days on

The program properties held and gained their person-side halves: a
pull that is held, a check that knows its seat, an error that carries
its reason.  The in-the-air cluster is where it was — the store (6b),
the language future (4b, 10, 15), the disk (16).  What the five days
mostly built is not an OS property at all: doors, tools, marks,
questions, the keeper, the cold arm, and four runs of a measurement
that he has now set aside.  The tree's method grew faster than its
operating system did.

Against what he named first — **raised up and working, and easy on
the user** — the properties say where the floor is uneven:

- a commit is not in force until his one line, and nothing but his
  memory carries it there (`card:lander.md`);
- a node's sitting ends a run the person wanted, and the run's own
  loop did not say so first;
- the kaizen lamp names a second file for a sitting that has one
  (kaizen 1544, twice) — a lamp change or a rule change, his;
- a person who has never read this tree still cannot start a program
  and find out what it did without being told: the stranger test in
  `vision.md`, unmet, and now the priority.
