# spec/os.md — the status pass retaken, 2026-08-28

*Two days after `doc/os-status-2026-08-26.md`, at Henri's "how about
we work toward the easy to attain features today?"  Only what moved is
written; a property not named here stands where the 08-26 pass left
it.  Grounded in what is in the tree; his to correct.*

## What moved

* **2, testing installation should not be hard** — *simple solution
  here* → **carded** (`card:node-install.md`).  Two instances arrived:
  `tools/install.sh --check` (08-27) is an install test for tend's own
  restraints, read back from a record beside the copies; and the
  toolbox manifest was completed the same day after a grep found three
  load-bearing deps undeclared — an install test that had said yes to
  an incomplete install.  What remained was per node: nothing says
  whether a node can run here without running it.  That is the card,
  and its day one is `tools/launch.sh NODE check`.

* **5, configuration recorded in the node; reconfigurable by pull** —
  *simple solution here* → **half satisfied, half decided against.**
  Recorded: yes, the grant beside the program and its state directory;
  and for the restraints themselves, `/usr/local/lib/tend/installed`
  is the record (commit, date, source, sha256 per file).  The second
  half — *by pull, reconfigured from wherever it came* — is not in the
  air; it was **decided against on 2026-08-27** (`card:session-program.md`,
  the limit's decision 1): a pull is the one thing a session writes,
  so a pull's text is never read as a grant; `pull sitting 900` is a
  line in the pull file and nothing more.  Reconfiguration by pull
  would make the pull a door into the grant, which is Rule 1 from the
  other side.  The property's provenance half ("from wherever it
  came") still waits on the store, as the 08-26 pass said; a cheap
  slice — the launcher writing what a run actually ran under (grant
  sha, tree commit, model file and hash) into the state, in
  `installed`'s shape — exists as a shape and has no reader yet, so it
  is not built (manifesto rule 1).

* **9, may crash but not hang** — *satisfied* → **plus one cord.**
  The sitting word on a node (08-27): a runner is stopped when its
  minutes are up, pulled or not, a close with a reason and not a
  crash.  The crash-loop backoff + andon half is still owed and now
  waits with `card:session-program.md` on the first node that leads.

* **13, start by pull; no pull → stop** — *satisfied* → **and served
  per node.**  `launch.sh NODE serve` (08-26 evening): the resolver's
  per-node decision as an mtime rule, so it holds for a server with no
  tally.  Shown for both nodes.

* **14, errors are not silent** — *satisfied* → **one silent state
  closed, one exception opened.**  Closed: a commit through the gate
  waiting on the person's `sudo`, which nothing said until today —
  `card:lander.md`'s lamp, in force since this morning.  Opened, the
  same morning: with `audio` out of the reach bound the andon cannot
  sound, and Henri's word is that it must — a cord a session has to
  be granted reach to pull is a silent failure with a reason attached
  (noted on `card:session-program.md`).

* **12, designed for AI use without threatening security** — *simple
  solution here* → **its one gap has a number.**  The session's exec
  set was read from the leash ledger (`card:work-environment-ai.md`
  §2026-08-28): 61 programs over four days, the top 30 carrying 98 %,
  23 execs touching a reach row.  The ambient-exec residual stands;
  it is now measured rather than argued.

* **1, auditable and bounded from its own interface** — *simple
  solution here* → **unchanged, and what it still needs is named.**
  The grant is the interface and the ledger is the audit, as before.
  The read that would claim it fully — what a node *did* against what
  it was *granted* — needs keep to record its refusals per node and
  something to read them beside the leash's rows.  Not built; no
  caller has asked for the read.

## The shape, two days on

The program properties held and gained a cord; the one carded today
(2) is the last of the "simple solution" group with a build in it.
The rest of that group is stance (3, 4a, 6a) or waits on a reader (1,
5's provenance).  The in-the-air cluster is where it was: the store
(6b, open problems 2 and 3) and the language future (4b, 10, 15).
