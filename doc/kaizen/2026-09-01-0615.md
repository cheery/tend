# Kaizen, 2026-09-01 06:15 — a cap that was four days behind its window, and three more that do not speak

*Three commits, 06:15 to 07:05: `ddd3e59` (batch 8 of the ingestion),
`d4bc9e2` (`F008` resolved), `9af3d98` (`F009` resolved, the cap sweep,
`F010` opened).  Henri's lines: "Lets check what's for work today.  At
least kaizen-ingestion you may proceed with", "I don't find anything
particularly that I would disagree with in the kaizen ingestion report",
"did we succeed in the compare.py benchmark, or should it be taken
again?", "can you widen -c 2048?", "do d and a", "ok. what's the next
useful task to take?", "lets finish the cap sweep. and F009 shape (a)".*

## What the sitting was

* **Batch 8 read**, ten kaizens of 2026-08-27 evening and 2026-08-28
  morning.  Eight `rule`, one promotion into `board/README.md`: *a check
  has three verdicts, not two — ✓, ✗, and "not from this seat."*  Three
  faces in one day plus a fourth batch 4 had read as `once`.
* **The reading found the rule's counter-example alive in the tree.**
  `card:silent-cord.md` §10:18 corrects the "no PipeWire socket on the
  work laptop" reading in place; `tools/toolbox.sh`'s andon comment still
  quoted the *uncorrected* version, citing the card that corrects it.
  Four days, in the file carrying one of the rule's own two mechanisms.
* **`F008` resolved**, shapes (d) and (a).  The digest carries 13 of 13
  cards and names what it drops when it drops.
* **`F009` resolved**, shape (a): a `because` cut at eight lines now says
  how many it left.
* **The cap sweep finished** — yesterday's item 4 — and `F010` opened:
  three of the tree's content caps still say nothing to the mind they cut.

## What went right, and why

**Henri's half of the ingestion came back, first time in eight batches.**
Fifty lines and seven batches of "still his and still undone", and the
answer was three words of disagreement — none.  It is the only outside
measurement the ledger has ever had.  It does not settle batch 1's
self-shaped strand, which asks him to *make* a call rather than to
disagree with one, and the batch paragraph says so rather than banking
the easier answer as the harder one.

**The `-c 2048` question was answered by measuring, and the measurement
overturned the entry's own reasoning.**  `F008` had written off shape (d)
with "the number is gemma's 8 k context".  It was not: 5000 was chosen for
a node at `-c 2048` and stayed 5000 when `37092d7` took the node to
`-c 8192` on 2026-08-28.  So (d) was never "raise the cap past what fits"
— it was "the cap has been four days behind the window it was sized for."
Reading the GGUF header (`context_length 262144`, 30 layers, 2 KV heads on
the five global ones) cost one command and made the whole recommendation
different from the one I would have given from the card's prose.

**The gate is the part that outlives the number.**
`test_the_cap_fits_the_window_the_node_actually_has` parses `-c` out of
`llm/grant`, so the two ends cannot drift apart again in silence.  It
proved itself within the hour: Henri widened the node to `-c 16384`
mid-sitting and the gate simply followed him.  That is the difference
between fixing a number and fixing why the number went stale.

**The second copy was found and gated rather than fixed and forgotten.**
`tools/compare.py` carried `out[:DIGEST_CHARS]` — the same cut in Python —
under a docstring claiming it "builds the same digest lead.sh builds",
with nothing checking it.  Two copies of one mechanism and no gate between
them is `F008`'s own shape.  The new test runs the real `lead.sh` against
a stub and compares what reached the model; **it passed on its first run,
so it was mutated by one character and shown red before it was trusted** —
`2026-08-27-1723`'s lesson, read in this morning's own batch, applied four
hours later.

**The sweep found three more and did not fix them.**  `propose.sh:60`,
`compare.py:242` and `consult.sh:56` all cut the material a mind reasons
from; `consult.sh` tells the *person* and never the model.  `F010` says
so and stops, because whether a drafting prompt should carry "your
material was cut" is measurable and unmeasured — a model told its material
is short may hedge, which is the opposite of what `F008` found for the
board.

## What went wrong — named as mine

**I told Henri the node was at `-c 2048` and it had been 8192 for four
days.**  I read it off `board/README.md`'s kaizen prose instead of
`llm/grant`, and the number went into a recommendation about F008 before
anyone checked it.  It changed his decision — at 2048 the whole board
cannot fit and at 8192 it fits three times over.  He caught it by asking
to widen a number that was already widened.  **This is
`count-before-you-write-it` in its exact form, one week old, and the file
was one `cat` away.**  The tree's own words for it are in the memory I
wrote on 2026-08-27.

**My first commit used `git add -A` while Henri was editing three tracked
files.**  `llm/grant` and both `doors/*/door` were his, mid-edit; the
commit missed sweeping them into my message *by timing alone*.  Nothing
broke and that is not the same as nothing being wrong: a session that
stages everything can commit the person's unfinished work under its own
account of what it did.  Both later commits used explicit paths.

**The harness ate a braced expansion four times in one sitting, and I
walked into the last two with my eyes open.**  Once in a shell string-length measurement
(caught immediately by an absurd number: "~130 chars"), once found
*already committed* in `consult.sh` since 2026-08-28, and once **inside
the paragraph of `F010` explaining that the harness does this** — blanked
to empty backticks. Caught by reading the file back, which the memory
already says is the only thing that catches it.  **A fourth face landed
while this kaizen was being written**, blanking the same form out of the
sentence above — so the sitting's count is four, and the honest reading
is that the memory is not holding.  It tells me what happens and not
what to do instead, and this mode asks me to reach for the shell by
default, so the reaching is structural and not carelessness.  That is a
mechanism question, and a mechanism is a card.

## What should change tomorrow

1. **`F010` is the person's call**, and it is the first cap question the
   tree can *measure* rather than argue: `compare.py` can run a draft with
   and without a "your material was cut" line and read the difference.
2. **The compare benchmark, two runs and one variable each.**  Henri wants
   `tencent/hy3` (262144 ctx, $0.13/M in, $0.53/M out — cheap enough that
   the budget argument is gone).  But `TEND_THINK`, the doors' `calls`
   16 → 10 that he changed today, and a new model are three variables
   against the 08-31 baseline.  Reliability first on the same model, then
   the model.
3. **Read the file, not the prose about the file** — the `-c 2048` error.
   The board's kaizen paragraphs are *history* and are deliberately not
   rewritten, which makes them exactly the wrong place to read a current
   number from.  One face; the ingestion will say if it recurs.
4. **A session commits by path, never `-A`** — one face, mine, and the
   near-miss is the whole of the evidence.
5. **The braced-expansion memory is not holding — four faces today, and
   the kaizen that recorded them was the fourth.**  `2026-08-26-1712`'s
   trigger ("a fourth is a mechanism owed") is the precedent that opened
   `card:lost-write.md`, and the same trigger has now fired for this.  A
   memory that says *what goes wrong* has failed four times in a day; what
   would hold is a check on the command before it runs, the way
   `lost-write` put the kernel in front of the write instead of a rule in
   front of the hand.  Named here, not built — a mechanism is a card, and
   the card is Henri's to open.
