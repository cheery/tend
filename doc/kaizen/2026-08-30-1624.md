# kaizen — sitting of 2026-08-30, 16:24 (after the close: a rule read as a budget, and the seat that answers it)

Two commits after kaizen 0952, on Henri's break: the seat line's one
sentence, and his README rule.  Written at the clock's ten minutes.

## What went right, and why

**A rule was measured on the mind it was written for within the
hour.**  Henri wrote "The word that carries no load is noise" into
README and watched qwen read it as a budget on tool calls.  The fix
went where the door mind reads — the seat line, nine words — and the
README paragraph says what the rule is not, in his words.  The test
holds the sentence, at 95 of 150 words.

**The second tooled turn conditioned.**  "How many fingers do I
have?" — four calls to establish an absence, then "I do not know",
refusing to guess a number into the record.  A memory carries it to
the card at the next sitting's start.

## What went wrong, and whose

**Mine: a grep anchored on a line start for a phrase mid-line** —
the gate `&&` blocked a correct patch once.  The patch's own assert is
the anchor check; the grep before it must match the file's shape, not
mine.

## What should change tomorrow

1. The second turn onto `card:tools.md`: the conditioning, the seat
   naming the parts, the second `private.md` specimen.
2. `readchars`, then `compare.py`.
3. `deliver.sh` says a door's error in one line (`429 … rate-limited
   upstream`), not the raw JSON.
