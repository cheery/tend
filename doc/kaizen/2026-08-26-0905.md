# Kaizen, 2026-08-26 09:05 — the outside run came and caught a detector: a false red from a one-seat oracle

*Written because the lamp lit on `9b49e91` and `bcf899b`.  gestate-50,
relaying at Henri's ask: he ran `python tools/suite.py` in `~/tend` from
his own terminal and one sandbox test failed while the fence held.*

## What the sitting was

`board/green.md`'s owed unfenced run happened, and it did exactly what
the card said an outside seat would: it caught a detector, not the
fence.  `test_the_fences_own_code_is_read_only_inside` went red on a
fence that was up — the write's EROFS landed in stderr (dash prints a
failed `>>` before `2>&1` applies), the assert read only stdout, and
`mv`/`rm` gave EBUSY over a writable `tools/`, not the EROFS wanted.
gestate-50 diagnosed it from Henri's paste.  Fixed both the failing
test and its latent twin: read both streams, `LC_ALL=C`, EROFS for the
write only.

## What went right

**The card predicted the seat, and the seat paid out the same day.**
The section written an hour earlier said sandbox was `none — nothing
can, from this seat`, owed to an unfenced run; the run came and returned
a real finding.  The measurement the card could not take from inside was
worth taking, and a second seat is what took it.

**A false red was read as one, and named as F88's twin.**  F88 was a
test green with the defect back; this was a test red with no defect at
all — the same failing (an oracle that has only run in one place) from
the mirror side.  That framing is new on the card and is the sitting's
one idea: not every survivor is a false green; a one-seat detector can
be a false red in another seat.

**The fix was verified as far as this seat allows, and no further.**
The assertion logic was run against the three real string-shapes —
Henri's file-bind, a whole-dir ro bind, a dropped `--ro-bind` — and got
green, green, red.  What it could not do, run bwrap, is named as owed
rather than claimed: the drop-a-ro-bind mutation belongs to the next
unfenced run, and the reply to gestate-50 asks for exactly it.

## What went wrong, named as the session's

**The detector this fix repairs is one I could not run while repairing
it**, and that is the uncomfortable middle the green card is most about.
I edited a test whose green, on my machine, means only that it skipped.
The honest guard was to verify the *logic* against the real strings and
to hand the *behavioural* proof to the seat that has bwrap — but a
reader should know the commit's own suite run proves nothing about this
test, and the commit message and the card both say so.

**I fixed the passing twin too, unasked.**  gestate-50 flagged one
failing test; I changed a second that was green.  The case for it is
real — identical latent bug, one binding away from firing — and the
logic is verified, but it is a passing detector edited on reasoning, and
that is the shape yesterday's batch-1 lesson warned about in a different
dress.  Marked here; the unfenced run covers both.

## What should change tomorrow

1. The unfenced mutation is owed: outside the fence, drop one
   `--ro-bind` from `sandbox.sh`'s protected loop and confirm the fixed
   test goes red.  Then sandbox's row on `board/green.md` is `a test,
   named` by execution, not only by logic.
2. Before editing a detector I cannot run, say so in the first line of
   the change, not the last — the reader decides how much the green is
   worth, and they can only do that if the seat is named up front.
3. `green` now has a false-red finding beside its false-green ones; the
   tally's shape is "a one-seat oracle is a claim about that seat", and
   it is worth watching whether the outside run turns up more of them.
