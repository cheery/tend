# Kaizen, 2026-08-25 06:46 — reach, measured instead of designed

*Written because the lamp lit on `574eda3`.  Henri asked whether the
session wanted to stop or continue `work-environment-ai`; it
continued, with the slice the card itself named: the reach table,
written from what a real run under bwrap complains about.  Thirteen
runs, one record, one commit.*

## What the sitting was

`doc/experiments/2026-08-25-reach.md` and the fence beside it.  The
suite passes fenced.  The sitting limit does not survive a fence.
Seven reaches, read off the complaints, given to Henri as a table and
not as a mechanism.

## What went right

**Measured, not designed.**  The card had said in advance that the
table gets written from complaints, and the sharpest row — the state
directory, without which a fenced session gets a fresh sitting clock —
is one no design pass would have produced, because it is a cord let
*through*, not a threat kept out.  Henri's "no vocabulary yet" was the
right instruction and it paid at the first attempt.

**Two of the session's own errors were caught by re-running.**  The
first pass invoked `limit.sh` with `sh` and read the leash ledger in a
second fence; both complaints were the session's, not the fence's, and
both were re-run before anything was written down.  The record says
so.

**Stopped at the table.**  Building the state-directory bind was one
line of bwrap away and the session did not; the card says the table is
for Henri to read first, and the mediation-order decision is his.

**The fence hook's first count**: it has run on every prompt since it
was installed and said nothing.  Zero false alarms, zero prompts of
noise.

## What went wrong, named as the session's

**The audit's reach was measured from the wrong side.**  The record
tested tend reaching gestate; the real case is gestate's audit
reaching tend, unfenced, from a gestate session.  Noted in the record;
not measured.

**"Not built" is a phrase that can hide a choice.**  The session
decided not to build the bind and called it the card's instruction.
It was, and it was also the session's judgement about the size of the
next step.  Both are true and the record only says the first.

## What should change tomorrow

1. Henri reads the seven-row table and strikes or adds rows; that is
   the vocabulary's second draft, and the first one he has touched.
2. If the fence is to be built, the state-directory bind is first and
   `--check` proves the clock is the host's before anything else.
3. The fence hook's false-alarm count stays at zero until it does not;
   the next kaizen says the number.
