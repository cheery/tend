# Kaizen, 2026-08-26 07:28 — the four survivors closed, each shown red before it was trusted

*Written because the lamp lit on `bc9c481`.  Henri: "ok, measure, then
build them."*

## What the sitting was

The four cheap repairs the morning's measurement listed on
`board/green.md`: `test_a_hook_removed_is_red` over all three of the
fence's hook lines; `test_precommit.py` driving the real hook against a
scratch commit whose suite says no, in place of asserting a string;
`test/test_suite.py`, new, reading `suite.py`'s exit code; the `done —`
and `shelved —` date held by `test_board.py`.  Order as told: the before
was already on the card (four rows GREEN), each repair was written, the
same break rerun against it in a fresh copy, and each row is now red by
a test that names it.  Suite 178 → 195.

## What went right

**Every repair was broken before it was trusted**, F88's rule, and the
harness from the morning made that one line per repair rather than a
new fixture each.  The order "measure, then build" costs nothing when
the measuring tool already exists.

**The first run of the new pre-commit test refused for the wrong
reason** — the scratch copy of the hook was not executable and the
shim's `exec` was what said no — and the test caught it, because it
asserts *"a gate failed"* and not merely a non-zero exit.  A test that
checked the exit code alone would have passed on a broken fixture: the
same blindness the test exists to close, one layer in.

## What went wrong, named as the session's

**The fixture bug was mine and it was the same class as the morning's
two**: a copy that was not quite the original (mode bits, this time).
Three fixture errors in one day, each caught by an output that read
wrong rather than by a check.  The pattern is that `write_text` of a
script is not a copy of a script; `shutil.copy` would have carried the
mode.  `_scratch` now chmods, and the next scratch copy of anything
executable should be a copy.

**One command ran from the wrong directory** (`python3 tools/suite.py`
after a `cd` into the scratchpad) and failed loudly; harmless, but the
memory about folding commands applies to mine too — the chain was long
enough to lose its cwd.

## What should change tomorrow

1. `board/green.md` stays open with one question: does the thirty-line
   harness join `tools/` beside `seedmutate.sh`'s shape, or stay the
   thing one hand does in scratch.  Henri's tiebreak; the card warns
   against a framework and the harness is not one yet.
2. Ten test files unmeasured; `test_kaizen.py` and `test_limit.py`
   first, since their hook lines are the ones the fence was found not
   to watch.
3. gestate-50 has the morning's numbers and two vocabulary questions
   and had not answered by this commit; the answers go on the card.
