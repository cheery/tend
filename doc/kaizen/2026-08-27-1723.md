# Kaizen, 2026-08-27 17:23 — a card for the wait, a checker for the twin, a command for each restraint

*Covers c45de08..HEAD — three commits: `lander` opened (470deb9), the
twin's checker (19ce40c), the `tend-<name>` commands (ee466b2).  An
hour into a 120-minute sitting; if it goes on, the next session
inherits the lamp.*

## What it was

Henri: "Write the lander card, make a checker for the tend-sheets.html"
— then, mid-turn, "neat symlinks into bin, eg. tend-keep
tend-reach-allow … or something else similar."

## What went right

**The card holds its decision open with a count.**  `lander`'s
`because` is the wait between the gate and the machine, measured twice
today; the 0710 shape (install on green gates) is on the card *with the
reason it is a door* — the gates are session-writable tests — and day
one is a lamp, which is also the measurement that would make the actor
unnecessary.  The card does not decide; it says what would decide it.

**The checker earned its standard on day one.**  Its first run was red
on seven lines, and reading them sorted two kinds: three were the
twin's rendering convention (tools by basename in `where` spans, a
heading's em-dash tail as a chip) — the standard learned them; one was
a real omission (R3 did not name `card:self.md`) — the twin was fixed.
A checker that had gone green on its first run would have measured
nothing.

**Wrappers, not symlinks, for a reason read off the scripts.**  Every
installed script finds its siblings by `dirname "$0"`; a symlink would
have put `$0` in `/usr/local/bin` and broken all twelve.  A two-line
`exec` has no `$0` problem and can supply `TEND_TREE` from the tree
you stand in — which is the thing Henri actually wanted: to call the
installed version from where he is.

## What went wrong — named as whose

**The session's: the bindir default fell through to `/usr/local/bin`
for a tmp prefix**, and the *other* install tests went red on a
read-only filesystem.  Found by the suite in the same minute; the
default now follows the prefix's shape.  Small, but it is the third
time today a default was written for the one case in front of the
session and not the cases the tests already had.

**Nobody's, noted: the commands are committed and not on the machine**
until `sudo tools/install.sh` — the `lander` card's own wait, live.

## What should change

1. **A default is written for the tests' cases first**, then the real
   one — the tests are the enumeration of shapes the tree already
   knows.
2. **`lander` day one — the lamp — is one slice of `install.sh
   --check` with a `--hook` form**; if Henri places the card, it is an
   afternoon.
3. `sudo tools/install.sh` puts `tend-<name>` on the machine; after it,
   `tend-fence`, `tend-andon pending`, `tend-limit` are the lines to
   hand him instead of paths.
