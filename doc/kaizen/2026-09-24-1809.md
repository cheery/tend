# Kaizen — 2026-09-24, the 18:09 sitting: the wire built, the model wrote the node, chapter 1 closed

*One file for one sitting — the extension of the 17:43 one, on his
"I extended the time", its first commit 18:09.  Seven commits before
this file, read off `git log 50a33d1..HEAD`: `be05164`, `0a20646`,
`8a28eb1`, `7445ffd`, `8caa6c4`, `37efd2e`, `8b1f6f6`.  His lines: "I
extended the time.  Continue on building the wire."; two pastes of
`tools/launch.sh ask-kude run`; "fix F030"; "anthropic sonnet"; his pick
"Type + language reference"; the paste of `tools/kudewrite.py --door
anthropic` and the model's node's run; "close the chapter"; "write the
kaizen and close".  Written in the defect shape.*

**What it was.**  Chapter 1 from its first code to its close in one
sitting.  The wire (`be05164`): the llm as a channel at `Llm`, whose
types the checker owns beside `Console`, a party that plays it as
`ask/ask.py` does, and `ask-kude/` — the three refusals of the done
line from the type alone, red first on `no type Llm`.  His run against
the real llm (`0a20646`), and in it keep's deprecation warning, the
second written sighting after F022's note, so F030; fixed at his word
with the layout measured first (`8a28eb1`), and resolved on his second
run (`7445ffd`).  The model's half: `tools/kudewrite.py` and
`ask-kude-model/` (`8caa6c4`), then his run of it — Sonnet in three
tries, refused twice for the language's rules and never for the
protocol, its program run against the real llm (`37efd2e`).  The chapter
closed at his word (`8b1f6f6`).  The whole suite 1077 passed, 18
skipped, at `8caa6c4`.

**What went right, and why.**  Each decision that shaped a measurement
went to him before it was built on: the door, and what the model is
handed — the one question that decided what the count would mean.  Red
first held every time, and each mutate row was run, not written and
trusted: ten rows this sitting, all red on the test they name.  Two
counts disagreed with what was expected (1069 for 1067, 1077 for 1075)
and each was traced to the tests that parametrize on new files before
the number went into prose.  keep's fix was measured on the machine's
own Python before a line was changed, and `gcc-sysv`'s refusal is why
the fix is `ms` and not a guess.  And `proposals/` stayed his to land:
the record is quoted in the card, not copied into the tree.

**What went wrong, whose.**  Mine, three.
1. A `sed -i` whose line address was a braced shell variable: the
   harness blanked it, the address vanished, and the mutate row went in
   after every line of `tools/mutate.sh`.  The memory
   `bash-heredoc-expansion` names exactly this, and the fence's hook
   catches only a heredoc — the second face of the day after the first
   sitting's heredoc.  Caught by reading the diff before staging;
   repaired by deleting every exact copy of the row and diffing against
   HEAD; the memory now carries this face.  The mechanism that would
   hold is still the one its 09-01 kaizen named: a check on the command
   before it runs, for any braced expansion, not only a heredoc's.
2. A test assertion that could not fail: the flock tried after the run
   ended, when the process's exit drops the lock whether or not
   `let_go` did.  Seen before the commit and replaced by an in-process
   test that tries the edge between `let_go` and the end, with a mutate
   row that shows it bites.  The rule it broke is the tree's own — a
   fixture has something on both sides of the seam.
3. "The nodes run the installed copy", written into F030 and told to
   him as the reason for his install: half true — `tools/launch.sh` runs
   the `keep.py` beside it, and his runs were from the tree.  Found when
   the installed path in my check did not exist and the warning's own
   path was the tree's; corrected in the entry beside the sentence.  The
   install was still right for the tick's copy, but he was given a
   reason that was not the one.

**What should change tomorrow.**  Chapter 2's goal is his; nothing is in
`doing`.  Waiting on his word from today: `card:one-piece.md`'s done line
and its gate; the two stale sentences from the first sitting (vision.md's
table calling the goal gate unbuilt, one-piece's own card still saying
three cards sit in `doing`); whether the kudewrite record lands under
`doc/experiments/`; and whether `ask-kude`'s grant takes `material
../board/README.md`, so it answers about the tree and not the word.
