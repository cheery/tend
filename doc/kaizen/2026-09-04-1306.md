# Kaizen — 2026-09-04, the 12:56 sitting: the kept loopback door, and a whole suite red that was not its

*One file for one sitting, 12:56 to his "nyt voidaan lopetella hetkeksi"
at about 13:21, thirty minutes declared and about 25 used; four commits
(`677b971`, `ddb4cb2`, `c42fb22`, and this file).  The lamp names the
file 13:06 by the first commit; the sitting began at 12:56.*

**What it was.**  His "tee session-program" at the top of a
thirty-minute clock.  The card's own next line since 2026-09-02 was
picked and built: `lead.sh NODE --kept --door llm` runs under keep — the
same confinement as a kept node turn plus one read outside the tree,
the door's key file — with health read at the door's base, and a door
that calls out still refused under `--kept`.  One test, both halves;
the old refusal test folded into it.  Then the whole suite to a file,
because the hook said the program tests do not run in it: five reds,
all the ask node's, none touching the change.  Read from the file:
the copied `ask/grant` carries `material ../board/README.md` and the
scratch tree has no board, so the node never started; with a board
given, three assertions written on 2026-09-02 for an ask with no
material — `ANSWER to: Say hi` verbatim, `800` in the cut line.  F024,
with the fixture half fixed and the three assertions left as the open
work.  Then his shell ran the live form: pull, wait at the door's
health, a draft under keep, first try — and no `C:` line, so the tools
loop inside a kept turn is still unrun.

**What went right.**  The suite went to a file and was read from the
file, both times, and the five reds were named as not the change's
from the assertion lines and not from the names — yesterday's
costliest wrong not repeated.  The cause was measured, not guessed:
the board added to the scratch tree turned five reds into three and
the three said exactly what they wanted.  The clock was read at every
step and the three-assertion fix was left on F024 at thirteen minutes
rather than done at speed — the tortoise, the first time it was the
choice and not the lesson.  The live turn was his to run and he ran
it inside the same sitting, so the card carries a measurement and not
a claim.

**Wrong, mine.**  Three.  (1) No mutate row: the new test's red
against the previous `lead.sh` is reasoned in the card — the loopback
half exits 1 under the old refusal — and not run, and the old refusal
message already contained "calls out", so the refusal half of the test
would have passed against the old code too; the card says "by its
first half's exit code", which is the honest scope, but a row would
have cost a minute.  (2) `test/test_doors.py` was named from memory,
"no tests ran" came back, and the next command found `test_door.py` —
a file name eyeballed.  (3) The card's entry says "13:0x" for a section
whose commit is timestamped; the lamp's own rule is a number counted,
and the x is a number not looked up.

**Left standing.**  F024's three assertions.  The tools loop inside a
kept turn, unmeasured: `lead.sh` has no knob to hold the digest back,
and that knob is the reading.  Still the card's: the node's sitting
against a loop longer than ten minutes, and `finish_reason` on the
not-a-completion line.  His queue is unchanged at nine; this sitting
placed no mark and no question.

**Tomorrow.**  His pick.  F024 is a fifteen-minute fix with the three
strings read off the run in `fixme/F024.md`.  The next meter run is
diffed against `doc/meter-2026-09-04.md`.  And the line under this
paragraph is his, whenever he has the digit: `henri: N — words`.
