# kaizen — sitting of 2026-08-29, 20:48 (the last minutes; the kanban sheet corrected, and the ledger's first catch)

One commit since `64658d2` (kaizen 2031): `281b425`, `spec/kanban.md`
corrected at Henri's word — *"the session installs the board.  It is
session's work, and session is good at doing it."* — plus this commit,
which carries the card note below and this file.  Written after his
"close": the correction arrived after it, and the sitting's clock had
been retaken by hand twice; the fifth kaizen file of one evening, and
the reason it exists is the finding below, not the correction.

## What went right, and why

**The ledger caught its first flake on its first evening, and the
flake was the session's own.**  Two commits — kaizen 2031 and the
kanban correction — were each refused once and landed unchanged on
retry.  The session had filtered the gate's output to nothing both
times (kaizen 1337's rule 1, broken twice in an hour).  What said
which test was `~/.local/state/tend/failed.log`: two `gate` lines
naming `test_a_program_busy_in_bursts_is_busy_over_the_idle_window`,
load 0.70 and 0.62, wall 214 and 210 s — the window rule's own test,
written at "do 1", failing 2 of 4 gate runs at no load.  Without the
ledger this would have been "the gate flaked" twice and nothing
written; with it, the card has a signature, a suspected cause (the
first window's slide and the idle check on the same tick when keep is
slow to start), and the next sitting's first command.

**The correction was the same one twice, and it was right both
times.**  Kaizen and kanban sheets each made the install the author's
because the mechanism touches hooks or gates; the person's rule is
that the session installs what it agreed to, and the tree is the
proof.  Both sheets now say so in his words.

## What went wrong, and whose

**The session's: a test landed unshaken.**  Kaizen 1934, item 3, the
same evening: *a shake after every rule change to the launcher's
idle.*  The burst test was written, went red then green once, and
rode into `25e1d2e` without the shake that the card had just made a
habit.  The gate then failed it twice.  The tool was there; the habit
was one commit old.

**The session's: the gate's output filtered, twice more.**  The rule
from 13:37 — read the gate whole when a commit is refused — was
broken at 20:40 and 20:48, and each time the session learned the
commit had not landed only from `git log`.  The ledger covered for it;
it should not have had to.

## What should change tomorrow

1. **Shake the burst test first** — `tools/suite.py --shake
   test/test_launch.py::test_a_program_busy_in_bursts_is_busy_over_the_idle_window 20`,
   without burners, then with — and read the launcher's ticks under it
   (item 2 of 1934).  Fix the rule or the fixture; never mark the test.
2. **A commit's output is read whole, every time**, not only when a
   refusal is suspected: `git commit … 2>&1 | tail -30` at minimum, and
   the ledger's line quoted in the retry's message.
3. **A test on the launcher's idle is shaken before it lands** — the
   card's rule, now with a specimen.
4. **Five kaizens in one evening is the extension problem, measured
   three times.**  The lamp reads a retaken sitting as a new one;
   `sitting-everywhere` and the limit own the fix (an extension within
   minutes is a continuation); until then, the person's "close" is the
   end and nothing after it gets a file of its own — the next sitting's
   kaizen covers it.
