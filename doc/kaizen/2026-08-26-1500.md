# Kaizen, 2026-08-26 15:00 — the resolver installed, a pull served from outside, and a key left off the list

*Written because the lamp lit on Henri's commit `977b523` (both
resolver patches, `--protect`) and the commit after it.  Henri's words
through the hour: the fence-down report, "isn't this the same error?",
"now the git commit went through without issues.. hm."*

## What the sitting was

The resolver went live.  Henri's hand applied two patches, ran the
key, installed the hook.  A session's pull inside the fence was served
by a runner started on his side between two commands, which outlived
the command, stopped on idle, and left a leash line.  And a test that
failed twice from his terminal passed under the gate — a flake on one
seat, recorded on two cards and not yet explained.

## What went right

**The fence caught the missing rule at the next prompt.**  The patch
put `resolve.sh` in the protected set; the deny-list did not have its
`Edit` rule; `fence.sh --hook` said "MISSING" with the exact line and
the exact key.  Nothing had to be diagnosed.

**The second patch's message was the measurement.**  "Started one, and
it has not taken the lock after 10s" is what replaced "started one":
the failing run now said what happened rather than what was hoped.
The ledger then added that the runner had run and exited 0 — so the
fact is narrower than "the runner did not start," and the card says
the narrow fact.

**The demonstration was run, not described.**  One pull, one look, the
state and the ledger read back.

## What went wrong, named as the session's

**A key was left off Henri's list.**  This session had handed him
`--protect` once already today for `node/run.sh`; for `resolve.sh` it
wrote the patch, the suite line and the commit line and not the key.
One prompt of his.  The countermeasure is mechanical: a patch that
touches `protected=` in `sandbox.sh` is handed with `tools/fence.sh
--protect` in the same block, always — the check would catch the
omission, and did, but the check's minute is his.

**Two guesses at a seat I cannot see.**  The first fix (3 s → wait for
lock) and the second (serialize, 10 s) were both reasoned from here
about a leash mode that does not run here.  The second was still worth
making — it is right regardless — but the honest move came third:
asking for `run.log`.  And that ask sent him through `!`, which is a
session's seat with an empty `/tmp`, so it found nothing; his own
terminal was the right place and I should have said so first.  A fact
for `fence`: `!` in a session is the session's fence, not the person's
shell.

**The flake is not explained.**  Named as such on `resolver` and
`green`; a count from Henri's seat comes before any reasoning.

## What should change tomorrow

1. Handing a `sandbox.sh` patch: the block is `git apply`, `fence.sh
   --protect`, suite, commit — never without the key.
2. Reading a pytest tmpdir on Henri's seat: his terminal, not `!`.
3. The flake: one loop on his seat, `N` runs of the one test, the count
   on `green`.  Then the reasoning.
4. The last bind — `node/state` read-only to the session, the pull file
   writable, `.venv` read-only — is safe now that the hook serves what
   a session pulls.  A fence patch, with the key.
