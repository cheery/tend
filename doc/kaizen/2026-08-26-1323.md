# Kaizen, 2026-08-26 13:23 — the resolver carded, and news that changes who the puller is

*Written because the lamp lit on `273b78b`.  Henri: "Do a card for the
resolver," and "it's ok that you answered late.  Do the question first
next time."  And the news: models on his work laptop at 5–9 t/s through
SYCL — "the llama.cpp arc has appeared.  We may be able to do a fully
local system."*

## What the sitting was

One card.  `board/resolver.md`: nothing here starts a program but a
hand in a shell, so the grant a program gets is whatever its launcher
baked in, and a pull with no runner is a line nobody reads.  Henri's
frame — the person always pulls, never starts — makes the pull the
launch and so the one place a grant is applied.  Placed at 3, below
`keep` (it is the route to that card's session half), the tiebreak his.
The news kept beside the card, not in it: a local model as the puller
is the same shape one level up, and is its own card if it becomes work.
`test_board.py` green, 58; the gates held.

## What went right

**The card names a problem and refuses the design.**  The `because` is
the gap — who starts, who restricts — in Henri's words; the shape is
written "so the first measurement has something to disagree with," and
the cost (a resolver is the first long-running thing tend would own, and
the leash and the sitting limit would both have to know it exists) is
in the card rather than discovered on day one.  §"What would make this
card wrong" asks whether *pulling* is *starting* in `vision.md`'s own
sentence, which is the question day one should read first.

**No question in the message, so no order to get wrong** — but the
13:17 kaizen's rule was written into memory this sitting anyway,
because Henri confirmed it: "Do the question first next time."

## What went wrong, named as the session's

**The memory write went through the fence and failed silently in the
middle of a longer command.**  `cat > ~/.claude/.../memory/x.md`
inside the fenced shell hit the empty home — two Finnish "no such file"
lines in a run whose last line was `58 passed`.  Caught because the
whole output was read, which is the 08-25-0721 lesson (`tail -N` hides
the reason) applied rather than re-learned.  The countermeasure is
already the tree's: the dedicated tools run outside the fence, and the
memory directory is theirs.  One more row in the reach table's spirit —
a place the fence's empty home refuses that a session will want — and
it is not a widening; the right tool exists.

## What should change tomorrow

1. Memory files: the Write/Edit tools, never the fenced shell.  Known
   now; noted so it is not remade.
2. `resolver` day one is a measurement — how a pull reaches a resolver
   without a daemon that outlives the sitting; what `pull` itself could
   do first.  Not begun.  The card's owed demonstration is one line:
   the node, pulled, starts confined.
3. The local-model news is in memory and beside the card.  If Henri
   brings it as work, it is a card of its own — a session that is a
   program tend runs, under a grant, pulled.
