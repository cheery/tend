# Kaizen — 2026-09-04, the tail of the 13:2x sitting: one commit after its kaizen

*One file for one commit (`f722547`, read off `git log -1` before this
line was written — the first draft of this line carried a hash that was
not on the log at all), the correction of the 13:30 kaizen's own first line, made after
that kaizen went in — so the lamp lit again and this file is what it
asked for.  The two-commit tail is the shape batch 10 promoted to the
board on the morning of this same day.*

**What it was.**  The 13:30 kaizen named the sitting's first commit as
`22ccf31`, which is the *previous* sitting's kaizen — the hash copied
off the lamp's "since the last kaizen (22ccf31)" line, not off `git
log`.  Read back from the log after the commit, fixed, committed: a
commit after the kaizen.

**Wrong, mine.**  The fix belonged in the kaizen's own commit: read
the log before writing the hash, not after.  The fifth face of *count
before you write it*, in its plainest form — an identifier is a number
too, and the lamp's line is not the log.  A memory carries the
count.

**Left standing.**  Nothing new.  His line: `henri: N — words`.
