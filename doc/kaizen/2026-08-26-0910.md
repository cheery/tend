# Kaizen, 2026-08-26 09:12 — the outside seat finished the measurement, and named two limits of the fence

*Written because the lamp lit on `57eab61`.  gestate-50 ran the owed
mutation from outside the fence and reported it.*

## What the sitting was

The close of the sandbox measurement `green` had left owed to an
unfenced seat.  gestate-50 ran control-then-mutation on a clone: the fix
passes 13 including bwrap, and dropping `sandbox.sh`'s own `--ro-bind`
turns `--check` to THE FENCE IS DOWN and the read-only test red.  So the
row is `a test, named` by execution.  Two findings came with it — the
mutation is destructive when it fires, and a tree under the rw scratch
bind is unprotected — both recorded, one as a test docstring note, one
as a header patch for Henri.

## What went right

**The two seats did the whole loop between them, and neither could have
alone.**  This seat wrote the fix and verified its logic; that seat ran
the bwrap proof this one cannot nest.  The card's "owed from an unfenced
seat" was not a hedge — it was a real division of a measurement across
two machines, and it closed the same day it opened.

**The control clone caught what a bare mutation would have miscounted.**
gestate ran the mutation first by mistake, into a scratch-dir clone
where the fence protected nothing, saw eight reds, and it was the
*control* — the intact clone read green first — that told a fence-edge
apart from a real result.  That is `mutate.sh`'s own rule (read the
intact copy before any row) holding across the fence, arrived at
independently, which is the strongest kind of agreement.

**A fence limit became a written line, not a mood.**  The scratch-dir
precedence hole is not tend's shape in practice, but it is a true thing
about the fence, and the choice was to write it down where the fence is
defined rather than leave it for the next person to rediscover.

## What went wrong, named as the session's

**Nothing new this session was the session's error** — the two mistakes
in the loop were the other seat's (mutation before control) and were
caught by the other seat's own control.  What this session owes forward
is only that the destructive-probe property was not foreseen when the
fix was written: I handed a mutation to be run without saying it would
rename the real script if the fence were down.  The docstring says it
now; it should have said it in the reply that first asked for the run.

## What should change tomorrow

1. `sandbox-limit.patch` is Henri's to apply — a header line that the
   fence cannot protect a tree under a writable row's path.  Until then
   the limit lives only in `board/green.md` and this kaizen.
2. `green`'s day-one measurement is now complete for every detector the
   suite holds.  The card can shift from a build to a standing practice:
   a new detector is measured when it lands, and `mutate.sh` re-reads
   the recorded rows.  Whether that shift is written into the card is
   Henri's call.
3. When a run is handed to another seat, its destructive edges are named
   in the handing, not discovered in the running.
