# Kaizen, 2026-08-28 10:17 — the andon chased to ground: player, then socket, then the fence

*Written at the sitting's close; three commits since the last kaizen
(`37678be`): `c916604` (toolbox player), `a3e8b08` and the silent-cord
confirmation.  Henri's lines: the jidoka round trip pasted, "lets
install pw-play so that andon sounds.. put it into the tools/toobox",
the host `systemctl`/`ls` output pasted, and `tools/andon.sh ring 1`
rung clean from his shell.*

## What the sitting was

* **The live round trip closed brick 1**: `tools/deliver.sh llm "What is
  jidoka…"` pulled, started the node, waited, and printed gemma's
  answer — the first real answer tend's own delivery carried.  The
  answer was wrong (gemma cold called jidoka a Buddhist non-attachment
  practice; it is Toyota's stop-the-line), which is the point: it is the
  brick-2 baseline, gemma with no tree, to measure the conditioned run
  against.
* **The andon's player declared in the toolbox** (`c916604`), red first
  — and the same edit says a green means "a player exists", never "the
  andon will sound", because the socket is runtime.
* **The andon chased to its real block** (`a3e8b08`, confirm): the
  player was present all along; the socket exists on the host since
  06:26; and yet a fenced session with `REACH=audio` allowed still
  cannot see `pipewire-0`.  The row is necessary and not sufficient — a
  session's per-command `--reach audio` nests inside its own fence, which
  never bound the socket.  Rung from Henri's shell outside the fence it
  sounded clean.  `silent-cord` now names the two builds: audio in the
  session's own fence, or the ring delegated host-side.

## What went right

**The andon was chased to ground instead of worked around.**  "Install
pw-play" was the asked fix; the honest one was three layers down (player
present → socket present → fence blocks), and each layer was measured
before the next was named.  The card that catches this — `silent-cord`,
opened this morning — went from a design worry to three dated
measurements and two concrete builds in one sitting, which is the board
doing its job.

**Delivery earned its keep on the first real question.**  One command,
a real answer, written to `replies` — and the answer's being wrong is
worth more to the project than a right one would have been, because it
is the baseline the whole conditioning question rests on.

## What went wrong, named as the session's

**"Install pw-play" was nearly taken at face value.**  The player was
already present; installing it would have changed nothing and hidden the
socket. The correction was made before acting — pw-play was declared in
the toolbox as the genuinely-useful part, and the socket named as the
real gap — but the ask pointed at the wrong layer and the session had to
catch it rather than follow it.  A fix that is asked is still measured.

**The 10:07 "no socket" reading was the fence's view, stated as the
machine's.**  It said "no PipeWire socket on the work laptop"; the
socket was there, unseen through the fence.  Corrected at 10:18 when
Henri checked the host.  A session inside the fence must say "I cannot
see X", not "X is absent" — the same seat-of-the-reader lesson the
`check` cards carry.

## What should change tomorrow

1. **`silent-cord`'s build** — audio in the session fence, or the ring
   across the resolver seam; the sound is proven, only its carriage is
   left.
2. **Brick 2** — gemma acting on what it reads; the jidoka baseline is
   captured and waiting.
3. **A session names its seat** — "I cannot see it from here" over "it
   is not there"; one face, the session's, and the `check` cards' too.
