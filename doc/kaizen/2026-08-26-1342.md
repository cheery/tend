# Kaizen, 2026-08-26 13:42 — the launcher joined the set by Henri's hand, and three cards read by their ledgers

*Written because the lamp lit on `6f310db` — Henri's commit, the
patch applied — and on the want this session left for it; and on
`3 readings` after.  Henri: "add node/run.sh to the protected set.
Then lets look at what is open now," then "ok, work-environment-ai and
green, and fence."*

## What the sitting was

`node/run.sh` into the protected set: a patch and a red test handed
over, applied by Henri, `6f310db`; the launcher is read-only from the
fence now.  Then three cards, each answered by a measurement that was
already lying in a ledger: `work-environment-ai`'s owed scope run has
been running for a day (236 scope records, 149 with cpu, 4 wall kills);
`green`'s owed test written seam-first and red by row; `fence`'s
"sitting of ordinary work, counted" — this one, 53 commands, 3
refusals, all the fence's, none the work's.  One new fact: with `net`
off the fence has loopback and nothing else.

## What went right

**The want carried the reason across a hand.**  A red test in the tree
meant no commit could pass the gate, so no kaizen could be committed;
`tools/kaizen.sh want` said so, the lamp held it, Henri's commit lit it
by name.  The rule — a session never judges whether it owes, it says
so — worked exactly once, which is the first time it has been needed.

**Three cards, no builds.**  Each was closed a step by reading an
instrument the tree already had.  The leash ledger answered a card that
had said "owed" for a day; the sitting itself was the fence's
demonstration.  That is the manifesto's sense of a demonstration and it
cost nothing but parsing.

**A surprise was measured, not explained.**  The network test's
loopback connect from inside the fence had to mean either the row was
on or bwrap raised `lo`.  Both halves checked — outside unreachable,
row unset — before a word went on the card.

## What went wrong, named as the session's

**The first ledger count was wrong by an order of magnitude and read as
if it were right.**  A line-based `awk` over a ledger whose command
field spans lines gave "other 5864" — continuation lines counted as
records — and "commands: 2978" for a sitting that ran 53.  The numbers
were nonsense on their face and I read past them to the next line.
Caught because the exits column printed heredoc fragments, which could
not be exit codes.  The countermeasure is not "read the output": the
ledger has a record shape (epoch, tab) and nothing in the tree parses
it — the second attempt did, in a heredoc, and is now in this file and
nowhere reusable.  If the ledger is read a second time, that parser is
a tool; the reach table's rule.

## What should change tomorrow

1. A ledger reader — `tools/leash.sh --read` or a `ledger.py` — on the
   second ask.  A record spans lines; a count that does not know that is
   wrong before it is printed.
2. `fence` has its demonstration and `work-environment-ai` has its run;
   whether either card moves is Henri's.  What both still hold is a
   decision of his, not a debt of the tree's.
3. Open now, honestly: `keep` and `resolver` on the session half;
   `kaizen-ingestion` batch 2 tomorrow; `cords` on 08-31; `fence` on the
   `display` caller; `green` on nothing.
