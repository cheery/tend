# kaizen — sitting of 2026-08-29, 21:00 (the close for good; the defect ledger and its sheet)

Two commits since `4c1759b` (kaizen 2048): `859862b`, `fixme/` — the
defect ledger in the board's shape, with its test on the gate and four
entries lifted out of the evening; and `spec/fixme.md`, the sheet, at
Henri's "I think it deserves that."  Written at his *"and next we
close for good, I give you time to write the kaizen"* — the sixth
kaizen file of one desk-day's evening, and the close.

## What went right, and why

**The ledger was asked for the minute it was needed, and its first
entries were already written — in the wrong places.**  Henri: *"I
think that we've reached a point where we need fixme/ -ledger."*  The
point was the failure ledger's first catch (F000), which had nowhere
to sit but a card's dated paragraph.  Four entries in twenty minutes,
each lifted out of prose that would have been read once: a race on a
card, a rule on a card, a harness fault in a kaizen's item 5, a
fixture leak in a commit message.  The mechanism was the shape, not
the writing — the board's block, two shelves, the id in the filename
— and the one rule that is not the board's came from gestate's most
expensive defect lesson: **a resolution names its gate or confesses.**
Two of the four are `none —` resolutions, and each says why.

**The test wrote a rule.**  The citation check refused the ledger's
own README for a bare `F182` — gestate's number — and then refused the
rewrite for the same reason, because the regex did not skip
`gestate:F182`.  Two refusals, one rule: another tree's number carries
its tree, and the one pre-ledger citation in a done card rides as a
baseline that may shrink and never grow.  Gestate's board had
proposed exactly that shape for a rule applied only to what comes
after it; the test found the need before the session read the
proposal.

**Three sheets in one evening, and the third was cheapest.**
`spec/fixme.md` was written in the shape the other two had settled —
author first, the thing, why, what to do, the lift — and its "why it
works" says honestly that it is a reason the thing was *built*, with
the week owed for which reasons held.  A sheet an hour after its
mechanism cannot claim more, and does not.

## What went wrong, and whose

**The session's: a slip in prose, and it was a citation.**  The
README's sentence on the baseline was written with "F182 — wait, that
is gestate's" in it — the session's own thinking left in the file —
and the test caught it as a dangling number before the session read
it back.  The gate held shape; the reader should have held the prose.

**The session's: the gate's output filtered, twice, before the
ledger covered.**  Named in 2048; repeated because this stretch's
commits were read whole (`> $O 2>&1; tail`), and the difference was
the ledger's two lines making the cost visible.

**The tree's: the burst test (F000) passed in both of this stretch's
gate runs.**  Which is what a 2-of-4 defect does, and why F000 is
open and not "seems fine now".

## What should change tomorrow

1. **F000 first, then F001** — the shake twenty times each way, the
   launcher's ticks under it, the rule or the fixture fixed, the gate
   named on the resolved entry.  Never a test marked.
2. **A `gate` that names a test id is checked to exist** — the next
   check in `test_fixme.py`, written the day an entry's gate goes
   missing, or sooner.
3. **F002 gets its mutate row** (kaizen 1934, item 5) and moves from
   `none —` to a named gate.
4. **The three sheets are read against their mechanisms on
   2026-09-05**, and `spec/fixme.md`'s "why it works" is rewritten
   from what held.
5. **The extension problem** — six kaizen files for one evening —
   goes to `card:sitting-everywhere.md` as a want: an extension
   within minutes of the block is the same sitting, and the lamp
   should read it so.  Until then, the person's "close for good" is
   the end; this file is the one that followed it.
6. The four measurements owed since 1918 stand: the tick's first
   night, the door's first turn, the launcher's tick reads, and
   `mutate.sh`'s signing row.
