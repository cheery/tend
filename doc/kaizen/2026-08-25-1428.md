# Kaizen, 2026-08-25 14:28 — a row measured to its refusal, and left there

*Written because the lamp lit on `03bb503`.  Henri: "do the display
measurement. move grant to done."*

## What the sitting was

Two small things asked in one line.  `grant` moved to `done/` with its
`because` no longer standing, the README's priority renumbered and the
two test docstrings that cited it repointed.  And the `display` row
measured with the row granted: every X client inside is refused for
want of the cookie — the bus row's day-one defect, now on `fence` —
and the session is Wayland, so the row's exposure once it works is
every X program, not the compositor's.

## What went right

**The measurement stopped at the first wall and said so.**  The row
refused every client, and the way past — binding the cookie — is a
widening that belongs to whoever brings a caller.  The session did not
bring the cookie in "just to see"; what would have been seen is written
as a bound instead (XWayland: X clients see X clients), and the card
says what was *not* done.  After `bus` turned out to be a door, this is
the right speed for the next row.

**A card moved and nothing broke.**  Moving between shelves never
renames the file (README), so `card:grant.md` citations across the
tree still resolve; the two `board/grant.md` paths in test docstrings
were repointed because a reader follows those by hand.  The board test
held the README to the shelves.

**Henri's settings change rode along.**  The bound tightened to
`net,audio,display` an hour ago and was left uncommitted rather than
owing a kaizen for one line; it went in with this commit, which is
the pattern: a person's settings edit travels with the next sitting's
work, not alone.

## What went wrong, named as the session's

**Nothing in the tree.**  A near thing: the measurement command had
`xdotool getactivewindow` in it — a read, but from the family of tools
that can also type into the person's windows.  It returned the refusal
and nothing else, and the card says no input was sent.  The next such
probe should say in advance which commands are reads and leave the
rest out of the line entirely.

## What should change tomorrow

1. The `display` row has a known defect (no cookie) and no caller.
   When a caller comes, the fix is one `--ro-bind-try` of `$XAUTHORITY`
   and a patch for Henri; the exposure to write down then is "every X
   program".
2. Three rows have now been measured past their names — `audio` is the
   card, `bus` was a door, `display` is every X program.  `net` has not
   been, beyond "it resolves names".  Its turn comes with `pull`, if the
   node ever needs it; measure before granting.
3. The board is `work-environment-ai`, `fence`, `self`, `cords`, `pull`.
   `pull` is the only one with nothing built; `fence` and `self` are
   open for counts.  The next sitting that wants to build has one card.
