# Kaizen, 2026-08-27 07:48 — a short close: the install card, written to the board's discipline

*A brief kaizen closing the sitting's tail.  The 0710 kaizen closed the
batching sitting; Henri then asked for one card and left, so this covers
the one uncovered commit (29ff793..HEAD) rather than leave the lamp lit
for a session that would write it without the context.*

## What it was

`board/install.md` — Henri, seeing the day's effort: "create an install
script that installs this to the machine and protects those files,
rather than the files in this tree ... we do way too much effort here
now."  Written as a card, placed at 2, board and suite green (70, 299).

## What went right

**The because named the problem, though the ask named a solution.**
Henri's words are an install script; the card's `because` is the
problem under it — the workbench and the enforcement are one directory,
and the in-force copy still lives where a session configures it — with
the install-script proposal kept in the body where the board puts
solutions (`test_board.py` checks presence, not this discipline; the
discipline is the writer's).  The card also carries its own "what would
make this wrong" as a measurement (the install must be one a session
cannot drive) rather than an opinion.

**The day's effort became the card's evidence, not a complaint.**  The
core.bare thrash that moved main twice is cited as the cost the card
names, so the card rests on what happened, not on a feeling.

## What went wrong

Nothing in this tail worth naming as a fault.  The standing residuals
are the prior kaizen's: the fetch/worktree foot-guns (in memory), the
lamp baseline bug (unfixed), and the unfenced-lander that `install` now
partly subsumes — a session should not be reaching across the boundary
for convenience, and `install` is the card that would end the reaching.

## What should change

`install` is the session-as-principal work's new neighbour and, if
Henri keeps it at 2, the next lead — it would end the clone-and-outside-
hand tax that this very sitting paid twice.  The afternoon's other
threads stand: work-environment-ai's examination, sitting-everywhere
day two.  His tiebreak on all of it.
