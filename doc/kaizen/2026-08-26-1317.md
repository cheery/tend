# Kaizen, 2026-08-26 13:17 — green's debt was the day's own rows, and a question about who starts a program

*Written because the lamp lit on `d0a96a1`.  Henri: "it stays open for
the session half, work on the green now," and a question — how do
programs get their restrictions, given that the user always pulls and
never starts a program themselves.*

## What the sitting was

Green, read back: every measurement the card owed had landed — the lamp
patch, the sandbox limit note, the unfenced sweep — so the live debt
was the tree's own: keep's write and network slices were broken by hand
today and never recorded as rows.  Five rows added to `tools/mutate.sh`,
each red by the test that names it through the harness.  `keep` marked
open for the session half at Henri's word.  The question answered in
the reply, not built.

## What went right

**The card was read by its open items, not its tail** — the 13:09
kaizen's first "tomorrow", applied an hour later.  It said green had no
owed measurement, which turned the sitting from "find something to do
on green" into "what does the tree owe green," and the answer was
small and exact.

**A fourth mutation came from asking what a careless change would look
like** — the ABI check and the v4 struct removed together, the shape of
a port to an older kernel.  Red by the connect that should have been
refused.  The three by-hand mutations were the ones I had thought of
while building; the fourth was the one a reader would make.

## What went wrong, named as the session's

**The rows were an afterthought, twice today.**  Write-scoping's row
was owed since morning; the net rows were owed at 13:09 and I committed
without them and called the slice done.  The green card now says "a
slice lands with its rows, the same commit" and calls it not yet a
rule — correctly, by the tree's own rule for rules.  But it was the
same session that skipped it twice, so for this session it is a
countermeasure: a keep-shaped commit is not staged until `mutate.sh`
has a row for it.  Noted here so the next kaizen can say whether it
held.

**A question was answered late.**  Henri asked how programs get their
restrictions in the same message as the green ask; the green work was
done first and the answer written last.  The order was fine for the
tree and wrong for the person: a question is a pull, and it was left
unserved for a sitting.  Answer first, then build — the answer costs a
paragraph and the build waits well.

## What should change tomorrow

1. Rows land with the slice, same commit, for anything under `tools/`
   or `node/` — this session's countermeasure, the tree's rule only if
   it is broken again and noticed.
2. A question in the message is served before the work in it.
3. Henri's question — that a program's restrictions should ride the
   *pull*, since nobody starts a program by hand — is not a card yet.
   It is a real shape (the pull path is the launch path, so the grant
   lives where the pull is answered), it touches `keep`'s session half,
   `pull`'s lifecycle and `work-environment-ai`'s broker, and the reply
   says what it would mean.  Whether it becomes a card is his.
