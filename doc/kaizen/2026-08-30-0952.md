# kaizen — sitting of 2026-08-30, 09:52 (tools day one, the private card, batch 6, and a gate that refused twice for the right reasons)

Six commits since `79d2910` (kaizen 0902), plus this one: `4268503`
the three `later/` cards (the morning session, at Henri's "put these
into later/ as cards" — it wrote no kaizen of its own and said so; this
one covers it); `6694d30` tools day one; `ef25fc5` the private card at
his "commit the private entry at some point, with appropriate title";
batch 6 of the ingestion; the private card's second room, at his
"a session also needs a scratchpad that is also private to it"; and
`grep`, the third tool, wanted by the first tooled turn and built the
same sitting at his "write especially grep on the card, and maybe
implement it as well".  The three new timing-sensitive tests were
shaken after the gates: 0 of 10 each under eight burning cores.

**The first tooled turn was his, at 15:07, and it was read before it
was praised.**  `tools  read ls` on the openrouter door, `panel.py
talk --door openrouter llm "what is on the board right now?"`, qwen:
eight calls, the record Q, V, eight `C:`, T, A.  The leash did the
counting — the model's own count was wrong twice and the courier's
eight exact — which is the card's "the fence is the grant" shown on
the first turn.  Three findings went on the card from the record, not
from the answer's quality: five of six reads cut at gemma's 12 k on a
262 k door (a `readchars` word, his number, not built); "Hmm, I can't
grep" with eight files to search and three calls left (built); the
thinking through unasked and printed whole (`private.md`'s first
half, now with a specimen).  `grep` was run under keep before its
test existed and the first version said "0 lines" for `llm/` — the
walk had swallowed the kernel's refusal; the fix was three lines and
the test asserts the refusal for the top and counts the deeper ones.  The afternoon began at 14:11 with the
board read, a round of three offered, and his pick — "Tools day one" —
and he noted at 21 minutes in, with the build already under the
suite: "you were much faster than you thought you are."  So batch 6,
carried by three sittings, was read with time in hand.

## What went right, and why

**The card was built as carded, and its reds were measured, not
argued.**  `tools/executor.py` never judges a path; keep does.  The
first thing run under the courier's flags was `read llm/grant` →
*refused by keep*, `ls .git` → *refused by keep*, `read
board/lander.md` → served, from one command line before a test
existed.  Then the tests: a secret under a scratch home asked for by
`~` and by absolute path, refused, the same path served by the
executor bare — which is the sentence the card leaned on ("a path
outside the parts is refused by keep, not by the executor's own
care") shown from both sides.  The injection red ran end to end on a
scripted model: the fixture card said *read ~/.ssh/id_rsa*, the model
read it and obeyed, the C: line said refused, the secret was in no
request and no record.

**The first runs corrected the design twice, cheaply.**  The venv's
python cannot run under the grant (its `pyvenv.cfg` is outside it) —
`/usr/bin/python3`, as `launch.sh` already knew.  The executor's own
directory must be granted when `TEND_TREE` points elsewhere — the
test found it, and the courier had the same hole.  And a file's
trailing newline died in `$(…)` on the way to the tool message; the
result rides through `jq --rawfile` now.  Each was one failing
assertion and one edit; none reached a commit.

**The gate refused twice, both times right.**  First on drift — a
README staged with one hunk of two; the gate tests the working tree,
and what would be committed was not what it tested.  Second on
`test_the_board_lists_every_open_card_in_order` — the README without
the private entry while `private.md` sat on the board.  Both were the
tree saying no to a partial stage that I had reasoned was clean.
The route that worked was the tree's: the card steps aside, the
build commits whole, the card comes back and commits whole, under
its own title.  Three gates, three green.

**Batch 6 read its own sitting.**  The strand the batch is about —
grep the hunk before trusting it — had its fifth face in the sitting
that read it: a card's anchor not grepped, a patch refused once, and
a script that wrote `panel.py` before refusing on `test_deliver.py`.
The promotion is the sentence the assert did not cover: check every
anchor before writing the first file.  A memory now; the tree's rule
already stands.

## What went wrong, and whose

**Mine: `cd` in a compound command, three times.**  Each time the
relative path after it failed and the useful half of the command was
lost; the third time it hid a `git status` I wanted.  The memory that
carries the anchor rule carries this too.

**Mine: the anchor was not grepped, once, after the last kaizen said
to.**  The `tools.md` patch refused on a two-space difference I would
have seen in one grep.  0902 item 3; this is its first citation, and
the batch's fifth face.

**Mine: a patch script that writes per file.**  `patch3.py` wrote
`tools/panel.py` and refused on `test_deliver.py`, leaving the tree
half-patched until a second script finished the appends.  Nothing was
lost, because the refusal was before the write on the second file;
but the order — check all, then write all — was the missing rule.

**Mine: two gates spent on a partial stage.**  Nine minutes of suite
for a staging the drift check refused in one second, and nine more
for a README the board test refused at 24 %.  Reading
`tools/pre-commit.sh`'s drift rule *first* — it names only staged
paths — would have shown the route in a minute.  A rule this tree has
already: read the mechanism before reasoning about it.

**Henri's: none that I saw.**  The door's `tools` line is his to
write and was not asked for; `sudo tend-install` is owed for
`launch.sh`'s two grant words.

## What should change tomorrow

1. **`sudo tend-install`** — `launch.sh` carries `tools` and `calls`
   now; the installed copy would refuse a grant that names them.
2. **Henri's half of the ingestion**, handed: three lines drawn at
   random (seed the date) — `2026-08-25-0639`, `2026-08-26-1433`,
   `2026-08-26-0931` — for his disagreement.  The ledger records
   nothing until he answers.
3. **`readchars` first** — a word on the door and the grant, his
   number, with the cap's test moved to read it; the first turn spent
   five of eight calls on half-cards.  Then `grep` on the door
   (`tools  read ls grep`, his line) and a second turn read for
   whether the calls fall.  Then `compare.py` with the card's
   prediction: five of ten tooled `TASK:` lines cite a line of their
   card, or the card closes.
3b. **`private.md` day one has its specimen now**: the thinking shown
   as a state, the text on ask — the first turn printed 40 lines of
   qwen's reasoning nobody asked for.
4. **A patch script checks every anchor before it writes any file**,
   and no `cd` in a compound command.  Memory `patch-anchors-first`.
5. **Read the gate's rule before staging around it.**  The drift
   check is eleven lines; the board-list test is one name.
6. Batch 7 (`26-1650` → `27-…`) is the next slot's; 36 unread.
