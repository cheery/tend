# Kaizen, 2026-08-26 07:51 — arrival fixed through the person's hand, red before trusted in both directions

*Written because the lamp lit on `fd13d5f`.  Henri: "arrival needs a
fix.  it's not a feature in my eyes that limit.sh blocks the messages
from others."  Then: "applied it, go ahead."*

## What the sitting was

The `arrival` card's day one.  Three tests written first and run on
the unfixed `tools/limit.sh` — two red, the mention-guard green, as
each should be; sixteen lines in the wake's shape sent to Henri as
`arrival.patch` because the script is in the protected set and the
filesystem refused the session; his hand applied it; 20 of 20, then
three `mutate.sh` rows red by name.  The card is `blocked` on gestate's
twin, the copy the block actually happened in, which is his to say.

## What went right

**The order was test, break, fix, break again.**  The tests were red on
the script as it stood, so "the fix makes them pass" meant something;
the harness rows were red on the fixed script, so "the tests hold the
fix" meant something.  Nothing was trusted for having passed once.

**The protected set cost what it was built to cost, and no more.**  One
patch file at the root, one short command for Henri, deleted in the
same commit.  The boundary was paid for, visibly, and nothing widened.

**The card said in advance what would make it wrong**, and the person
answered that section in one sentence.  The decision was his and it
was cheap to make because the card had already framed it.

## What went wrong, named as the session's

**The first row of the rerun printed its `sed` over the label column**
because I called `mutate.sh` one row at a time from a shell loop and
cut the output by column.  Cosmetic, and mine; the recorded rows in the
file have labels, and running the sweep would have printed them.

**The card was blocked, not done, and that took a paragraph to
justify.**  Right, I think — the messages still block one way — but a
card that needs a paragraph to say which shelf it is on is a card whose
`because` straddles two trees.  The better shape may have been two
cards, one per copy, the second in gestate's board.

## What should change tomorrow

1. gestate's twin gets the same sixteen lines by Henri's hand, and
   `arrival` moves to `done/` in the commit that records it.
2. Before a session messages another, it accepts that the message
   may wait; until the twin is fixed, tend → gestate past a limit is
   not delivered.
3. Eight test files still unmeasured; `tools/mutate.sh` with no
   arguments re-reads all 67 rows.
