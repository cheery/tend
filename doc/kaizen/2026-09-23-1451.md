# Kaizen — 2026-09-23, the 14:51 sitting: the cut, a real program, and a card to continue from

*One file for one sitting.  Three commits before this file, read off
`git log 1ed6710..HEAD`: `39bf599`, `5172ff5`, `8ed7ba4`.  His lines:
"This programming language project we have.  Kude.  Can we continue on
it today?  But can you first describe where is it going?  What
properties is it going to get next, and where is it going to?"; "Do the
day 3 now, and then lets discuss where this goes."; "lets create a card
that maps the next direction, and immediately do it."; "Sign as it
stands" (his pick, asked for the new card's `done` line); "This works.
Lets leave a card where to continue from, then we can stop."  Written
in the defect shape: for each wrong, what broke, what fixed it, what
now holds it.*

**What it was.**  Three stretches.  A description first, at his
question and before any work: where Kude stood, that day three was the
cut and closed the signed `done` line, and three directions after it.
Then day three of `card:protocol.md` (`39bf599`): `new c: T (p | q)`,
duality read from each callee's own type, a tree of parties and so no
state where every party waits, coroutines and a queue each way with no
lock; eleven tests, six mutate rows, the whole suite 967 green.  Then
the direction he picked: `card:real-program.md` written, its `done`
line put to him as one question and signed, and its day one built in
the same stretch (`5172ff5`): the terminal as a channel at `Console`,
`Str` checked everywhere a value goes, tail calls, and `kude/nl.kude`
numbering a hundred thousand lines in 1.1 s; twelve tests, seven rows,
all twenty-two Kude rows re-run red, the whole suite 985 green.  Last,
at his "This works", both cards to `done/` and `card:kude.md` opened as
the place to continue from, its five roads unchosen (`8ed7ba4`).

**What went right, and why.**  The question was answered before the
work, so "do day three" was said to a plan he had read.  The new card's
`done` line was asked for and signed before a line of the build — one
question, one click, and `card:done-when.md` kept rather than argued
around at his "immediately".  Red first held on both days: ten red of
ten on day three, ten red and one green on purpose on day one.  After
a refactor that touched every rule, all twenty-two Kude rows were run
again rather than only the new ones, and the whole suite after each
build commit, because the hook runs the gates only.

**What went wrong, named as whose.**  Five wrongs, all mine; three
caught by a mechanism, two by my own reading before a commit.

1. *Two file writes through a heredoc.*  The test append and a patch to
   `kude.py`, both refused by the fence (`card:rewritten-command.md`).
   Fixed: the Edit tool.  Holds: the fence — and it is the rule my own
   memory carries as "four failures in one day", met twice more here.
   The fence is what holds it; the memory did not.
2. *A fixture that claimed a deadlock the wire does not have.*  Two
   clients on one channel, written to make the runner report every
   party waiting; with a queue each way, each client took the other's
   `deposit` as its number and answered `a = deposit` in silence.
   Caught by the test itself, red after the program.  Fixed: the
   runner refuses a message of the wrong kind, a test holds it, and the
   deadlock test uses two banks.  Holds: that test and its mutate row.
   The old rule in a new face — a fixture is a claim about the thing it
   copies, and this one copied a synchronous wire.
3. *Message types compared at a cut, tested by nothing.*  `same` gained
   `ua[2] == ub[2]` and no test would have gone red without it.  Caught
   by reading the diff for rows.  Fixed: a test written after the
   program, said so on the card.  Holds: its mutate row, red.
4. *`if True:` left in the check to save a dedent.*  Caught by my
   reading before the tests ran.  Fixed: dedented.  Holds: nothing but
   reading.
5. *"About 950 lines" in a card.*  Eyeballed; `wc -l` said 985.  Caught
   before commit by counting, which is the rule in
   `count-before-you-write-it`.  Holds: the habit, not a gate.

One reading that is mine and not measured: "This works" was taken as
his run of both `done` lines from his shell, and both cards were
closed on it.  If he ran only one, `done/` is one card ahead of him.

**What should change tomorrow.**  Kude's next day starts at
`card:kude.md` with his pick of a road; the card's reading is that
road 3 (this tree's wires) or 4 (a model writing Kude) is where it
stops being built for its own joy.  And the heredoc reflex: six
refusals across two days of a rule I carry in memory says the memory
is not the mechanism — the fence is, and it works; the cost is only a
retried step, so no new gate is proposed.
