# Kaizen, 2026-08-25 08:28 — the fix was confirmed and superseded in the same message

*Written because the lamp lit on `0d13006`, recording a measured
finding from the gestate session that the sitting could not have
produced from inside the fence.  The sitting was at its 120-minute
limit; this is a close, not an extension — an important finding
recorded so it survives, and nothing built on it.*

## What the sitting was

The gestate session confirmed the leash's cpu bug (`cpu=?` in scope
mode) is deterministic, not a race, and measured the real source: only
a `--wait` service carries a resources record, read from the journal.
Recorded on `card:grant.md` with the exact mechanism.  The scope read
in `367c531` stays as an honest `?` until `grant` builds the service
path — outside the fence, where alone it can be verified.

## What went right

**The honest `?` was the right default, and it paid.**  The fix could
have guessed a number; it returned `?` when the counter was gone, and
because it did, the gestate ledger is blank rather than wrong in scope
mode.  A sensor that says "I could not read this" is what let the other
session find the deeper truth without a false figure to unlearn first.

**The finding was recorded, not acted on, at the limit.**  The sitting
was out of clock and the mechanism cannot be tested from inside the
fence anyway.  Building the service path now would have been a blind
rework at minute 120 — twice wrong.  The card carries the recipe
verbatim; the next sitting builds and the unfenced side verifies.

## What went wrong, named as the session's

**367c531 inferred where it could have asked.**  The commit said "read
before the scope is stopped" as the fix; the other session measured
that `CPUUsageNSec` is `[not set]` after the payload exits regardless.
The session had no bus to measure with — true — but it wrote the
mechanism as settled rather than as owed a run, and the "demonstration
owed" note was the only thing that kept it honest.  A budget path this
session cannot test is one it should propose, not commit as done.

## What should change tomorrow

1. `grant` builds the leash as a `--wait` service reading
   `CPU_USAGE_NSEC` from the journal, outside the fence; the recipe and
   the trap are on the card.
2. The standing rule from 08:24 holds: the sensor's check is a band,
   and it runs where the sensor runs — outside the fence.
3. A mechanism a session cannot test is proposed, not declared.  This
   is the third time in two days a scope/budget claim was written
   ahead of its measurement; the andon's week (`card:cords.md`) is
   where that pattern should be counted.
