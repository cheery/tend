# Kaizen, 2026-08-26 15:49 — the mediation order reframed by Henri, and a port granted while a binary installs

*Written because the lamp lit on the card commit and the keep commit.
Henri: the session is a program with two differences — the cords, and
refining its scope under the scope it is in; "put it on the card"; "we
have to travel on the work machine … maybe we can do the initial tests
here.  Lets use a tiny model"; "A moment.. I install new version of
llama.cpp."*

## What the sitting was

His framing of the mediation order went on `work-environment-ai` with
a reading beneath it: one boundary, neither first; the session's
program is a program that carries the cords and attenuates; the
measurement is `llama-server` as the second node.  While he installs
the binary, the grant it needs was built: `keep --bind PORT`, one
listening port and nothing else — measured raw, tested, three rows red.
Two memories: the work-machine permit, and that model paths and ports
stay out of committed files.  This machine: 4 cores, 8 GB, no model
visible from the fence.

## What went right

**The question was answered before anything was built** — the
13:17 rule, kept.  And the answer was an assessment, not a design: what
his frame dissolves, what the tree already shows for it, where the two
session-programs differ (ports, not hosts), and what one measurement
would prove or break it.

**The wait was used.**  A caller for the per-port turn appeared in his
words; the turn was built, measured raw first, and rowed, in the
minutes his install takes — so the node's launcher has its grant ready
when the binary is.

## What went wrong, named as the session's

**Nothing measured wrong; one thing not yet measurable.**  Whether
`llama-server` runs under `--bind PORT` with only its model and its
state granted is the day-one measurement and it waits on his install
and a model file.  The grant shape is a guess until then — a good one
(a server's reach is one port) and still a guess.

## What should change tomorrow

1. The second node, day one: a tiny model under `llm/model/`
   (gitignored), `llama-server` through a launcher with `--allow` the
   model, `--write` its state, `--bind` its port; pulled; a prompt
   answered from inside the fence over loopback.  Measure what it
   reads that the grant did not give, and widen by measurement.
2. What the second program earns, if it works: the grant beside the
   program, not in a launcher — `keep` and `resolver` both said so.
   Not before it works.
3. The real run is on the work laptop; nothing machine-specific in the
   tree.
