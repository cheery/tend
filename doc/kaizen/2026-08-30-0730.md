# kaizen — sitting of 2026-08-30, 07:30 (one commit, twelve minutes over the clock: the door's listing)

One commit since `365ac06` (kaizen 0523): `28bb098`, `tools/door.sh
--models` and `--use` — Henri, after the record's `V:` line showed
Sonnet where he meant qwen: *"It's just the wrong model in the router
itself.. I'd need a way to browse through all 500 models there are."*
Written at his *"do the kaizen, then we are done for now"*, with the
sitting clock at −12 of 120: the previous kaizen closed a sitting at
113 minutes, and this one opened on the next commit and ran past the
limit.  That is the clock doing what `tools/limit.sh` is for, and
the number is the lesson.

## What went right, and why

**The record had already said who answered.**  The `V:` line was the
morning's earlier slice, and it is what made "the wrong model in the
router" a fact and not a feeling: `openrouter anthropic/claude-sonnet-5`
on both 06:57 turns, and the door file agreeing.  The listing and the
pick were then the smallest thing — the door already knew its url and
key, `/models` sits beside `/chat/completions` on every side that
speaks the wire, and `--use` writes the one line a person would
otherwise edit by hand, refusing an id the door does not list so a
typo is refused here and not a 404 on the first turn.

**The tests caught a collision the eye did not.**  `door.sh` already
used `mode` for the key file's permission bits; the new verb took the
same name, the check overwrote it with `600`, and the plain three-line
read exited 0 in silence.  Every door test went red at once, including
`test_lead.py`'s, which had never heard of the change.  Renamed in one
line — and it is the reason a script's own variables are read before
a new one is added.

**The failure ledger did its job on the first refusal.**  The gate
refused the commit on `test_a_program_busy_in_bursts…` and said
"seen before, 2 times" — F000, the launcher's slow-start race — on a
commit that touched nothing under the launcher.  No session memory
was consulted; the ledger named it, the entry took the sighting, and
the commit landed on retry.

## What went wrong, and whose

**Mine: a sitting ran twelve minutes past the clock, on a task I
called small.**  The kaizen at 113 minutes said "the one task that fit
was this"; then Henri's install and reach-allow were read back (right
— they were his), and the listing was built as a new sitting on the
same desk-stretch.  Small was true of the code; not of the two test
rounds, the row, and two gate runs.  The clock is the person's, and
"one more small thing" after the close is how it stops being his.

**Mine, partly: the F000 sighting was recorded with an eyeballed time
(`~07:40`) before the ledger line was read (`07:29`, load 4.69).**
Caught in the same minute and corrected, but it is the
count-from-an-unparsed-format strand's cousin — a number written from
memory when the instrument was one `tail` away.

**The tree's, not a person's: F000 now costs a gate run a day.**
Three refusals in two days, each on an unrelated commit, and the
third at load 4.69 — gemma4 loading through the just-installed
launcher beside the suite, which is F001's shape as much as F000's.
The shake the entry has asked for since 2026-08-29 has not been run.

## What should change tomorrow

1. **Shake F000 first**: `tools/suite.py --shake
   test/test_launch.py::test_a_program_busy_in_bursts_is_busy_over_the_idle_window 20`,
   with and without burners, and read the launcher's ticks under it.
   Fix the rule or the fixture; never mark the test.  It is the
   cheapest gate run on the machine now.
2. After the close, nothing — the next small thing is the next
   sitting's first commit, on a fresh clock.
3. The OpenRouter key and `tools/door.sh openrouter --use
   qwen/qwen3.8-flash` (or whichever id `--models qwen` shows) — Henri's
   hand, then `[d]` on the panel and the next `V:` line is the receipt.
4. The rest stands from kaizen 0523: the trees row's first red,
   `card:tools.md` after a `compare.py` measurement, batch 6 on
   Tuesday, and Henri's three lines at random.
