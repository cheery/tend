# Kaizen, 2026-08-26 08:01 — one ledger, one word: arrival done in both trees

*Written because the lamp lit on `dc18f08`.  gestate-50: "gestate's
copy unfixed: no longer … limit.sh now logs a <cross-session-message>
as `peer`."  Henri: "applied it, go ahead."*

## What the sitting was

gestate's twin got the fix the same morning (`ffa9c40`, with F182 for
its `test_precommit.py` in tend's shape).  Before moving `arrival`,
one check: both copies of `limit.sh` write the one ledger, and
gestate's `sittings.py` skips `("wake", "peer")` by name — tend logged
`message`, which that reader would have counted as a person sitting
down.  One word through Henri's hand (`note peer`), the test and the
harness row aligned first and red until he applied it, then the card
to `done/`, `green`'s sentence about gestate's copy corrected.

## What went right

**The peer's "done" was read against the shared state, not taken.**
The two copies are twins by Henri's decision and the ledger is one
file; a name that differed between them was a defect the reader would
have shown as a wrong number in his sittings, silently.  Ten seconds of
`grep` on `sittings.py` found it before the card moved.

**Two hands, two trees, one morning.**  `arrival` was opened at 07:43
and finished at 08:01 in both copies, each fix through the person's
hand, each red before trusted.  The protected set did not slow it.

## What went wrong, named as the session's

**The gate refused my commit, and it was right.**  Rewriting the card's
header to swap `blocked` for `done —`, I cut from the `blocked` line to
`see` and dropped `because` and `asked` with it.  `test_board.py` said
so by name; `pre-commit.sh` refused; two fields came back from HEAD.
The board's own contract caught the board's keeper — which is what the
morning's whole card was about.

**gestate-50's lesson, kept here because it fits**: their `git
checkout` after a limiter mutation threw away the uncommitted fix
together with the mutation.  Mutate committed files, or copy first —
which is why `tools/mutate.sh` copies the working tree and gives the
copy its own `.git` before any `sed` runs.  The shape was right for a
reason I had not yet paid for; now somebody has.

## What should change tomorrow

1. The board stands at seven open cards; `arrival` is the fourth done.
   Eight test files unmeasured — `tools/mutate.sh` with no arguments
   re-reads all 67 rows first, so a verdict that has moved is seen
   before a new one is written.
2. A card whose problem lives in two trees: open one per tree next
   time, each on its own board, and let the `see` lines cross.  Today's
   single card needed a paragraph to say which shelf it was on and a
   commit to move it that dropped two fields.
3. When two copies share a file, a fix's *names* are part of the fix:
   check the reader, not just the writer.
