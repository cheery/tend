# Kaizen — 2026-09-23, the 16:03 sitting: a goal for tend, the first gate built, and a narration with its first chapter

*One file for one sitting.  Four commits before this file, read off
`git log c0843b7..HEAD`: `08f06fe`, `bb50dad`, `02b311b`, `3e728af`.
His lines: a lean consultant's advice passed on — "Työt on hyvä
jaksottaa omiin kokonaisuuksiin … Toisekseen tavoitteet on tärkeä
asettaa niin työkokonaisuuksien kuin projektien alussa."; "Goal for
tend is to become an operating system/working environment/programming
language based on lean philosophy, for both sessions and humans …";
"support = scaffold and ease of movement within that scaffold …
project must not consume its creator, we need slacked, weeks can be
skipped.  Write the goal into vision.md and card the two gates.  I have
to admit lot of the pull for the whole project is my tastes …"; "Build
the done-when gate first then.  Also.  Lets write some kind of a
chronological progression, maybe even a narration that we can build in
journal …"; his picks "Tend's own wire in Kude" and "Model writes the
same node"; "We could add into the chapter 1: The model writes a
program"; "write the kaizen and close".  Written in the defect shape.*

**What it was.**  The consultant's two points read against the board
first — fifteen open cards, three `doing` for weeks, four real `done`
lines — and answered with numbers before anything was proposed.  Then
the goal: his words into vision.md verbatim, a session's draft under
them corrected by him (three bases, ten principles and where tend
stands on each, a one-week yardstick with skipped weeks as slack), and
`card:one-piece.md` for the WIP gate (`08f06fe`).  Then the other gate,
`card:done-when.md`'s day one (`bb50dad`): a `commit-msg` hook that
refuses a change to a program citing no signed card and no F-number,
and a board test refusing a `doing` card with an unsigned goal; this
commit was the gate's first live pass.  Then the narration in
journal.md — chapters one at a time, each opened by a signed goal — with
a prologue and chapter 1, tend's own wire in Kude (`02b311b`), widened
at his word to a door model writing the same node, its tries counted
(`3e728af`).  The whole suite 1054 green at the end.

**What went right, and why.**  Every goal set in this sitting was
signed before anything was built on it — the vision's draft corrected
before it was written, the gate's line already his, chapter 1's line
put to him as one question, and its widening as another.  The sitting
did what the consultant asked of the project, on itself.  The gate was
checked against every caller of `--install` before it landed (the
memory rule from 2026-09-03), and two old tests that went red for the
right reason were changed saying why, not loosened.  And a duplicate
was refused: the goal-first gate already had a signed card, so it got a
dated note instead of a second card.

**What went wrong, named as whose.**  Three wrongs, all mine.

1. *An edit under `board/` while the whole suite ran.*  The suite was
   backgrounded after `bb50dad`; I edited `board/README.md` and
   journal.md during it; `test_marks` compared shifted working-tree
   lines against `git blame` and went red — on the failure ledger,
   2026-09-23 16:18, `board/README.md:340`.  Caught by the suite
   itself; the test passed alone and the whole suite passed on a rerun
   with nothing moving.  What holds it: nothing but memory, and the
   memory already says "no edits under tools/ test/ board/ while a hook
   runs" — it did not say *suite*, and I read it as not covering one.
   The fix is to the memory's words.
2. *His words tidied in a verbatim line.*  "When person works" became
   "When a person works" and two more of his lines were smoothed in the
   first write to vision.md.  Caught by my own reread before commit and
   put back to his words.  Holds: nothing; `test_marks` guards approved
   rules, not a vision line.
3. *A gate built wider than its signed proposal.*  The card proposed
   `tools/` and `test/`; the build gates `node/`, `kude/` and any `.py`
   or `.sh` too.  Said in the commit and on the card, with the reason
   (`kude/` became a program after the proposal) — but it was decided
   by me, and the line he signed does say "the tree's programs".  Named
   so he can narrow it.

**What should change tomorrow.**  Chapter 1 starts: the node-and-llm
wire as a Kude type, then the node in Kude, then a door model writing
the same node with its tries counted.  Before it, his word is waiting
on three things: `card:one-piece.md`'s `done` line, the fate of
`flake`, `hold` and `session-program`, and `card:done-when.md`'s second
clause.
