# Kaizen, 2026-08-26 13:56 — the session half, measured three ways; and a row that survived because it broke nothing

*Written because the lamp lit on `7508bee`.  Henri: "you could work on
the keep and resolver.  They're most interesting ones from the ones you
listed."*

## What the sitting was

Both cards open on the session half, so three measurements before any
build.  The state row: from the fence a session reads every other
tool's state under `~/.local/state` — another assistant's prompt
history among it — while tend's own mechanisms read two directories;
`state-row.patch` for Henri's hand narrows the row to those two, with
its test inside the patch.  The bypass: `node.py` started raw twice in
244 ledger records, both this session's measurements; two raw runners
on one state both opened and served the same pulls; the node now holds
its own lock and refuses to be two.  And the ledger's second read made
`tools/ledger.py`, with tests and rows.  Nine tests, six rows.

## What went right

**The 13:42 rule fired on schedule.**  The ledger was read a second
time and the parser became a tool before the reading, not after — the
count that answered the resolver's question (2 raw, 9 through the
launcher) came out of `ledger.py grep`, and its first draft of that
grep matched heredocs that *write* tests; sharpened to "a shell line
running node.py" before the number went on a card.

**The patch carried its own red test, so the tree stayed green.**
Last time (`protect-run.patch`) the red test sat in the tree and no
commit could pass the gate until Henri's hand; this time the test is in
the patch, the tree is green, and the kaizen could be committed.  The
same hand-off, one lesson later.

**A closure was put where it holds regardless of the path.**  The
two-runner defect could have been closed in the launcher; it was closed
in the node, where a hand that bypasses the launcher still meets it.
Rule 1 checked: the node refuses to be two, it does not bound its own
reach.

## What went wrong, named as the session's

**A mutation row survived, and it was the row.**  `HEAD.match(line) →
True` left the copy behaving as the tree because a second guard caught
the same lines.  The harness said GREEN and could not say why; I read
the code and found the row had broken nothing.  Green's tally has a
third shape now — a row that does not break what it names — and the
harness cannot distinguish it from a blind detector.  The countermeasure
is the one the card already has: a survivor is read, never counted.  It
cost one reading; it would have cost a false "blind" on the tally if
not read.

**The state-row patch was not run under bwrap.**  It cannot be, from
here, and I said so on the card; still, a fence change verified by
`sh -n` and `--rows` is verified by logic, not execution, and gestate-50's
run on 08-26 morning is the precedent for what that misses.  Henri's
unfenced run is the measurement.

## What should change tomorrow

1. `state-row.patch` — apply, run `python3 tools/suite.py` unfenced,
   commit.  If the bwrap tests are red, the patch is wrong and this
   file is where it said so.
2. The true session half is now one sentence on both cards: a session
   can execute the node raw when no runner is up, unconfined, because
   only the launcher applies the grant.  The fix is in what a session
   may execute — a fence row or keep on the session — and it is the
   tree row, the hard one.  Not begun; named.
3. The next fence row to read by purpose is `trees` (gestate, read-only,
   whole): what does a tend session actually read there?  The ledger
   and `ledger.py grep` can answer it the way they answered the bypass.
