# Kaizen, 2026-08-27 16:50 — day two: the tree's copies unbound, and the first restraint fixed in place

*Covers 82f1436..HEAD: the board commit for day two (ec5f961), Henri's
merge of `install-day2` (89cc066), the lamp's baseline fix (eea4c87),
and his `--free` edit committed (1f831fc).  Written ~30 minutes into a
120-minute sitting; if it goes on, the next session inherits the lamp.*

## What it was

Henri: "Lets do install day two, take the tree binds off."  Built in
one more clone — the scripts were still ro-bound — as one rule in two
scripts: *what runs protects itself, a workbench is free.*  He merged,
installed, freed; the session then edited `tools/kaizen.sh` in the tree
in place, tested, and committed through the gate — the first restraint
changed without a clone, and `install.sh --check` immediately reported
the drift between HEAD and the prefix.

## What went right

**One rule, read off the mechanism itself.**  `sandbox.sh` knows which
copy it is by `$here`; `fence.sh` knows which side is in force by the
hook lines.  Nothing new is configured; the two facts that already
existed decide.  A mixed state is red rather than guessed at.

**The measurement was the fix.**  Instead of touching a file to prove
it writable, the session fixed the oldest open small bug (the lamp's
baseline, `--diff-filter=A`) — so day two's proof is a commit that was
owed anyway, and the drift line in `--check` proved the other half:
vetted is not in force until Henri's hand.

**A gotcha found by writing, not by failing:** `tools/sandbox.sh
--check` from the tree measures the tree's copy, not the fence in
force.  `install.sh --check` now ends by running the installed one.

## What went wrong — named as whose

**The session's: the fixture assumption bit again, in a second file.**
The 16:40 lesson (the test fixture reading a settings file that now
carries the prefix) had to be re-applied to `test_fence.py`'s `Repo`,
which also copies the real settings.  The session found it by reading
this time, not by the gate, and wrote `to_tree_lines` — but it is the
same lesson twice in one evening: a fixture that copies a live
enforcement file models whatever state the person left it in.

**And a third time, at the gate, 17:20:** `test_install`'s `--free`
test copied the tree's settings — which Henri had just freed — and had
no rules to lift.  Three fixtures, one live file, one evening.  The
rule for next time is plain: **a test never copies `.claude/settings.json`
as it is; it builds the side it means.**

**Nobody's, still owed: the bwrap test is proposed.**  `test_an_installed_
copy_leaves_the_trees_copies_writable` skips inside the fence; Henri's
`--check` ran the installed sandbox's probes green (the same property,
by the script's own probes), but the pytest line has not executed.

## What should change

1. **`tools/install.sh --check` first, every session.**  Drift between
   HEAD and the prefix is now the one silent state; the session that
   commits a restraint says "not in force until `sudo
   tools/install.sh`" in the same breath, and the kaizen lamp could
   learn to say it too (a small slice: light when `--check` is red).
2. **The clone rule is retired for the tree** (`board/README.md`
   corrected in place; memory updated).  The next time a session
   reaches for a clone, the first question is `tools/fence.sh`'s
   in-force line.
3. **`install` is recommended done**; `lander` is the residue and is
   card-shaped on its own now.  `cords` also waits on Henri's review.
   Both moves are his.
