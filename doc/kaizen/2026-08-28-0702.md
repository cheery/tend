# Kaizen, 2026-08-28 07:02 — the work laptop: a check that ran, three faces of "installed", and a launcher that stopped a server for starting

*Written because the lamp lit on `460a287` and the clock says seven
minutes; the sitting began 06:36 at "We've arrived to the work laptop",
was regranted at 06:38, 07:45 and 07:46, and holds five commits:
`460a287`, `206af8a`, `bcd8e96`, `fc3ff1c`, `a353e3e`.  Henri's lines:
"there should be everything you need in place", "Oh that.  Should I turn
the gpgsign off?", "You could implement 'allow' that accepts absence,
come up with some good name for it", and four runs of `tend-launch llm
check` and two of `run`, pasted.*

## What the sitting was

* **`launch.sh llm check` on the machine it was built for**, the card's
  promised next line.  It said ✗ before anything ran — no model — and
  the runner's log from 06:36 held the same refusal after the fact.
* **The suite's first run here: 54 red on one cause** — fixtures that
  set `user.name` and inherited `commit.gpgsign = true` from Henri's
  `~/.gitconfig`, with the key behind a fence that probes `~/.ssh` does
  not exist.  `test/conftest.py` gives every test an empty git config
  and its own identity; 357 green after it.
* **Three faces of "installed" in ninety minutes.**  The model arrived
  and the check said yes while `llama-server` could not load
  (`libsvml.so`, an Intel-LLVM build; the runtime in his home).  `check`
  reads `ldd` now.  Then, from his shell with oneAPI on
  `LD_LIBRARY_PATH`, *loads* — and keep would have refused every one of
  those twelve libraries at the loader, `SYSTEM_READ` not reaching a
  home directory.  `check` resolves each library against keep's own
  list now.  Then the run: *can not find preferred GPU platform* — a
  runtime open no check reads.  `allow /sys` and `write /dev/dri` were
  proposed from a fence that has neither, and the next run measured
  them true.
* **`allow-try`** — the word was in the tree already, bwrap's
  `--ro-bind-try` in `sandbox.sh`; readable where it is, said and not
  refused where it is not.  The oneAPI runtime is named with it, so the
  other machine is not refused for a directory it never had.
* **The launcher stopped a server that was starting.**  The second run
  reached the GPU, loaded the model, and compiled kernels for 45 s in
  silence; the pulse was quiet, the idle stop fired, and the compiler
  reported the SIGTERM as its own crash.  CPU progress is read beside
  the pulse now, red first.

## What went right

**Every ✗ the check gave was the truth from its seat, and the seat was
named.**  Inside the fence the model was absent, then the libraries,
then `/opt`, then `/sys` — each line said what and from where; from his
shell the same check went green step by step as he moved things.  The
card asked for exactly this: "is it installed?" answered from wherever
the question is asked.

**The run disagreed with the check twice, and both times the tree
gained a line rather than an excuse.**  The GPU open and the kernel
compile are things a run shows and a check cannot; the card says so,
and what the run showed became a grant line and a launcher rule.

**The gate did its job on a machine it had never met.**  It refused a
commit with no pytest, refused again with 54 red, and passed 361 — and
Henri installed pytest, unsigned this repo locally, and ran
`sudo tools/install.sh` three times without a word about it.

## What went wrong, named as mine

**A fixture built on the person's machine, twice.**  The busy-program
fixture burned its CPU in a child (`timeout sh`) and then ran the
venv's `python3`, which reads `.venv/pyvenv.cfg` outside keep's grant —
the mechanism was right and the test was wrong, two rounds.  The
README's rule holds in the small: a fixture means one side, and
`/usr/bin/python3` is what it means.

**The harness rewrote `` inside a quoted heredoc** and I read the
resulting `v=;` as my own bug for one round.  `cat -A` found it; the
expansions are written by concatenation now.  Not mine, but the round
spent on it was.

**The card's day-one note called a shared library "the toolbox's kind
of knowledge".**  It was the node's, and it took a binary that could
not start to say so.  A sentence that assigns a gap to someone else's
mechanism should be read as a guess until it is measured.

## What should change tomorrow

1. **The third run.**  `sudo tools/install.sh`, then `tend-launch llm
   run`; if the kernel compile is silent for 45 s again the launcher
   holds now, and if it is 45 s *every* start, the SYCL cache wants a
   `write` under `$STATE` (the card names the shape).
2. **The `/dev/shm` warning** from OpenMP — see whether the run cares.
3. **`/sys` in `SYSTEM_READ`?**  It is the machine, not the person's
   data, and a grant line for it on every node that touches a device is
   the same line repeated.  A `keep` question, once a second node needs
   it.
4. **`-c 2048 -t 2`, no `-ngl`** in `llm/grant` — still the tiny-model
   shape; Henri's.
5. **The card closes** when check and run agree here.  Not yet.
