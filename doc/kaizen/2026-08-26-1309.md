# Kaizen, 2026-08-26 13:09 — keep's last bit, and a board reading that was wrong by two slices

*Written because the lamp lit on `9e8f8f0`.  Henri: "do keep's slices."*

## What the sitting was

`tools/keep.py --no-net`: the network bits, the last Landlock bit keep
had left unset, handled with nothing granted — no TCP connect, no TCP
bind, UNIX sockets untouched — refused out loud below ABI 4, opt-in
with the default stated.  `node/run.sh` is the first caller; the
node's grant now reads in full: code readable, state writable, no
network.  Three tests; three mutations on a scratch clone, all red for
the right reason; 221 pass.

## What went right

**Measured before designed.**  A raw ctypes probe from this seat came
first — connect, bind on a port, bind on port 0, a UNIX bind — before
and after `restrict_self`.  The port-0 result (also EACCES) was not
guessable from the docs and settled the design: "handle both bits,
grant nothing" is exactly "no TCP at all," and the docstring says so
with the measurement's date.

**The detector was broken before it was trusted, and on a clone.**
`green`'s practice from this morning applied the same day: zeroed bits,
a flag parsed but dropped, a launcher line removed — each went red with
`connected` where `refused EACCES` was expected.  The scratchpad clone
is the right seat for it; the 08-26 green sitting learned that the hard
way with a `--ro-bind` mutation.

## What went wrong, named as the session's

**The earlier board reading was wrong about keep, and Henri acted on
it.**  At 13:04 I told him keep's workable slices were "write-scoping
and the launch path."  Both were already built that morning.  I had
read each card's *last section* as its latest state, and keep's last
section is dated 08-25 — the two 08-26 sections were inserted above
it.  Henri said "do keep's slices" on that reading; had the card really
had those two open, this would have been a wasted sitting.  It was
caught only because I read the whole card before touching anything.
The countermeasure is not "read more carefully": it is that a card's
open work should be greppable by phrase — every keep section ends its
open items with "stays open"/"still open"/"what is still open," and a
`grep -n 'open' board/<card>.md | tail` reads the newest by *content*
order rather than file order.  Written into "tomorrow" below rather
than into a tool, because the tree's rule is a tool on the second ask.

**A placeholder went into a committed card.**  The section header said
"13:xx"; the lamp named the time a minute later.  Fixed in this commit.
Small, and the shape is the one the 09:31 kaizen named: what is in the
tree at commit time is what is committed.

## What should change tomorrow

1. Reading a card's state: not `tail`, but the newest section by date,
   and the open items by phrase.  Sections are appended out of date
   order in this tree, on purpose, when a slice is built under an older
   heading.
2. The board reading move (13:04's tomorrow-2) has now been used twice
   in one day and was wrong once.  If Henri asks for it again, it is a
   tool — and the tool reads "stays open" lines, not last sections.
3. keep's card is narrowed to per-port grants (no caller), and its
   `because` no longer describes the node.  Whether the card is done is
   Henri's; `green`'s two small rows and `kaizen-ingestion` batch 2
   remain the other workable moves.
