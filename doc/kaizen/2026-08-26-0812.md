# Kaizen, 2026-08-26 08:12 — the journal gets a schedule, and the lamp was fooled by the ledger meant to read it

*Written because the lamp lit on `2f873d3` — then went dark, because
that commit touched `doc/kaizen/`, and lit again on `kaizen.sh want`.
Henri: "heijunka the kaizens, schedule 10 for each day."*

## What the sitting was

The first slice of `kaizen-ingestion`, and not a mechanism: a fixed
table on the card — three batches of ten, 26–28 August, then the ten
oldest unread — in the shape of gestate's sweep plan, with its rules
carried (a missed day moves down and never doubles; slack is not spare
capacity; the trip-wire; Henri's weekly three).  Five verdicts for what
one reading produces, and a ledger with nothing in it but its rule.
Batch 1 is today's and is unread.

## What went right

**The shape was borrowed with its evidence, not its prose.**  The
sweep's plan exists because a burst of verdicts degrades; the card's
own `because` says the journal became write-only by twenty kaizens in
a day.  Same rule, one floor up, named where it came from.

**The cord worked the first time it was pulled.**  The lamp went dark
with a kaizen owed; `tools/kaizen.sh want "why"` lit it again with the
reason, and this file is the answer.  Henri built that on 08-24 for a
session that "should have a way to tell when you want another"; today
it was for a lamp that could not tell.

**Nothing lights for an unread batch yet, on purpose.**  One `comm`,
not built until the slipping it would catch is measured.

## What went wrong, named as the session's

**I put a file in the lamp's directory and put the lamp out.**  The
lamp's rule is in its header — the newest commit touching
`doc/kaizen/` — and I had read that header this morning to break it
twelve ways in a copy.  The thirteenth break was mine, in the live
tree, by accident, an hour later.  Moved the ledger to `doc/ingested.md`;
the `green` row for the lamp is named on the card and not yet written,
because `kaizen.sh` is in the protected set and the fix — match the
kaizen's file name, not the directory — is Henri's hand.

**The plan puts this session's own kaizens in batch 3**, six of them,
and this session is the reader least able to see them.  Stated on the
card as a rule, but noticed while writing the table, not before.

## What should change tomorrow

1. Batch 1 — 24-1549 through 25-0744 — is today's and unread.  A
   session with a sitting reads it and writes ten lines in
   `doc/ingested.md`.
2. The lamp's `green` row: a non-kaizen file in `doc/kaizen/`, the
   lamp goes dark, `test_kaizen.py` green.  Write the test red, then
   the one-line fix to `kaizen.sh` as a patch for Henri.
3. Henri's first three lines, picked at random, land Friday 2026-08-28.
