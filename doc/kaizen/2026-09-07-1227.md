# Kaizen — 2026-09-07, the 12:27 sitting: the rule signed, the chain built under it, and run three times by his hand

*One file for one sitting.  The limit's log says 12:27, 15 by default,
extended by him to 180 in the first minutes.  Eight commits before
this file, read off `tools/kaizen.sh list`: `b88d997`, `15cbefb`,
`b2ba5ef`, `d5aab4d`, `0e0e7bd`, `a96a17a`, `967fd51`, `e0c6ddf`.
His lines: "I think we should start with card:done-when.md and equip
the window-related cards with it first"; "I have written that chain
where I described how the window system should work, do you find it?";
"I signed the done field.  I'd like you to implement that chain I
described with ->, if it can be implemented already"; "I signed the
canvas-windows done field"; "When I log out, the window closed but it
did not come back when I logged in.. Other than that.  It's exactly
how I want it!"; "Now it opens on login, but it does not open where I
left it.  It opens to the center"; "It works!  And it works very well.
Mark the canvas done, if there's nothing else in there that is
unimplemented"; "yes.  lets do that what you recommend"; "I think this
is good time for the kaizen.  Btw.  How did you do this so quickly?"
Written in the defect shape again: for each wrong, what broke, what
fixed it, what now holds it.*

**What it was.**  Four stretches.  First, at his "start with
card:done-when.md": the last sitting's two answers committed as
promised, then a drafted `done` line on the five window-related cards,
each with a mark for his hand — and, the minute the meter was run to
show him the five, F028: `--waiting` said nothing waited while six
marks did, the empty `henri: )*` placeholder read as an answer, and
the done-when card's own mark struck by its prose saying "`henri:`
line" above the empty one.  Fixed the same hour, red first twice, two
mutate rows.  Second, at his "implement that chain": the rule he had
signed seventeen minutes earlier said the windows card's line was a
session's draft, so the design went onto the card as day two,
proposed, and the build waited — his line came in ten minutes.
Third, the build: `windows.py open` writes the file, the extension
places the window, the mirror and `.gone` retired by his line's last
clause; sixty green, five mutate rows red.  Fourth, his hand three
times the same hour, a wrinkle between each — the file deleted at
log-out (gnome-session quits every app while the shell still runs;
the end-session dialog's signal and a three-second grace), the window
back at the centre (GNOME places a new window as it maps; placed again
at `shown`, the write-back settled after) — and his "It works!  And it
works very well."  Then the close: every clause of his signed line
walked, one not built and handed on with two more things the card
held, the card to `done/`, `later/compositor.md` shelved on arrival.
The whole suite ran once at the close, from this seat: 909 green, 18
skipped, six and a half minutes; the gates ran at every commit.

**What went right, and why.**  The done-when rule on its first day did
exactly what it was carded for: the concept was written, his marks
answered, his `done` line signed — and only then a build, which took
an hour from his "implement" to his "it works", where yesterday's took
a day to find it was the wrong half.  His question at the close —
"how did you do this so quickly?" — has that answer: the hard
measurement was yesterday's, the spec was finished before the build,
the terminal did the process half, and each wrinkle left a fact
readable from this seat (the canvas directory: empty with the
log-out's time; a file holding the screen's centre), so each fix was
a fact and not a guess.  And two counts before prose paid a third
time: the meter's footer against a grep, six apart, and F028 out of
the difference before any number went into a sentence.

**What broke, what fixed it, what holds it.**  Ten, one per line.

1. The meter's `--waiting` empty for six marks (F028, two faces).
   Caught by the two-count rule, a memory — a ceremony, and it held
   this time.  Fixed in `tools/meter.py`.  Held by the test and two
   mutate rows.
2. A patch anchor not unique (`if said:` twice).  Nothing written,
   because the script asserted first.  Held by the script's shape.
3. "The form since 2026-09-01" in the F028 entry, for 09-06.  Fixed
   by `git log -S` before the commit.  Held by a memory
   (`count-before-you-write-it`) — a ceremony.
4. A heredoc that writes.  Refused by the fence before a byte; the
   file went through the Write tool.  Held by the fence hook.
5. The executor test's second reference to the old fixture name,
   after the patch changed the first.  Caught red by `test_executor.py`
   in the commit chain, which stopped before the commit.  Held by the
   test, and by the chain's `&&`.
6. Red first not seen for the command's tests — written beside the
   program, not before it.  Fixed by five mutate rows, red by another
   route.  Held by nothing: yesterday's defect 4 again, a ceremony,
   and it failed again.
7. The file deleted at log-out.  Found by his hand; the cause read
   off the canvas from this seat in a minute.  Fixed by three guards
   in the extension.  Held by nothing from this seat: the third
   verdict, said so on the card.
8. The window back at the centre, and the file overwritten with it.
   Found by his hand; the file said.  Fixed by placing at `shown` and
   settling before write-back.  Held the same way: his run.
9. Three things in the extension found on a second reading while the
   first commit's hook ran — a drag fighting its own write-back, an
   unguarded signal connect, the title said once.  Fixed before his
   hand.  Held by nothing: a reading, a ceremony — and 7 and 8 are
   what it did not find.
10. The chain asked for while the windows card's line was unsigned.
    Refused by the session, not by a gate: done-when's day one is
    not built.  Held by nobody until it is.

Of the ten, five were caught by a mechanism (2, 4, 5 and F028's own
test and rows after the fact), three by his hand (7, 8 and the
question that became 10), and 3, 6 and 9 by a reading.  The two that
cost most, 7 and 8, are the ones no seat here can hold, and each was
one run of his and one minute of reading a file.

**Left standing.**  Four `done` drafts wait for his hand: `canvas`,
`edge`, `cords-crate`, `hold-mirror` (`tools/meter.py --waiting`).
`card:done-when.md` is signed and its day one — the board test at
`doing`, the commit gate, the meter's unsigned-line list — is not
built; defect 10 is the reason to build it next.  Handed on at the
windows card's close: the contents edit to the notebook card when it
opens (`spec/canvas.md` §18:35), the layered pull graph to
`card:canvas.md`, the compositor to `later/compositor.md`.  In the
extension: the three-second grace is a compromise a dialog-less
log-out needs, and the shutdown path (login1) is guarded and not run.
The old mirror's files on his desk were his to `rm` and he did.  Batch
13 of the ingestion is owed: eight kaizens unread — six of 09-04, two
of 09-06 (the meter and `ls` agree) — and this one the ninth.  His
number under this one.

**Tomorrow.**  `card:done-when.md` day one, signed and not built —
the gate that would have refused defect 10 instead of a session.  Then
the notebook card from `spec/canvas.md` §18:25 and §18:35, written
before its build with a `done` line, the panel its first cell.  The
line under this is his: `henri: N — words`.
