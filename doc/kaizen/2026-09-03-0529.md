# Kaizen — 2026-09-03, the 05:20 sitting: a wrapper keep stepped over, and a check taught to walk as keep walks

*One file for one sitting, 05:20 to about 05:36, at the 15-minute clock's
boundary; one commit (`0e060a6`, 05:29) and this file.  The lamp named
this file by that commit.*

**What it was.**  Three questions from his laptop before any tree work.
A SYCL build of llama-server needing `LD_LIBRARY_PATH`: rpath first
(did not help — `setvars.sh` sets more than the library path), then a
wrapper that sources `setvars.sh` and execs, which worked.  Then "where
did we leave off": read from the 18:27 kaizen's *Tomorrow* — the grant
line, the material measurement, the three *ehkä* lines — and the
wrapper answered the grant line: the machine's fact lives beside the
binary on the machine, not in git.  Then the wrapper at
`~/.local/bin/llama-server` that `ask run` "hyppää yli": keep's Landlock
does not read the home, `execvp` treats EACCES on a PATH entry as
"keep looking", and the bare binary in `/usr/local/bin` ran and died.
He moved the wrapper to `/usr/local/bin` at the session's word and said
"tee se check-korjaus": `check` now walks PATH as keep would and says
skipped / found nothing / refused, naming what keep would run instead.
Red first, `F018` resolved with its gate the same hour, 365 green at
the port.  Then his "pieni outo juttu": the panel's `pulled 08:05` on
the llm row at 05:3x — answered below the clock, not built.

**What went right.**  The diagnosis was read from `keep.py` and
`launch.sh`, not guessed: `SYSTEM_READ`, `os.execvp`, the check's
`command -v` at line 319 — and the symptom in his words ("hyppää yli")
was the mechanism's own word for it, a skip and not an error.  The fix
was placed where the check already knew its seat was not the runner's
(the ldd line), so it is the third face of one lesson and the entry
says so.  The gate covers four verdicts, and the suite found the fifth
case the reader had not (a builtin named by `command -v`).  The
questions were answered before the work each time.

**Wrong, mine.**  None caught by a fence this sitting.  One in
judgement: the full `test_launch.py` run showed one timing test red,
green alone at once, and the session ran it alone rather than through
`tools/suite.py --shake` — so `card:flake.md`'s ledger has no line for
it and the count it exists to keep was not kept.  The F018 entry says
so; the next red on that test is the second, not the first.

**Left standing.**  1. `sudo tend-install` — his line; the lander says
the installed launcher is one commit behind.  2. The panel's
`pulled HH:MM` is the *person's* pull file's last line
(`llm/state/pull`, 2026-09-02 08:05) shown without its date, on a row
whose pull today is a process's edge (`pulled/ask`, 05:28) that writes
no line there.  Two things in one: a time with no day, and the edge's
own time not shown.  Not built; an F-entry when he says.  3. The check
cannot see through a wrapper: `/usr/local/bin/llama-server` is a
script now, so the ldd line has nothing to read and llm's check says ✓
with no "loads".  True of the wrapper, silent about the binary.

**Tomorrow.**  1. His install, then `ask run` from a shell with no
oneAPI — the tick's runner through the wrapper, the measurement owed
on `card:edge.md`.  2. The panel's time (above).  3. `card:material.md`'s
day one.  4. The three *ehkä* lines, unchanged.
