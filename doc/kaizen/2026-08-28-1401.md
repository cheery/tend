# Kaizen, 2026-08-28 14:01 — the loop closed and the draft did not; a canvas nobody understood yet; the tool ate a `$` again

*Written when the desk's clock closed the sitting (60 of 60 min at
18:40), not at a word from Henri — the README's second case.  Eight
commits since the last kaizen (`961850b`): `3b6ce41` (his amendment to
kaizen 1313), `f783bb2`, `4770302`, `c74d033`, `880da41`, `76a136b`,
`735d029`, `37092d7`.  His lines: "you could continue on
card:session-program.md and then card:canvas.md, or if you have a
different proposal … choose that"; "I tried the canvas, it was
interesting but I am a bit mystified by it"; "pick a batch of things
that's make most sense to do next"; "install: done"; "I have anthropic
api key here.. you could try how sonnet or opus fares".*

## What the sitting was

* **`canvas` day one** (`f783bb2`): the panel reads a directory of
  `<name>.pin` files, one row per pin from `run.lock`/`stopped`/`watch`,
  a runner's non-zero stop on the andon's timeline.  Seven tests, red
  first with the 13:27 minute as fixture.  He pinned the llm node and
  looked: *mystified* — recorded on the card as a measurement.
* **The first live led turns, read** (`f783bb2`, `880da41`, `735d029`):
  three turns at 13:48–13:58 had happened before this session and no
  card said so.  gemma picked `canvas.md` and proposed; two turns echoed
  the prompt's `<placeholders>`; after the brackets went, two more
  echoed the digest's `===` fence; after the install and the filename
  fix, the pick landed on `work-environment-ai.md` and the draft was
  two sentences of tautology.  The loop is whole, end to end, with no
  hand in the tree.  What it carries is the `because` alone.
* **`silent-cord` day one** (`c74d033`): a fenced ring that fails at the
  player is a pull (`pulled` and the quiet window count `ring-failed`),
  and `andon.sh relay` on the person's side sounds each failed ring
  once, run by the resolver's hook.  Seven tests; one old test reversed
  with its reason kept.  Installed by his sudo at 18:05.
* **Small, from his paste** (`76a136b`): `ask " "` rang a blank line.
* **`tools/compare.py`** (`37092d7`): the same turn to Sonnet 5 and
  Opus 5, on his side — unrun, proposed.

## What went right

**The session read the state before the cards, and found the
milestone.**  `proposals/lead/` and `lead.log` had three live turns the
previous session never saw, because they happened after its last
commit.  Ten minutes of reading turned "what is left is Henri's hand"
into "the hand ran, here is what it produced" — and the session-program
card now carries the measurement rather than a wait.  Reading what a
program left is cheaper than asking what happened.

**The rule held on every live turn, and that is the result.**
`<canvas-script.md>`, `canvas.md ===` — each was refused by the open
shelf and became a cord pull, never a proposal on a card that does not
exist.  The tuning went in the field reader, not in the trust.

**A batch was picked by kind, not by order.**  Asked to "pick a batch",
the session sorted the open cards into *waits on his hand*, *levelled by
rule* (ingestion: ten a day, and today's ten were read), and *buildable
from this seat* — and the last kind had one card.  The priority list
says what matters; the seat says what can move.

## What went wrong, and whose

**The Bash tool ate a `$` in a patch script — kaizen 1313's own
lesson, paid the same afternoon.**  The patch matched nothing, said
nothing was wrong, and the assertion that guarded it fired only because
the patch asserted its own match.  The session's, for reading the
lesson four hours earlier and writing `$` into a heredoc anyway.  What
closed it was a mechanism, not care: **patch scripts go through the
Write tool**, whose text no shell expands.  Two more patches that way,
both clean.

**A prompt tuned against one echo produced the next.**  The angle
brackets went and the `===` fence came; the session had told the model
"exactly as it appears between === below", which is asking for the
fence.  The session's.  The fix that held was not a third prompt but a
reader that wants only what the shelf judges — the first word ending
in `.md` — and that is the general form: read for the thing that is
checked, not for the shape that was asked.

**The card stamps read "14:20" at 17:33.**  The lamp names the sitting
file by its first commit's time and the session took that for the
clock.  Caught by `date`, one sed, no harm — but a timestamp written
from a filename is a fixture copied from the live thing, in miniature.

**A fixture with one second between a failed ring and its relay gave
the wrong answer** — the README's one-second lesson in its plainest
form, and the mechanism was the thing fixed: relay goes by the
record's order, not the clock.  The tree's rule, applied to its newest
line within the minute it was written.

## What should change tomorrow

1. **`tools/compare.py`** — `.venv/bin/pip install anthropic`, then one
   line.  It says whether the 18:09 tautology is the 26B model or the
   turn's shape; the three dials on `session-program` §18:20 wait on
   that answer, and so does `later/model-acceptance.md`.
2. **`silent-cord`'s demonstration**: `tend-reach-allow` without
   `audio`, one fenced ask-and-ring, a tone from the hook.  Then the
   panel-plus-hook double sound is a want to decide.
3. **The canvas**: leave it pinned; the next 13:27 says whether the
   row is what he needs or the view should say what a pin is.  Then
   the one-second watch in `pull` is reverted.
4. **Patch scripts through Write, never a heredoc with `$` or `${`** —
   the second time this is written; if it is a third, it is a line in
   the README.
