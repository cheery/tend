# Kaizen, 2026-08-25 14:24 — a granted row overturned the card that asked for it

*Written because the lamp lit on `151f6a3`.  Henri: "would you like to
do grant next?", then "ok. patch is in."*

## What the sitting was

`card:grant.md`, with `bus` in the bound for the first time.  Four
measurements from inside the fence — a scope works in bwrap, the bus
row is an escape, the cpu counter is readable from inside the scope and
gone after it, the leash mistook a payload's 124 for its own — and the
build the card had sketched: the leash wraps the fence, the bus row is
removed, `leash.sh --inner` reads the tally before the scope's last
process exits.  Henri applied the two protected files as a patch; the
first fenced command after it wrote `scope  cpu=6.8s` to the ledger.

## What went right

**The row was measured before it was used.**  The card's "what would
make it dangerous" paragraph named the risk and said *decide by
measuring what systemd-run actually needs*.  The measurement took one
command and found the row was a door out, before anything was built on
it.  That paragraph paid for itself.

**A wrong section was overturned and kept.**  The card said a scope
"cannot be made in bwrap's namespaces".  With a working socket it can.
The sentence stays with its correction beside it, per the README's
rule that a tree which only ever reads as right teaches nothing.

**The `?` gave way to a number by moving one process boundary.**  The
morning's kaizen said the honest `?` was the right default and would
pay; it did — the question "why always `?`" had exactly one answer,
found by reading from inside the scope instead of after it.  No
journal, no service, and the command keeps its env, cwd and stdio.

**The protected set bit, and the loop cost was one line of his.**
Yesterday's build, first real test: two files the session could not
touch went out as a patch at the tree root, `git apply grant.patch`,
and the next command was the demonstration.  The cost the last kaizen
asked to measure is: one scratch file, one patch, one line.

## What went wrong, named as the session's

**A test was left expecting the old prefix.**  `test_there_is_no_nofence`
went red after the patch; the session had updated four rewrite tests
and missed the fifth because it read the first assertion and not the
second.  The gate caught it before the commit; the fix was one line.

**The 124 bug was found by accident.**  The leash's first line from
inside said "the 900s budget is spent" after 3 seconds.  The tests had
worked around it (`; true` after every inner `timeout`) rather than
naming it — a workaround in a test is a bug with an alibi.

## What should change tomorrow

1. `display` is the `bus` question unmeasured: an X socket takes input
   for every window on it.  Before anyone grants it, one command.
2. The leash's defaults — 900 s, half the cores — are now the grant for
   every command a session runs.  A session cannot ask for more.  The
   first command that needs more is the card.
3. `grant`'s `because` no longer stands as written — the budget applies
   inside the fence, and the ledger says so on every line.  Whether the
   card moves to `done/` is Henri's; the count of `plain` lines that
   mattered can run from `done/` as well as from the board.
4. A workaround in a test (`; true`) is to be read as a bug report.
