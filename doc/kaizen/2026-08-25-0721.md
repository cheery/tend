# Kaizen, 2026-08-25 07:21 — the first commits from inside the fence

*Written because the lamp lit on `e613dcd`, the first commit made from
inside the fence.  Henri had re-run the installer against the fixed
hook; this is the count `2026-08-25-0714.md` item 3 asked for.*

## The count

Commands run under the hook since the fix: five, all wrapped
(`TEND_FENCED=1` printed first each time).  Rows asked for: zero.
Broke: one — the gate refused the first commit, because
`tools/sandbox.sh` refused to nest before it had answered `--rows`,
and three tests that need no bwrap ask exactly that.  The refusal
moved to where bwrap is started; the second commit went through.

## What went right

**The gate ran inside the fence and held.**  That is the arrangement
working end to end: a session's shell inside, the person's cords
through, the suite at the commit, `.claude/` read-only, and the hook
line now in HEAD where `--restore` keeps it.

**The Edit tool as the way out.**  Twice this evening the fix to the
fence was made with a tool the hook does not wrap, which is why a
broken fence never needed Henri to undo it.  That is by construction
— the deny-list governs the edit tools, the fence governs the shell,
and the two do not share a failure.

## What went wrong, named as the session's

**Guard before answers.**  The nesting refusal was put at the top of
the script because that is where guards go; the tests that run at
the gate said otherwise within the minute.  Small, and the gate
caught it; noted because "where guards go" is a habit, not a reason.

**`tail -2` on a refusal.**  The gate's message was cut to `end.` and
the failure had to be fetched again.  When a command can refuse,
show all of what it said.

## What should change tomorrow

1. `TEND_REACH_ALLOW` is Henri's to set; until then no row.  The
   first row asked for, and why, goes in the next kaizen.
2. `.claude/settings.json.before-fence-hook` is his backup, untracked;
   his to delete when he trusts the line in HEAD.
3. The board: `fence` is open for one more sitting of ordinary work,
   counted; `work-environment-ai` has its order by trial; `cords`
   waits on 2026-08-31, and the andon now has a fence to run under.
