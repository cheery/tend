# Kaizen, 2026-08-28 18:30 — two Claude models on the node's turn, and a shelved card woken by the result

*Written at Henri's "we are done for now.  let's continue tomorrow
here."  Two commits since the last kaizen (`f3311c0`): `ce0f7d3`,
`16c4d91`.  His lines: the two `tools/compare.py` runs pasted whole;
`tools/install.sh --hooks apply`; "do session-program".*

## What the sitting was

* **`compare.py`, run twice** (`ce0f7d3`): the first run's drafts came
  back empty at `600 (stop max_tokens)` — the tool's fault, fixed —
  and the second run, thinking off at the node's limits, had Sonnet 5
  and Opus 5 both pick `canvas.md` and draft the thing they named.
  Opus reached the canvas card's own prediction (the one-second watch
  is what a death record replaces) from the `because` alone.
* **The fork decided** (`16c4d91`): the tautology is gemma's, the
  loop's shape holds; `later/model-acceptance.md` is on the board —
  its blocking event (a door where a model is admitted) has happened,
  and `compare.py` is its first instrument.  Placed last, his tiebreak.
  `canvas` points at Opus's draft as day two; `lander` records the
  hooks applied.

## What went right

**A measurement was designed to have one variable and then had one.**
Same digest, same two prompts, same limits, same reading of the reply;
only the model differed — and the difference was in kind, not degree.
The question of the previous sitting ("the model or the turn's shape?")
was answered in twenty minutes of his shell time because the tool
copied the node's conditions instead of approximating them.

**The result moved a card, not a dial.**  The obvious next step after
18:09 was to tune gemma's prompt and context a third time.  The
comparison said the loop was fine, and the right act was to wake the
card that judges models rather than build for the one that cannot
hold the turn.

## What went wrong, and whose

**The first compare run copied the node's `max_tokens` and not the
node's `enable_thinking:false`** — adaptive thinking spent the whole
600 and wrote nothing.  The session's.  The README's fixture lesson
in its API form: half a condition copied from the live thing is a
fixture of the wrong thing.  Caught by the `usage` line the tool
wrote into its own account, which is the reason the line is there.

**The model's draft is better than the session's build, and the
session has to say so without landing it.**  `read_log` merges a
death at view time and loses it at the next clean stop; Opus's line
keeps it in the record.  The rule held — the model proposes, the
person lands — and the card carries the session's reading and the
file's path, not the words.  Nobody's fault; named so the temptation
is visible.

## What should change tomorrow

1. **Read `proposals/compare/2026-08-28-1835-claude-opus-5.md`** and
   say whether it lands as canvas day two.  If yes, the runner's stop
   path appends one line to the andon record, red first, and the
   one-second watch in `pull` is reverted in the same commit.
2. **Place `model-acceptance`**, and answer its question 3 — per model
   or per session.  Its day one is a sheet that says what a pass
   licenses, before any refusal is wired.
3. **Say which model leads**, if any: a Claude-led turn under keep is a
   build (a reach the node does not have), and it is not started
   before that word.
4. **`kaizen-ingestion` batch 4** in the morning — today wrote four
   kaizens and read none back.
