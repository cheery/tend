# Kaizen, 2026-08-26 07:39 — the harness joined the tree, and two fixtures were found to have no seam

*Written because the lamp lit on `92eb569`.  Henri: "put the harness in
tools/, then measure test_kaizen and test_limit."*

## What the sitting was

`tools/mutate.sh`, the scratch harness rewritten in `seedmutate.sh`'s
shape with a self-check before any row is read, and 64 recorded rows;
`test/test_mutate.py` for its two claims.  Then thirty breaks of
`kaizen.sh` and `limit.sh` against their tests: 28 red by name, two
survived, both the same shape — a fixture with nothing on either side
of the seam the defect opens (one uncovered commit, so first==last; a
grant and a read in the same second, so started==now).  Both repaired
and shown red before trusted.  Suite 200 passed.

## What went right

**The harness refused to read four rows**, and all four were bad
input: three `sed`s of mine that matched nothing, and one the guard
itself could not see (a hook removed from `.git/hooks` is invisible to
`git status`) — which it can see now.  A `NOOP` column that fires is
worth more than one that never does; the morning had none and reported
a self-deleting `sed` as GREEN.

**The gate column was made honest.**  It read `test_precommit.py`
alone and reported `suite.py return 0` as survived, when
`test_suite.py` in the same suite is red on it.  The gate runs the
whole suite; the column now reads what the gate reads.

**Both survivors were named as one shape, and the shape is F88's.**
Not the wiring this time — the fixture: a test that measures a thing
at a point where the defect and the correct program produce the same
number.  gestate's was a note boundary where the level was 0.0000;
tend's were a session of one commit and a clock read within a second
of its grant.

## What went wrong, named as the session's

**`bash -n` failed on my own file** because I put the rows after
`exit`, and two of `test_mutate.py`'s three tests passed anyway —
runtime never reaches the data, `-n` does.  A tool about detectors,
whose own parse test was the one that caught it.

**Three `sed`s that matched nothing** were written from memory of the
line rather than from the line.  The guard caught them; the cost was
one rerun.  A break should be written with the target line on screen.

**The kaizen fixture's first repair was three hours off**: I set
`GIT_COMMITTER_DATE` in `+0000` and `--date=format:` renders in the
commit's own zone.  The intact copy went red and the harness said
"nothing below can be read" — the self-check working as built, on the
first day it existed.

## What should change tomorrow

1. Eight test files unmeasured: `test_fence_hook`, `test_keep`,
   `test_leash`, `test_node`, `test_rules`, `test_sandbox`,
   `test_selfmatch`, `test_summary`, `test_toolbox`.  Rows are added
   with a date; `tools/mutate.sh` with no arguments says whether the
   tree still holds every verdict.
2. When a fixture is written, put something on both sides of the seam
   — two commits, two seconds, a level that is not zero — and try the
   defect before trusting the green.  This is the day's one lesson in
   three faces, and `kaizen-ingestion` is the card that should one
   day hand it up without a session having lived all three.
3. gestate-50 has not answered the morning's two questions.  The
   sweep's vocabulary is theirs; the answers go on `board/green.md`.
