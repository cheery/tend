# kaizen — sitting of 2026-08-30, 05:23 (the morning the person talked to the model, and found out which one)

Eight commits since `d64c6d4` (kaizen 2100): the reach key restricted
(`582b8b9`, F004), the trees card (`2e2be02`), ingestion batch 5
(`1141a40`), then four slices on the panel in two hours — talk and
unpin (`ec442ca`), thinking (`1eaa8a3`), streaming (`3c6efdc`), the
mind named and a door (`66a3a6f`) — and the tools card (`f90fe4c`).
Written at Henri's *"is there any small tasks you could take before
it's over for today?"* with fifteen minutes on the clock: the one
task that fit was this.

## What went right, and why

**A hand-edit was the first defect of the day, and the key caught
it on its own first run.**  The hook line read `TEND_REACH_ALLOW=net,tree`
— Henri's hand, reaching for the other tree with a name the fence
cannot read.  The fix was the key asking the fence for its rows
(`sandbox.sh --rows`, the `off` ones) instead of keeping a list, and
its `--rows` said `? tree is on the line and is not a row` against
the live tree before the commit.  F004 resolved the day it was found,
its gate named; and the want under the hand-edit became
`card:trees.md` the same hour, because a refusal with nowhere to sit
is a refusal that comes back.

**Question first, twice, and the second time it was a memory.**
Batch 5 found `2026-08-26-1323` recording *"Do the question first next
time"* as written to memory — and the memory directory did not hold
it; the write had died in the fenced home, as the same kaizen said two
sentences earlier.  The ledger's first promotion to a memory was that
write, done with the tool that runs outside the fence.  The rest of
the sitting applied it: "isn't there a way to allow you to access
other trees?", "is it local?", "can I enable thinking?", "would it be
time for tools?" — each answered in a paragraph before anything was
built, and two of them became cards rather than slices.

**The record said who answered.**  The `V:` line was written for the
door an hour before it was needed; at 06:57 Henri thought he was
talking to qwen3.8 flash and the record said `anthropic/claude-sonnet-5`.
And the morning's own mind was found the same way: a 1B model that
sorted before `gemma` in `ls` order, at 51 t/s, inventing the Five S's
— `card:model-acceptance.md`'s `because` met in the plainest form,
and the fix was one grant word so the pick is written.

**Streaming was measured, not assumed.**  The stub sends the answer
in two halves with a pause, and the test reads the first half off
disk while `deliver` is still running.  A row that drops the live
write is red by seven tests.

**Every slice landed with its rows, the same commit** — eleven rows
in four commits, each run red by the test that names it before the
commit.  The 2026-08-26-1317 countermeasure, now a habit the ledger
called practice and not yet a rule; a day of keeping it is a face
toward the rule.

## What went wrong, and whose

**Mine: the harness expanded my `${…}` forms and I ran a bash script
with `sh`, in the same command.**  Three mutation rows came back
"redirection unexpected" with empty test names, and it was my own
memory file that said why (`bash-heredoc-expansion`).  Cost: one
round of writing the runs into a script file, which is what the
memory told me to do in the first place.  Read the memory before the
shell, not after the error.

**Mine: a subshell ate the thinking.**  `ask()` set `thought=` inside
`$(...)`; the `T:` line was never written and the test said so.  The
fix was a file, and the comment says why — but the first draft was
written as if a function called in a substitution shared its
variables, which is the kind of shell fact a test finds and a re-read
does not.

**Mine: two fixtures assumed what the fixture did not do.**  The
panel's door test asserted "no pull file" against a node fixture that
writes one; the launch test read the model off `grant`, which prints
the program line unexpanded.  Both found in one run, both fixed in
the test, neither in the code — the fixture rule of 08-26 in the
small: read the fixture before asserting on it.

**Henri's, and named as such because it is the tree's design**: the
`.claude/settings.json` line still carries `tree`, and the installed
`reach-allow.sh` and `launch.sh` are behind HEAD.  Both are his hand
by construction (the lander card says why); the resolver keeps
starting MiniCPM from the old launcher until `sudo tools/install.sh`.

## What should change tomorrow

1. `sudo tools/install.sh`, `tend-reach-allow net`, the OpenRouter key
   at `~/.config/tend/openrouter.key` (mode 600) — then the held node
   loads gemma4 on its next start, and the door opens.
2. `card:trees.md`'s first red: `sandbox.sh --rows` says `on` for a
   `trees` row that binds nothing on this laptop.  A few lines in a
   protected file; the small task that did not fit today's clock.
3. `card:tools.md` day one, if the door conversation earns it: two
   read-only tools under keep, a `C:` line per call.  The instrument
   for whether it is needed at all is `tools/compare.py`, and that
   measurement comes before the executor.
4. Batch 6 of the ingestion is Tuesday's; Henri's half — three lines
   at random, disagreed with — is now five batches and fifty lines
   overdue, and the sixth arrives before it.
5. The count-from-an-unparsed-format strand stands at two faces
   (`26-1342`, `26-1433`); a `git log` reader in `tools/` is its
   third, on the next ask.
