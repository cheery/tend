# kaizen — sitting of 2026-08-30, 18:52 (the close's tail: readchars measured, named, and put on the doors)

Two commits in ten minutes, after the 1741 kaizen was already written:
Henri's "propose a good readchars amount that reads the card
completely.. then propose some mechanism that allows the session to
read more", answered on `card:tools.md` (38d0932) and on both door
files at his hand (fbd61c2, `readchars 60000`).  Written the next
morning by the session he assigned before going back to rest; the lamp
carried it overnight, as the board says it does.

## What went right, and why

**The number was measured before it was proposed.**  The largest card
is 48k, the largest file under the parts 51k; 60000 reads every card
whole with a third to spare on a 262k door.  Ten minutes, no guessing.

**The mechanism was proposed, not built, at the close.**  `read` takes
only a path and the cut mark reads as an end — the third turn's "I
have now" was half a README.  The card now carries the design
(`read(path, line)`, a cut mark that says how to continue) for the
next sitting instead of a tired build at 19:00.

## What went wrong, and whose

**The sitting's kaizen was written before the sitting ended — the
session's.**  The 1741 kaizen went in at Henri's "this is the last
session today", and two more commits followed it inside the same
sitting, leaving this two-commit tail owed overnight.  Cheap this
time — the lamp carried it and nothing was lost — but the close's
kaizen belongs after the close's last commit, not after the words.

## What should change tomorrow

1. `sudo tend-install` — still Henri's line; F005's fix is not in
   force until it runs (carried from 1741).
2. The mechanism named above: `read(path, line)` and the executor
   taking `.` as the parts — this morning's build.
3. `compare.py`'s tools arm — `bench/` was filled by his hand at
   18:39, so `card:simpleqa.md`'s day one has its data waiting on it.
