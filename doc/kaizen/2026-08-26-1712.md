# Kaizen — the day's direction marked, and a toolbox idea pressure-tested rather than adopted

*Written because the lamp lit on the marking commit.  Henri: "mark that
the cords are next in line tomorrow.  And that we examine that session
as a principal thing a bit.  We actually would need accurate
standardized toolbox … Do you spot any flaws in my reasoning?"*

## What the sitting was

Closing marks.  `session-program` named next for 2026-08-27 (the cords
on a node, the limit first).  Henri's toolbox idea — declare the
available tools and enforce that only they are available, for easier
install tests — seeded on `work-environment-ai` for the
session-as-principal examination, with three flaws named: a list is not
enforcement (PATH is not a boundary; real enforcement is bind-level and
leaky through the library closure); the install-test "why" is carried
by the cheap manifest half alone, so it cannot justify the enforcement;
and node-vs-session-vs-travel (a node's enforced toolbox is its keep
grant already, a session's set is broad and machine-varying).  A
project memory for tomorrow's session.

## What went right

**The question was answered before the marking, and answered as a
critique.**  Henri asked for flaws, not agreement; three real ones were
named and the sound part credited, and the idea was seeded as a
*solution looking for its problem* — not carded, because the board
refuses a `because` that names a fix.  The decoupled first step (a
manifest for install tests, separate from enforcement) is the honest
offer.

**The residual stayed owned.**  The toolbox idea is the candidate fix
for exactly the ambient-exec hole recorded at §16:50 when `keep` and
`resolver` closed — so the thread from this morning's judgement to
tomorrow's examination is unbroken and written down.

## What went wrong, named as the session's

**The memory write went through the fenced shell and failed, again.**
Third time this span (08-26 13:23, and now twice more) I reached for
`cat > ~/.claude/.../memory/…` inside the fence and hit the empty home.
The lesson is already a memory (`the dedicated tools, not the fenced
shell`) and I did not apply it — the reading≠applying strand the
ingestion ledger tracks (batch 1, [[2026-08-24-1549]]).  Fixed with the
Write tool; the countermeasure that would actually hold is a hook or a
habit, not another note, and it is worth raising whether memory writes
should have a one-line helper that runs on the person's side.

## What should change tomorrow

1. `session-program` day one: the sitting limit on the llm node, from
   the state the launcher keeps, stopped on the person's clock not the
   program's idle.  One cord, shown to hold.
2. The session-as-principal examination: weigh the toolbox idea's three
   flaws; if a step is taken, it is the manifest for install tests
   first, enforcement held separate.
3. Memory files are written with the Write tool, never the fenced
   shell.  If this recurs a fourth time, it is a mechanism owed, not a
   resolve.
