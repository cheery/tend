# Kaizen, 2026-08-26 13:34 — resolver's day one: a measurement that answered the card's hardest paragraph, and a helper written twice

*Written because the lamp lit on `78d13f8`.  Henri: "do resolver's day
one."*

## What the sitting was

The card said measure, then the smallest thing.  Measured: a process
started detached inside a fenced command dies with the command
(`--unshare-pid --die-with-parent`), so from a session's seat no
resolver can outlive its pull's command, and from a person's shell it
survives and stops on idle — neither needs a daemon.  Built: in
`node/run.sh`, `pull` starts a confined runner when none holds the
lock, waits for it to open, then pulls; `run` holds `<state>/run.lock`
on an fd the runner inherits and refuses a second runner with 75.
`node.py` untouched.  Three tests, four rows red by name, README's "Try
it" leads with `pull`.  228 pass.

## What went right

**The measurement was the design.**  The card's costliest paragraph —
"the first long-running thing tend would own; the leash and the sitting
limit would have to know it" — dissolved on one `setsid -f` and a
marker file that never came.  Nothing long-running is possible from
this seat, so nothing has to know about it.  Reading the leash and
limit headers first is what made the marker's absence *mean* that,
rather than look like a bug.

**The rows landed in the same commit as the slice** — the 13:17
countermeasure, kept once.  And the four breaks were chosen against the
mechanism's four moves (start, check the lock, take the lock, wait for
the open), not against lines that happened to exist.

**The pull-before-open race was caught by reading `cmd_run`**, not by a
flake: the node reads the ledger *at open*, so a pull written before
that is seen and never served.  The wait-for-generation loop exists
because of that line, and the fourth row is that loop removed.

## What went wrong, named as the session's

**A helper was written and not used, then the grant was pasted three
times.**  The first draft of `run.sh` defined `confined()` and repeated
the five-line keep invocation in each verb anyway.  Caught on re-read
before commit, and the second draft has the grant in one place — which
matters here more than elsewhere, because the grant *is* the boundary
and three copies are three places for it to drift.  The countermeasure
is not "re-read": it is that `test_the_node_launcher_asks_for_no_net`
should assert the grant appears exactly once in the launcher, and it
does not yet.  Named for tomorrow rather than done now, because the
sitting has run long and a test written at the end of one is the
fixture-with-no-seam shape.

**`run.sh` is in the tree, not the protected set, and this sitting made
it the launch path.**  The card says so, and says the decision is
Henri's; it is still true that a session can now edit the thing that
applies every program's grant.  Not a regression — it could before —
but the file's weight changed today and its protection did not.

## What should change tomorrow

1. `test_keep.py`: the launcher's grant appears once.  Small, with a
   seam (paste the block twice in a copy, expect red).
2. Henri's decision, on the card: does `node/run.sh` join the protected
   set now that it is the pull path?  The two widening keys went in
   yesterday for the same reason.
3. `resolver`'s owed demonstration is done; the card stays open on the
   session half — a session can still start a program another way —
   which is `keep`'s open half by the same name.  A second pulled
   program is what would earn the grant-beside-the-program shape.
