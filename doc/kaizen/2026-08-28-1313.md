# Kaizen, 2026-08-28 13:13 — the node's cords, the kernel's boundary, one silent crash, and a canvas

*Written at Henri's "we are done for now.  let's continue tomorrow
here."  Four commits since the last kaizen (`abbfeae`): `1e72dc8`,
`19c279e`, `82f11f3`, `3b61855`.  His lines: "take session-program";
"ouch!  I forgot that, but it should not crash silently"; "pull waiting
until an obvious crash is something we will eventually revert"; "open
the canvas card".*

## What the sitting was

* **The watcher heartbeat** (`1e72dc8`): the runner touches
  `$STATE/watch` each tick; a held lock with a stale heartbeat is read by
  `status`/`check`/`serve` as *the cords are cut*, `serve` kills it, and
  the stop escalates past a program that ignores TERM — the laptop's
  25-minute overrun, reproduced without strace.  Four tests.
* **`keep --connect` and `lead.sh --kept`** (`19c279e`): bind's twin in
  keep, and a led turn confined by the kernel — a proposal written and a
  board write refused in the same turn.  Three tests, none skipped.
* **The first live attempt** (`82f11f3`): `lead.sh llm --kept` said "not
  up"; the log said llama-server died at the loader (libsvml.so — the
  shell had no oneAPI on its path).  `pull`/`serve` now say a runner
  that dies at once, and a kept turn says the last stop and waits for a
  loading runner.  Three tests.
* **`canvas` opened** (`3b61855`): pins, canvases, the panel grown with
  an andon/log column; the 13:27 minute is its `because`.

## What went right

**Each brick was a tool the person runs, and the boundary moved outward
one step at a time.**  lead.sh trusts nothing the model says about the
card; the kept turn trusts nothing lead.sh says about its writes — the
kernel refuses.  That is the tree's rule applied to its own newest
part, and it took one Landlock bit that had been handled and never
granted since day one.

**The failure named the card.**  The 13:27 crash was fixed in an hour
and Henri said the fix would be reverted — and he was right for the
reason the README already holds: a one-second window is a fixture of
one second.  Instead of defending it, the session said what replaces
it, and his next two messages were the card.  A prediction from him is
a `because` waiting to be written.

## What went wrong, and whose

**The gate was refused twice by the session's own tool, not by a
test.**  Two commits were refused with no `FAILED` in sight and passed
unchanged on retry; the common factor was the tool's 120 s timeout
landing inside a 110–119 s suite.  The session first called it a flaky
test, ran the suite again to chase it, and only saw the pattern on the
second refusal.  The session's — and the lesson is the old one: a red
that vanishes on retry is a claim about the instrument first.  Every
commit now runs with a long timeout.

**Two edits were eaten by the tool's own expansion** (`$` and ``
in command text), one silently — a pid file holding a literal `$`.
Noticed by the tests both times, which is what they are for.

**`launch.sh` drifted from the installed copy three times in one
sitting**, and each time the install was Henri's sudo.  Nobody's fault
and `card:lander.md`'s exact problem, measured three more times.

## What should change tomorrow

1. **`sudo tools/install.sh`**, then a pull from a shell with oneAPI on
   its loader path, then `tools/lead.sh llm --kept` — the first live
   led turn, and read what gemma picks in `proposals/lead/`.  That is
   what `session-program` waits on and what `later/model-acceptance.md`
   wakes to.
2. **`canvas` day one**: the canvas directory, `llm.pin` by hand, one
   row on the panel with the stop's reason in the log column — red
   first with a pin whose `stopped` says `exited 127`.  When it is on
   the screen, revert the one-second watch in `pull`.
3. **A long timeout on every gated commit**, and a red that clears on
   retry is read as the instrument before the tree.
