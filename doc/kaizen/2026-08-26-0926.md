# Kaizen, 2026-08-26 09:26 — the person's two keys came into the tree, and self closed

*Written because the lamp lit on `a9a9958`.  Henri: "Would it be time to
move the ~/hook-installer.sh ~/reach-allow.sh inside the project, so
that they're clearly tend's files?" — then "go ahead, I'll copy them
into tools/", "both are in, go ahead".*

## What the sitting was

The `self` card's last open line — the two widening keys in `~` — moved
into `tools/` and into the protected set the same act, and the card
closed.  Verified first that neither takes a session-writable input;
made both relocatable so a fresh clone is self-installing; moved them,
patched `sandbox.sh` to list them and ran `fence.sh --protect` (Henri's
two steps) to bind them read-only and deny their edit.  Proved the
two-step green in a scratch clone before the real run.  `self` to
`done/`, the README renumbered, the rules-sheet citation followed.

## What went right

**A question was answered before it was acted on.**  Henri asked "would
it be time" and the deliverable was an assessment first — the card's own
"a set, not a folder", the check that the keys read no session-writable
input, the cost in his muscle memory — and only on "go ahead" did the
move start.  The card had done the thinking a year of sittings ago; the
sitting's job was to honour it, not redo it.

**The move made the keys more portable, not just more tidy.**  They were
pinned to `/home/cheery/tend`; now they find their own root, so
`hook-installer.sh` in a fresh clone installs that clone's fence.  "One
clone away" was half of why they came in, and it is real now.

**The protection and the move were one commit.**  The card's rule — a
key in the writable tree is only safe once the set holds it — was kept
literally: the commit that put them in the tree is the commit that
protected them, proved green in a clone first, and the read-only bind
was confirmed from inside the fence before the commit.

## What went wrong, named as the session's

**I stole a decorator and caught it only because the wrong test went
red.**  Inserting the new test above `test_the_fences_own_code_is_read_
only_inside`, I split its `@needs_bwrap` from its `def` — my test took
the skip, its test lost it and ran inside the fence and failed.  The red
was in the neighbour, not the newcomer, which is the tell that the edit
landed a line off.  It is the batch-1 lesson wearing today's clothes:
reading a mechanism (where the decorator sat) is not holding its shape
while editing beside it.  Caught by a number that could not be right,
not by care — which is the whole method, and also the thing I keep
having to be caught by.

## What should change tomorrow

1. When inserting a function next to a decorated one, look at the line
   *above* the anchor before replacing it — a decorator is part of the
   next def, not the previous one.
2. The board is down to five open cards; `self` is the fifth done
   (`gates`, `grant`, `pull`, `arrival`, `self`).  `work-environment-ai`,
   `keep`, `kaizen-ingestion`, `fence`, `cords`, `green` remain — two
   count-shaped, one blocked, the rest with named slices.
3. `sandbox-limit.patch` is still Henri's to apply; the scratch-dir
   fence limit lives only in `board/green.md` until it does.
