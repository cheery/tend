# Kaizen, 2026-08-26 07:43 — a card from a blocked message, and the vocabulary settled by the tree that owns it

*Written because the lamp lit on `9fbf3a2`.  Henri: "The questions
were blocked by the sitting limiter.  Resend them."  Then: "make a card
from it."*

## What the sitting was

The two questions to gestate-50 resent; `board/arrival.md` opened from
what blocked them — the sitting limit counts a session's message as a
person sitting down, the twin of the 2026-08-23 wake defect, reproduced
on a temporary desk (exit 2, a `block` row) and found in Henri's ledger
at 07:20:29 before the card was written.  Then gestate-50's answers:
not `partial` but a test that names it — written, red by name on the
`Bash(git push:*)` row; and gestate's borrowed `test_precommit.py`
has the same blindness, measured there with `|| true` (6 of 6 passed).
Both on `green`.

## What went right

**The card was measured before it was asserted.**  The reproduction
took one temporary desk and thirty seconds, and the ledger row is the
event itself, not a description of it.  The card's `because` is the
row.

**The vocabulary question went to the tree that owns the vocabulary**,
and the answer was a refusal to grow it — the exact rule the sweep
card's question 4 states.  A verdict word invented here to fit an
awkward row would have been the third way an instrument fails, with a
new name.

**The peer's answer was measured, not read**, and said so.  A yes from
reading would have been enough; a `|| true` run there is a fact two
trees now share.

## What went wrong, named as the session's

**I sent a message into a limit I knew existed.**  `limit.sh` is in
this tree; its header says the grant is a typed prompt; a message from
me is not one.  The block was predictable from files I had read that
morning, and I noticed it only when Henri said so.  The card is the
right outcome; noticing before sending would have been the cheaper
one.

**The first reply to Henri offered the finding and stopped**, which
was right — but it framed the fix as "not mine to make unasked" when
the card was the thing to offer.  He had to ask for the card.

## What should change tomorrow

1. `arrival`'s day one: the tag beside `<task-notification>` in
   `tools/limit.sh`, two tests in the wake's shape, each red before
   trusted; then the debt to gestate's twin, by hand, where its header
   says.  Henri's tiebreak on whether that is a fix or a feature.
2. Before a session messages another, it reads the other's clock — or
   accepts that the message may wait.  Until `arrival` is built, a
   message to gestate past its limit is a message not delivered.
3. gestate's `test_precommit.py` blindness is Henri's to card there;
   the mutation to show it red is on `green`.
