# Kaizen, 2026-08-26 15:24 — the lock patch landed, the leash measured warm at 75 ms, and its clock moved before its probe

*Written because the lamp lit on Henri's commit `f02f58d` (the lock
patch) and the leash commit after.  Henri: "applied resolver-lock,
suite green, committed", and the two timings — 75 ms, 85 ms.*

## What the sitting was

The race is closed on his seat: the runner waits 2 s for the lock, the
resolver waits 30 s for a cold start.  His `time tools/leash.sh --
true` twice said the scope path costs 75 ms warm — so the ten seconds
this session had pinned on "the scope path from cold" was not its
steady price, and the card said so.  Where the ten seconds sat was
read off `leash.sh` itself: the probe ran before the clock was
stamped, so its cost never reached the ledger and a slow probe looked
like a runner that started late.  Moved the stamp before the probe;
a fake `systemd-run` on PATH that sleeps a second proves the wall now
carries it; a row puts the stamp back and is red.

## What went right

**A number from the person corrected a sentence from the session
within the hour.**  "~10 s from cold" was an inference from timestamps;
75 ms is a measurement; the card now holds both and says which is
which.  And the correction found a real defect on the way — the ledger
could not see the leash's own cost — which is the ledger's whole job.

**The fix had a seam before it was trusted.**  A slow probe cannot be
summoned on demand, so the test fakes one on PATH; red with the stamp
after the probe, green before.

## What went wrong, named as the session's

**A cause was named on a card before it was measured.**  "The leash's
scope path took ~10 s to start a runner from cold" was written on
`resolver` and `work-environment-ai` at 15:12 as *measured* — it was
inferred from a ledger that, it turns out, could not have shown the
probe's time at all.  The inference was close (the ten seconds *are*
before the leash's clock) and the conclusion was wrong (they are not
the path's price).  The rule the tree already has is "measured, not
designed"; the finer one this needs is **an inference from an
instrument is bounded by what the instrument can see** — and I had
read `start=$(date +%s)`'s position an hour earlier without asking
what it hid.

## What should change tomorrow

1. The ten seconds are still unexplained — only relocated to "before
   the leash's clock, on the first two starts after a quiet spell."
   With the clock moved, the next such start writes its cost into the
   ledger, and `ledger.py grep 'run --idle'` will show a long wall
   where there was a late start.  Wait for it; do not reason about it.
2. The last bind: `node/state` read-only to the session, the pull file
   writable, `.venv` read-only — one fence patch, with the key.  Safe
   now: the hook serves what a session pulls, and the race is closed.
3. When a card says "measured," the next reader should be able to
   find the number.  Where a section says "measured" of an inference,
   it is wrong by this tree's own rule; both 15:12 sections now carry
   the correction beneath them rather than a rewrite.
