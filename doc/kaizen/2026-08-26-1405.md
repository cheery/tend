# Kaizen, 2026-08-26 14:05 — the state row landed by Henri's hand, after the fence went down once on the way

*Written because the lamp lit on `da1c1f1` — Henri's commit: "applied
state-row-2, the fence is up, suite green, committed."*

## What the sitting was

The state row is two directories now.  Two patches, one hand: the
first narrowed the bind and moved the test's probe; Henri's unfenced
`--check` came back **THE FENCE IS DOWN**, because the check's own
probe still wrote at the parent.  The second moved that probe under
`tend/` and added the probe the row now needs — a file written at the
parent inside must not be there outside.  His run: fence up, suite
green.  From inside, measured after: another assistant's prompt history
is gone from the session's reach; `tend/` passes through.

## What went right

**The unfenced run was the gate, and it refused.**  Every fence change
is verified by logic from here and by execution from Henri's seat; the
check found in one run what `sh -n` and `--rows` could not, and said
which line.  That is the check doing the one thing it exists for, on
the first fence change a session made since it was written.

**The fix added a probe, not just moved one.**  A row that says "not
the parent" and a check that only tests "tend/ passes" would be green
with the parent bound again.  The second probe is the row's other
half, and it is the seam a future widening would show through.

## What went wrong, named as the session's

**The same miss twice in one patch.**  I moved the test's probe and not
the check's, because I grepped the test file and not the script for
what wrote at the parent.  The countermeasure is mechanical: a fence
patch is grepped for every literal path it unbinds — `grep -n
'local/state' tools/sandbox.sh test/*.py` would have listed line 214.
Cost: one round-trip through Henri's hand, which the tree measures in
his minutes, not mine.

**A measurement from the wrong seat.**  Checking "is the parent
writable" from inside the fence answers nothing — a touch lands on the
tmpfs home and is visible to the same seat that made it.  I printed
"PARENT WRITABLE" before seeing that.  The rule is the one `--check`
already embodies: the fence is graded from outside.

## What should change tomorrow

1. A fence patch: grep the tree for every path the patch unbinds
   before handing it over.  One line, no tool.
2. The `trees` row (gestate, read-only, whole) read by purpose — what a
   tend session actually reads there — is the next state-row-shaped
   measurement.  `tools/ledger.py grep gestate` is where it starts.
3. `keep`'s session half has its first row narrowed; the tree row is
   still the hard one and unbegun.
