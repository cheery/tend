# Kaizen, 2026-08-25 14:12 — a question answered by measuring, and the answer was "don't build"

*Written because the lamp lit on `3f69d49`.  Henri: "look at the
.git/hooks writability question together", then "Write option 1."*

## What the sitting was

The line `self` left open: is the pre-commit gate in the protected set?
Measured from inside the fence — five routes past the gate, all open,
the decisive one being that the gate is the suite and the suite is the
tree.  Written on `done/gates.md` as a category, not a gap: the set
bounds what a session may do, the gate bounds what the tree accepts.
Not built; counted from here.  And a line on `fence` that
`Bash(git config:*)` is decoration inside, `.git/config` being writable.

## What went right

**The clock was read first.**  The previous kaizen said it should be;
it was — 105 minutes left, and the sitting sized itself to that.

**The measurement went before the opinion.**  The question came in as
"should the hook be protected"; the answer came out of a table, and the
table's last row — `test/` writable — made the question dissolve rather
than get decided.  The right instrument for a category question is a
list of what is actually open.

**Three options, one recommended, and the recommendation was the
cheapest.**  Manifesto rule 1 held against a plausible small build (a
`SessionStart` read-back lamp); it is written down as the thing to
build *if* the count moves, with the count started at zero, the
`cords` pattern reused rather than reinvented.

**Gestate's prior thinking was found and cited, not re-derived.**
`done/cheap-gates.md` had asked the same question a week ago and
answered with evidence; this tree did the same and said where it
borrowed the stance.

## What went wrong, named as the session's

**Nothing in the tree, this time — but a near thing in the reply.**
The first draft of the reading leaned toward option 2 before the
`--no-verify` count (zero) was in hand; the count is what made option 1
the honest recommendation.  The order in the head should match the
order on the page: evidence, then leaning.

## What should change tomorrow

1. `done/gates.md` now carries a count of skipped gates found late,
   starting at `(none yet)`, beside `cords`'s count of guessed
   questions.  A session that skips a gate writes the line; a session
   that finds one skipped writes it too.
2. The deny-list has at least two rules a reader would over-credit
   (`Edit(./.claude/**)` for shells, `Bash(git config:*)` inside).  The
   fence card carries both.  If a third appears, the list wants a header
   saying which rules are boundary and which are courtesy.
3. The sitting has ~90 minutes left.  `grant` is the next card that can
   move from here now that `bus` is in the bound — the measurement of
   whether a bus works inside at all is one command.
