# Kaizen — 2026-09-03, the 18:19 sitting: the lock class closed, measured twice, and the board's closing notes moved to the journal

*One file for one sitting, 18:19 to his "sitten alkaa varmaan olla aika
kaizenin" about 19:50, declared at 120 minutes and used about 90; five
commits (`614d6e3`, `a7961f6`, `4e01876`, `8b9abb1`, `d68a159`) and this
file.  The lamp names the file 19:04 by the first commit; the sitting
began at 18:19.*

**What it was.**  His "aloita lock-testistä", and `card:lock-test.md`
went from open to `done/` inside the sitting.  The primitive was measured
before it was written: a raw `flock -n true` read said held of a free lock
22 of 40 times under a loop of the tree's own readers, a read that waits a
window 0 of 40 — so `held PATH [WINDOW]` in `tools/launch.sh` and
`_lock_held`'s window in `tools/panel.py` are a wait, not the retry the
card proposed.  Every raw read in `tools/` is on it, `lead.sh` included;
`hammer` and `hold` are fixtures in `test/conftest.py`, the F020 test their
first caller; two mutate rows red; the whole suite green on its second run.
He installed by hand and moved the card.  Then, at his word, the board's
closing notes — every paragraph the README carried about a card as it
closed, 120 lines — went to `journal.md` verbatim, and that move tripped
the same false red the morning's 66-line move had, which the 08:50 kaizen
had said would be an F-number on a second sighting: `F021`, fixed in the
marks gate with a fixture that holds the defect, and the journal commit
went through the gate with no `--no-verify`.

**What went right.**  The measurement changed the design twice and both
times before the number reached prose for good: idle, the window replaced
the retry; under the shake, 9 of 30 red at 50 ms sent the number back to
the bench — 200 reads of each shape under eight burners put the cliff
between 50 and 100 ms, and the default is 200.  The explanation is in the
card, not just the number: under load a "momentary" reader holds for
whole timeslices, and a blocking wait is woken at the instant the next
reader is runnable too, where a poll samples independent instants — which
is why the panel's polled read survived at 50 ms and the shell's wait did
not.  The card's own "what would make this wrong" was met halfway and
said so.  Every caller was run (lead, resolve, keep, the die flow), the
red-first proof was mutate rows and not a claim, and F021 was handled as
the morning's rule said: one sighting a note, the second a mechanism.

**Wrong, mine.**  Four, all small, one that cost evidence.  (1) The first
whole-suite run had one red — the die flow, F019 and F020's own gate —
and I read the run through `tail -6`, so the traceback is gone; the
second run was green and the shake 0 of 25, and the ledger line stands
with no assertion beside it.  Full output to a file, always; the tail is
for the summary line only.  (2) The idle number went into three files
before the shake ran; the tree's instrument for exactly this was there
the whole time, and every number was rewritten an hour later.  A session
memory now says: a lock- or timing-shaped primitive is shaken before its
number is written down.  (3) The first hammer probe burst forty in-process
reads inside one of the hammer's rounds and saw nothing — a fixture's
readiness assumed, not signalled; the hammer now says when it is up and
the probe spreads its reads.  (4) The F021 test called a fixture directly
and miscounted its own insertion by one; both caught by running it, both
mine to have read first.

**Left standing.**  Nothing owed.  The die flow's sixteenth ledger line is
unread: the next sighting is read whole, and if it is the lock family it
is this card's F-number.  Thirteen open cards, four on `later/`.

**Tomorrow.**  His pick.  `lander`'s log is read on 2026-09-04.
`card:material.md`'s next step and `card:session-program.md`'s lead are
the build leads.  He said the sitting went well, and it did: the work was
measured, the board is lighter, and the journal has what happened.
