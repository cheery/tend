# Kaizen, 2026-08-28 07:58 — the card closed on a run, the first answer, and a cache that segfaulted the same way on both sides of the boundary

*Written at 08:45 with eleven minutes on the clock; the sitting began
07:55 on a regrant and holds six commits: `32472c9`, `462ac80`,
`855db69`, `13ba626`, `5764639`, `2690e19`.  Henri's lines: four runs
of `tend-launch llm …` pasted, `curl` at the port, "a. can you do that,
or give me a command to do that?", two by-hand `llama-server` runs
pasted, "I checked into the cache dir mid-run.  There is something
there now."*

## What the sitting was

* **`node-install` closed** (`462ac80`): the third run under keep
  loaded the model in 80 s, listened, and stopped on idle as the grant
  said — check and run agreeing on the machine the card was written for,
  one day after it was opened.
* **The first answer** (`855db69`): pulled with a question, asked at
  the port, 9.4 tokens a second on the Arc under keep; all 200 tokens
  were the model's reasoning.  Two facts went to `session-program`: a
  pull with words is an ask nobody delivers, and the sitting cord held
  on a node without firing.
* **`env NAME=VALUE`** (`13ba626`), at "can you do that": a grant word,
  exported once after the grant is read, shown by `grant` and `check`,
  red first.  Smaller than the card had said — `$STATE` was writable
  already; the runtime only needed telling.
* **The SYCL cache, measured and taken out** (`5764639`, `2690e19`):
  two starts under keep died 139 at the kernel build with no cache
  written; `mkdir` under `keep --write` works, so not the bits; the same
  two variables by hand outside keep — *Segmentation fault (core
  dumped)*, same place, same empty directory.  The runtime's, not the
  boundary's.  The driver's own cache is the next variable: by hand it
  loads, does not create its directory, and writes once the directory
  exists — the second start's number is pending as this is written.
* **The gate went red for the environment** (`5764639`): this job's
  `FORCE_COLOR=3` coloured pytest's output and `mutate.sh`'s line-start
  anchors missed.  `run_tests` asks for `--color=no`.

## What went right

**One variable at a time, and the split was run on the side that could
run it.**  The cache question had two suspects; the session could
measure one from the fence (`mkdir` under keep) and could not measure
the other; the other went to Henri's hand as one command, and its
answer closed the question in a line.  Nothing was declared that was
not run.

**The tree did not carry a grant that killed its node.**  The two
lines went in with "the second start is the measurement", the
measurement said 139 twice, and they came out in the same hour — still
in the file as a comment with the number beside them.

**A run answered what no check could.**  The GPU open, the silent
compile, the idle stop on a starting server: each was the run's to show
and each became a line, and the card's close says which is which.

## What went wrong, named as mine

**"The second start is the measurement" gave Henri a `sleep 150; sleep
40` script and no `check | grep env` in it**, so had the install been
stale the first sign would have been a crash; it was not stale, by his
hand, not by my script.  A measurement script carries its own
precondition.

**A cache directory the driver does not create** was in my first
command; the empty `/tmp/nc` was read as "cache off" for one round
before "the driver does not `mkdir`" was said.  Known behaviour,
unrecalled until the evidence forced it.

**The gate's red was blamed on the tree for one round** — `test_mutate`
had failed once this morning for the venv, and the second red looked
like the first until the row's empty tally said "no line matched".

## What should change tomorrow

1. **The driver cache's number.**  If the second start by hand is well
   under 1:20: `env NEO_CACHE_PERSISTENT=1`, `env
   NEO_CACHE_DIR=$STATE/neo-cache` in `llm/grant`, and something makes
   the directory before exec — the driver will not.  Henri's call
   whether that is the launcher's or a grant line's.
2. **A pull with words** — `session-program`'s; the node that reads its
   pull file is the one that leads.
3. **`/sys` in `SYSTEM_READ`** — a `keep` question when a second node
   touches a device.
4. **`-c 2048 -t 2`, no `-ngl`** — still the tiny-model shape.
5. **A measurement script names its precondition** — one face, mine;
   the ingestion will say if it recurs.
