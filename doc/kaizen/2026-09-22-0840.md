# Kaizen — 2026-09-22, the 08:28 sitting: a bound that never crossed, a language named, and its first two days

*One file for one sitting, the first in fifteen days.  The limit's log
says 08:28, extended to 180; its last line says a sitting started at
10:17 on the 15-minute default, two minutes before his close, and why
is not read from this seat — the log, not this file, is the record.
Seven commits before this file, read off `git log b8a7652..HEAD`:
`ed9b31f`, `d492767`, `2ac3c89`, `aee9993`, `319f2d4`, `9c87d82`,
`32acf66`.  His lines: "I haven't opened a tend session for a long
while, also I have a new idea to bring forward.  The work you've
implemented to tend workspace has been working well"; "I started
discussing it with general session and then thought, it'd be tend's
project"; "I restarted the session with --resume, try again";
"installed and restarted, try read the file again"; "I am curious, why
the name 'protocol' although it's a programming language?"; "I do not
know about the name 'protocol'.  It's a bit odd choice for the name of
a programming language, fine name for a card"; "Yes, this is fine.
I'm out of ideas.  How would you name the language?"; "Kude it is,
write it in and start day one"; "The previous session said this would
be lot like gluing existing things together.  But the milestone gcd
and the two party protocol seems a bit steep.  Or does it?"; "Ok, do
day two then"; "ok.  this is enough for now.  write the kaizen."
Written in the defect shape: for each wrong, what broke, what fixed
it, what now holds it.*

**What it was.**  Three stretches.  First, half an hour on a file
that could not be read: he bound `~/misc` with `tools/reach-allow.sh
--trees`, the line was written, and from inside the fence nothing
changed — before and after his restart.  The cause read from the
source: the hook's line sets `TEND_TREES` in the hook's environment,
the hook answers with a rewritten command, the harness runs that in
its own, and the sandbox fell to its default of `~/gestate`, which was
the setting since the row was named on 2026-08-26, so the seam had
never been crossed by anything that could show.  F029, fixed with a
test across the seam and a mutate row, in force at his install, and
resolved from the next session at his "installed and restarted"
(`ed9b31f`, `d492767`).  Second, the idea: the transcript of his talk
with a general session on 2026-09-21 — five designs for a language on
linear logic, the second picked, the LPVM paper as the machine under
it, "Ok.  Tehdään jotakin valmiiksi sitten" — copied byte for byte
onto a `doc/notes/` shelf taken from gestate at his word, and
`card:protocol.md` written from it with a drafted `done` line and
three marks; he signed the line as it stood, kept the card's name and
refused it for the language, and named the language Kude at his "How
would you name the language?" from a session's proposal (`2ac3c89`,
`aee9993`, `319f2d4`).  Third, the build: day one, the paper's `gcd`
as clauses with a checker that walks every combination of the guards'
atoms and refuses the case with no clause or two, and a runner —
eighteen tests red before the program, green on its first run, four
mutate rows (`9c87d82`); then, at his question whether the milestone
was steep, the answer that it was one real piece and three of glue,
and day two at his "do day two": session types on a channel, each
clause walked along the type and refused naming the state, the
branches of an offer the clauses of the predicate, the transcript's
Bank and Client checking, and one party run against a scripted partner
on stdin — thirteen tests, five rows (`32acf66`).  The whole suite at
09:24, after day one: 943 green, 18 skipped.

**What went right, and why.**  The method took a raw idea to a signed
definition of done in under thirty minutes and to two days of build
in the next sixty, and the order held throughout: the card before the
build, his line before anything under `tools/`, red first on all
thirty-one tests, the header of the program saying what is not built.
The board's gates refused two commits and were right both times: the
card unplaced in the README's priority, and a mark of the session's
with no question mark in it.  Kude's own checker refused two of the
session's test fixtures — a bank with one clause is not exhaustive —
which is the program being right about its tests on its first day.
And the name went the way `keeper.md` says it should: the session
proposed with a reason and a check within his bound, and he chose.

**What went wrong, named as whose.**  Eight wrongs; six caught by a
mechanism, two by his hand.

1. *The trees bound never crossed the hook* — the tree's, since
   2026-08-26, and shown by his hand naming a second directory.  Fixed
   in `tools/fence-hook.sh`; held by the test across the seam and the
   row that blanks the forwarding line.  Two tests had held one side
   each of that seam for four weeks, which is the "fixture is a claim"
   rule in a new face: the seam between two tested things is a third
   thing to test.
2. *A cause said to him before it was measured* — the session's.  The
   first reading, "the harness captures its hooks at session start",
   is a true fact about the harness offered as the cause of something
   it did not cause, and it was said to him; his restart disproved it
   in one step.  Recorded in F029's "what it cost".  Nothing holds it
   but the rule already on the board — *a session inside the fence
   says "I cannot see X"* — whose neighbour this is: a cause offered
   to him is one that was measured, or it is said as a guess.  First
   sighting here; the second is an F-number.
3. *A card committed unplaced* — the session's; `test_board.py` refused
   the commit and named the card.  Held.
4. *A mark with no question mark* — the session's; `test_questions.py`
   refused the commit.  Held, on its second ever catch.
5. *A mutate row survived* — the session's: the after-a-call refusal
   has two branches and the test exercised one.  The test grew the
   other comparison; the row is red.  Held by the row.
6. *Two fixtures refused by the thing they tested* — the session's; a
   one-clause bank is not a bank.  Fixed with the whole bank under the
   clause on trial, and the comment names the day.
7. *A heredoc that writes* — the session's memory lapse; the fence
   refused it with the route named, and the Edit tool took it.  Held.
8. *A loop that fed the harness the script's own tail* — the
   session's: `tail -5` of a file whose rows sit mid-file, piped into
   a harness that reads stdin, hung until the leash's 900 seconds
   killed it — "a hang is a crash", as written.  The second run read
   the rows from a file with stdin closed and took a minute.  First
   sighting; what the second would look like: a mutate loop hanging
   with no verdict lines.  Held by the leash, which is the mechanism
   working as it says.

**What should change tomorrow.**  Day three is the cut: two parties
in one run, one channel between them, queues in place of stdin — and
the `done` line's last clause, no deadlock because the check said so,
is structural for two parties and the card says so.  The reach tool's
comment still says a change "takes effect on the next prompt", which
was always true of its line and is now true of its bind; it can stay.
The note in item 2 is the one to watch: it is the shape of the wrong a
session cannot see from inside, and the day's F029 shows the other
half — the same seat found the real cause in the source within the
hour, once it stopped explaining and read.
