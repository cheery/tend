# Kaizen, 2026-08-25 07:32 — three waypoints, placed

*Written because the lamp lit on the commit that put `grant` and
`pull` on the board and the andon's waypoint on `cords`.  Henri asked
for direction, read three waypoints back, and said to card them.*

## What the sitting was

A conversation, then three cards.  The first waypoint was already a
card, so it became a section on `cords` rather than a duplicate; the
other two are `grant` (reach and budget are two dials, and one goes
dark inside the fence) and `pull` (tend governs everything and runs
nothing of its own).  Each says what would make it wrong; `grant`
also says what would make it dangerous — the bus row is wider than a
cgroup, as `audio` was wider than a speaker.

## What went right

**The `because` on each names a problem the day measured.**  `grant`'s
is the `plain` line the leash wrote inside the fence this morning;
`pull`'s is the reach experiment's own last section — a fence with no
program of tend's own to put in it.  Neither is a wish.

**One card, not two, for the andon.**  Henri said three cards; the
board's rule is one file per problem, and the problem already had a
file.  Said so rather than making a duplicate to match the count.

**The order was marked as the session's.**  Cards 3–5 are in the order
a session predicted; the README says so, and the tiebreak is his.

## What went wrong, named as the session's

**A direction read out by the session became three cards within the
hour.**  That is fast, and it is also the governed party proposing
its own next work and then carding it.  Henri asked, chose, and said
the words — the cards are his — but the prediction and the placing
were the session's, and a board that fills at the session's pace is
one to watch for the session's shape in it.  Next time a direction
is asked for, offer what would kill each waypoint before offering
the order.

## What should change tomorrow

1. `cords` on 2026-08-31, with the count; `audio` measured socket-only.
2. `grant` starts by measuring what `systemd-run` actually needs, not
   by handing the bus in.
3. `pull` stays one node, under a hundred lines, or it is a card.
4. Rows asked for so far under the hook: zero.
