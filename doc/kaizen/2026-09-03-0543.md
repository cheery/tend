# Kaizen — 2026-09-03, the 05:43 sitting: his laptop's three questions became a measurement and two flakes fixed

*One file for one sitting, 05:43 to his "ei tehdä toistaiseksi pitemmälle"
about 08:42; seven commits (`436fe66`..`292fd38`) and this file.  The
lamp named it by the first commit.  A long sitting, worked at his pace —
he came and went from his laptop, and the desk carried the between.*

**What it was.**  It began on his work laptop, not the board: llama-server
built for SYCL would not start without `LD_LIBRARY_PATH`.  Three questions,
each answered before the next: rpath first (it did not help — `setvars.sh`
sets more than the library path), then a wrapper that sources `setvars.sh`
and execs (it worked), then "where did we leave off" (read from the 18:27
kaizen's *Tomorrow*).  The wrapper answered the grant-line question the
18:27 kaizen left him: the machine's fact lives beside the binary on the
machine, not in git.  From there the desk turned to the board.  Two more
laptop problems came as the node ran: `ask run` skipped the wrapper
(`F018` — `check` resolved the program from the shell's seat, not keep's,
and keep's `execvp` walked past a home it cannot read to the bare binary
that dies at its loader; `check` now walks PATH as keep does), and the
wrapper died under keep at `setvars.sh` writing `/dev/null`, which the
write boundary denied (`write /dev/null` in `llm/grant`).  Then the
board: the panel's `pulled 08:05` was yesterday's hand pull shown with no
day (dated now, and the live edge shows its own time); `card:material.md`'s
day one — `material PATH` as a grant word — and its measurement; the
measurement's own finding turned into `ask` saying when a reply was cut;
and two flakes he asked be looked at and fixed, `F019` and `F020`.

**The measurement.**  `card:material.md`'s question is answered, and
strongly: cold, the mind said tend is an English verb (the dictionary);
with `board/README.md` and `vision.md` in hand it read the tree and
answered from it in the tree's own words — the enforcement boundary
outside the session.  A grant word puts the tree in front of the mind and
the mind uses it.  The cost is the output cap, not the input: the material
was handed whole, but 436 words of thinking plus the answer hit
`ASK_TOKENS=800` and `content` was cut mid-sentence at "jossa".  That cut,
passed off as an answer, became `ask`'s next fix.

**The two flakes, and the one shape under both.**  `F019` and `F020` are
one lesson from two sides: the tree tests an advisory lock by taking it —
a momentary exclusive `flock -n true` — and two such tests on one lock
collide for the microseconds either holds it.  On the release side
(`F019`) a lingering watcher's poll made a let-go edge read as held.  On
the start side (`F020`) the same idiom left a pulled node *unstarted*: the
watcher read the edge lock to know when to serve and was fooled by a
reader's momentary test into never serving, and serve's own guard read a
momentary contender as "a runner is up".  The fixes stop reading locks to
make decisions: the watcher asks `serve` on an interval and stops on
lock-free signals (`run.pid` alive, or `stopped`), serve retries, and the
tests poll for release and for the pull instead of reading once.  Every
reader in the tree already tolerated the collision by re-reading; only the
watcher and the tests decided on one look.

**What went right.**  Each of his questions was answered before the work
in it.  The diagnoses were read from the code and the record, not guessed:
`F018` from `keep.py`'s `execvp` and `SYSTEM_READ`, `F020` from a debug
trace of the watcher that showed it leaving its wait on a reader's lock.
`F019`'s finding named its own family, so when he pointed at the die-serve
flake the shape was already in hand.  The shake — `card:flake.md`'s
instrument — did its job three times: it turned "flaky" into a number and
caught two fix designs that were not enough before they shipped.

**Wrong, mine.**  Two.  The heredoc route hit the fence twice more
(`repro.py`, the revert script) — the `bash-heredoc-expansion` memory says
exactly this and was read at the start, and "this one is safe" is the
sentence every failure begins with; the Write tool is the road, and the
fence caught both at no cost.  And `F020`'s watcher took three designs —
the first (`serve` until `run.pid`) I verified against the die and shipped
toward, and the full suite caught it spinning the ask stub, because I had
checked the target and not the other user of the mechanism; the second
(three consecutive holds) shook 6 of 25; the third landed.  The right
lesson: verify a shared-mechanism fix against every caller, not the one in
front of me — and the shake is what made that cheap.

**Left standing.**  Nothing owed to a decision.  The `ASK_TOKENS` default
stays 800 — a node that reads the tree wants more, his call whether that
is the default or per-run.  `card:material.md` stays open at his word; the
two `material` lines are not in `ask/grant`, kept to the measurement.  The
three *ehkä* lines on `card:edge.md`, unchanged.

**Tomorrow.**  His pick.  `card:material.md`'s next step (the feature, or
the digest-vs-tool question), `card:session-program.md`'s lead (a node
working the board, now that `edge` and `material` both hold), or the F010
content cuts.  The install is done — `F018`, `F019`, `F020` in force.
