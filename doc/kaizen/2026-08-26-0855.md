# Kaizen, 2026-08-26 08:55 — green's standing debt: the rest of the suite swept, and the seat named as the limit

*Written because the lamp lit on `88d192d`.  Henri: "look at green,
maybe work on it."*

## What the sitting was

`board/green.md`'s eight unmeasured test files, broken one at a time
through `tools/mutate.sh`, each break aimed at the property the file
guards.  Seven went red on the test that names it and are recorded rows
now; the eighth, `test_sandbox.py`, could not be measured from inside
the fence and that is written down as the finding, not passed over.

## What went right

**The harness paid for itself.**  It was built this morning and today
it swept eight files in one batch, each a fresh copy and one `sed`,
with the self-check refusing to read a row until the intact copy was
green.  What was thirty lines of scratch three sittings ago is now the
thing that answered the card's standing question in one pass.

**A green that was not a pass was read correctly.**  `test_sandbox.py`
came back "6 passed, 7 skipped" on a real break of `sandbox.sh`, and
the honest reading is not "the detector survived" but "the detector did
not run" — its checks need bwrap, and a fenced session cannot nest one.
Calling that `none — nothing can, from this seat` rather than a survivor
is the difference between measuring and scoring, and the card's own
vocabulary had the word ready.

**No survivor was a rule.**  Every measurable break was caught by the
test whose docstring is about exactly it — the boot surface's one line,
the summary's live claim, the leash's hang, the fence-hook's wrap. The
tally the card has kept for a week held again: what goes unnoticed here
is wiring or a seatless fixture, never a rule.

## What went wrong, named as the session's

**I could not measure a third of the fence's own guarantees from where
I sit, and only noticed the seat mattered when a break came back
green.**  `TEND_FENCED=1` was readable at the start of the sitting; a
session that checks its seat before a sweep knows in advance which
detectors it cannot reach, rather than discovering it one green row at
a time.  The three unfenced-seat detectors — sandbox whole, the leash
scope path, the fence-hook round-trip — are one owed run from outside,
and naming them together is the honest close.

## What should change tomorrow

1. The unfenced-seat sweep: one run of tend's suite from outside the
   fence — a person's, or a gestate session's — closes sandbox and the
   two half-measured detectors at once.  It is `card:work-environment-ai`'s
   shape and belongs with its owed-outside runs.
2. Before a sweep, a session reads its own seat (`TEND_FENCED`, bwrap,
   a user manager) and says which detectors are out of reach from it,
   up front.
3. `board/green.md` has now measured every detector it can from inside;
   what remains is the outside run and whatever new detectors arrive.
   The card is close to the state where its day-one measurement is done
   and it becomes a standing practice rather than a build.
