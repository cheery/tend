# Kaizen, 2026-08-27 17:14 — two cards closed, and a move that broke citations became a notation

*Covers 4a6d353..HEAD — one commit, 4c886dd: `install` and `cords`
moved to `done/` on Henri's review, and the `card:<name>.md` citation
form taken from gestate.  A short close on the sitting's tail.*

## What it was

Henri: "check that the install and cords are done, and then mark them
done if they are fully done."  Checked from both sides first — his
`sudo tools/install.sh`, `tools/andon.sh answered`, and the suite from
outside (346, none skipped); from inside, `--check` green and the cord
`not pulled`.  Then the move — and the gate refused it three times.

## What went right

**"Fully done" was measured, not assumed.**  The first check found the
kaizen.sh fix vetted but not in force and the cord's first loop never
closed; both became one line each of his before the move, so `done/`
holds two cards whose last claims executed.

**The gate turned a move into a mechanism.**  Moving `cords.md` broke
`doc/summary/interfaces.md`'s path citation, which is exactly the case
the `card:` notation exists for — Henri, mid-turn: *"you could copy the
card:cords.md -notation from gestate now.  It starts to matter slowly."*
It is in: the root README and the summaries cite by card name; a
resolver in `test_board.py` reads every citation in the board, the
READMEs and the summaries and resolves it on any shelf of either tree;
`test_summary.py` holds the andon claim to the card's shelf in both
directions, so the sheet can never again say "not built" about a card
in `done/` — or "built" about one that is not.

**The resolver earned its cross-tree clause on its first run**: two
citations in `work-environment-ai` point at gestate's cards, and
`working-standard.md` had moved shelf there.  Resolved, not dangling.

## What went wrong — named as whose

**The session's: three refusals for one commit.**  The sheet's claim
(a test built to go red on exactly this move — it did its job), then
a wrong path for the sheet, then the sheet's own intro and its HTML
twin still saying "not built."  Each refusal was the gate reading a
document the session had not; the second was a plain mistake.  Reading
`test_summary.py` *before* the move would have made it one commit.

## What should change

1. **Before moving a card, grep for its path** — `board/<name>.md` —
   outside `board/`; the notation makes new citations safe, but old
   path citations in dated documents (`doc/os-status-*.md`) are left
   as records and are not resolved by the test on purpose.
2. **Sheet 2 has a twin** (`tend-sheets.html`); an edit to the text is
   an edit to both, and nothing checks that yet — a small slice for the
   summary lamp.
3. Four cards open; `lander` is the card-shaped residue of `install`,
   and whether to write it is Henri's.
