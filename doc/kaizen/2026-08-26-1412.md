# Kaizen, 2026-08-26 14:12 — the trees row, with the 14:05 rule run first

*Written because the lamp lit on the keep commit.  Henri: "do the trees
row."*

## What the sitting was

The `trees` row read by purpose, as the state row was: 11 ledger
records reaching gestate opened board, tools, two root documents and
the twin fence's settings; the cards cite spec, doc/memory, journal,
the manifesto; nothing ever opened the source, the tests, 3.2 GB of
builds, `.git`, or `.claude/worktrees`.  `trees-row.patch` binds the
parts and not the tree, moves the check's probe *and* adds the row's
other half, carries its own test.  Handed over; the bwrap run is
Henri's.

## What went right

**The 14:05 rule was run before the patch, not after.**  Every line
naming the path — the variable, the row, the bind loop, the probe, the
row-names test — was listed first; the probe was the one that would
have brought the fence down again, and it was moved with the other
half added in the same edit.  One round-trip through Henri's hand was
spent on this lesson at 14:05; this one should cost none.  His run
says whether it did.

**"By purpose" held as a method twice.**  The same instrument
(`ledger.py grep`, then the cards' citations) gave the same shape of
answer for a different row, and the answer was not a judgement about
gestate — the parts left out are the ones a tend session has no reason
to carry, and a reach nobody needs is a reach nobody should carry.

## What went wrong, named as the session's

**Nothing measured wrong; one thing not measured.**  Whether a session
inside the narrowed row can still run the audit
(`python ~/gestate/tools/seedaudit.py ~/tend`) — the row's named
purpose — was verified by reading the script's imports (stdlib only,
reads the target tree), not by running it.  From this seat the
narrowed fence cannot be entered, so the run is Henri's, and the test
in the patch does not cover it.  If the audit needs something outside
the parts, his run will say, and the parts list is one line to widen.

## What should change tomorrow

1. After the patch: `python ~/gestate/tools/seedaudit.py ~/tend` from
   inside a fenced session — one run, and the row's purpose is
   demonstrated rather than read.
2. The tree row is what remains of `keep`'s session half, and it is the
   one where the purpose reads everything.  It needs a different
   question than "what does the purpose read" — perhaps "what does the
   purpose *write*," since the fence already binds the protected set
   read-only and the write side is where a session's blast radius is.
3. `resolver`'s session half is the same sentence; both cards wait on
   that question, not on a slice.
