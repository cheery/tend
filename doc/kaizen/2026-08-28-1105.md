# Kaizen, 2026-08-28 11:05 — the cord heard, and brick 3 closed the road

*Written at Henri's "we can stop here for now.  I need to go in 10
minutes"; two commits since the last kaizen (`0f0d993`): `339e706` and
`a16a8ac`.  His lines: "yes, I heard it" and "brick 3".*

## What the sitting was

* **The reach-free cord confirmed end to end** (`339e706`): rung from
  inside the fence, the restarted panel played the andon's real tone and
  Henri heard it in the room.  A fenced session writes the record with no
  row; the panel outside the fence reads it and sounds it.
  `card:andon-panel.md`'s audio line is closed, and the card is his to
  move to `done/`.
* **Brick 3 built** (`a16a8ac`): `tools/propose.sh` has the node draft
  tree-shaped work and writes it, banner-marked, to a gitignored
  `proposals/` area — never a tracked file.  The boundary is the tree's
  own rule: the model proposes, the person lands it.  Five tests red
  first; the load-bearing one gives propose a card and asserts it is left
  byte-for-byte.  This closes the road's three bricks — read (deliver),
  read-and-answer (consult), draft-and-propose (propose).

## What went right

**The three bricks were built in the order that keeps the model further
from the tree at each step, and each one is a tool a person runs, not a
loop that runs itself.**  Nothing the model does can change the tree
without a hand — deliver writes only replies, consult only reads,
propose only drafts to a gitignored area.  The dangerous brick (the
model writing) was made safe by shape, not by trust: it writes where the
person looks and nowhere else, and the test proves the byte-for-byte
boundary.

**The milestone got its measurement this day**: from "is the node
installed?" at 06:37 to "the node reads the tree and is conditioned by
it, and drafts work a person lands" by 11:10 — and the conditioning
claim is a line (jidoka, cold vs grounded), not a recollection.

## What went wrong, named as the session's

**Brick 3's boundary is in the tool, not in `keep`.**  `propose.sh`
writes only `proposals/` because its code does; a model process confined
by `keep` so it *could* only write there is the stronger form, and this
is the weaker.  Honest for a person-run tool, and said on the card — but
the tree's own lesson is that a boundary a party could step around is not
the boundary, and the keep-enforced version is owed.

## What should change tomorrow

1. **Harden brick 3 in `keep`** — the model's write confined, not just
   the tool's; `card:session-program.md`.
2. **A node that leads** — the three bricks under a loop with the node's
   own cords (limit built; lamp and andon-on-a-node unbuilt).
3. **Un-shelve `later/model-acceptance.md`** — the door it waited on (a
   model admitted and answering on the tree) is open.
4. **Land a real proposal** — run `propose.sh` once and apply its draft
   by hand, so brick 3 has a landed artifact, not only a green test.
5. **Close or grow `andon-panel`** — the TUI is heard; the server/GUI
   views are its open remainder.
