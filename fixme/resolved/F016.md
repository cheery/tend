# F016 — propose.sh writes a banner with nothing under it when the reply is not a completion

    status     resolved — 2026-09-02, the same hour: an empty draft is
               refused with the door's error on one line, its code and
               its words (`tools/propose.sh`, after the `jq -e` read).
               The gate is
               test_propose.py::test_a_reply_with_no_completion_writes_no_proposal
               — the stub answers a 503 JSON error as llama-server does
               while loading; exit 1, the words on stderr, no file.  Red
               first against the previous propose.sh
    shows      `proposals/arms-warm/2026-09-02-0805-…-4.md` through
               `-18.md`: seventeen files in three seconds (08:05:43–46),
               667 bytes each, the whole of each a banner — "PROPOSAL —
               drafted by the tend llm node through the llm door … Task:
               … Material: board/README.md manifesto.md" — and no draft
               line under it.  `propose.sh` printed `proposed: <path>` for
               each and the run's log read as seventeen successes
    seen       2026-09-02 08:05, Henri's shell, run 2 of the gemma4 arm
               (`card:session-program.md`): the node's `sitting 10` fired
               at the fourth warm turn, `curl: (52) Empty reply`, two
               refused connections, then `launch: started llm` — the
               hold's tick restarting it — and while llama-server loaded
               the model every ask got its 503 JSON error and every one
               became a file
    suspected  measured in the code: `tools/propose.sh:100` reads the
               draft with `jq -er '.choices[0].message | (.content // "")
               … else (.reasoning_content // "") end'`.  On an error body
               `.choices` is null, so the whole expression yields `""` —
               and `jq -e` exits 0 on an empty string, because only
               `false` and `null` are falsy to it.  So the "not a
               completion" branch one line below never ran, and an empty
               string went under the banner
    gate       the test above.  Not gated: a completion whose content is
               whitespace only, which the same `-z` would pass through as
               a draft of nothing — no face of that has been seen
    see        F014 (the door's transport error hidden, fixed this
               morning by the same "one line, its words" rule),
               tools/deliver.sh's "not a completion" branch (kaizen 1624 —
               a door's refusal is one line, never the raw body), F015,
               card:session-program.md §"Run 2"

## What it cost

Seventeen files that read as seventeen drafts in the run's own log,
and would have counted as seventeen warm turns with zero fields — a
number that would have said "the warm arm collapsed" about a node that
was loading.  Found because the file sizes were all 667 bytes and
seventeen drafts in three seconds is not a rate a 26B model has.
