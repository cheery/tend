# F023 — `check` says ✓ on a wrapper script and nothing about the binary it runs

    status     resolved — 2026-09-04: a program that is a script gets the
               third verdict as its own line — `· program … is a script —
               ldd has nothing to read; what it runs (target) is not checked
               from here`, the target read off the wrapper's exec line when it
               has one.  Gate:
               test_launch.py::test_check_says_a_wrapper_is_a_script_and_does_not_vouch_for_what_it_runs
    shows      `tools/launch.sh llm check` on 2026-09-03 morning: ✓ on the
               program, and no "loads" line at all — `/usr/local/bin/llama-server`
               had become a wrapper script around the oneAPI-built binary, and
               `ldd` of a script says "not a dynamic executable" to stderr and
               exits 1, so the ldd block was skipped in silence.  True of the
               wrapper, silent about the binary; a check has three verdicts and
               silence is none of them (board/README.md §"What the days taught")
    seen       doc/kaizen/2026-09-03-0529.md "Left standing" 3, by the session
               that installed the wrapper's day; asked for as a fix at Henri's
               "korjaa nuo pienet ongelmat ensin", 2026-09-04
    suspected  measured, not suspected: the ldd `if` at tools/launch.sh:373
               had no `else`, so a script fell through to the next section
    gate       test_launch.py::test_check_says_a_wrapper_is_a_script_and_does_not_vouch_for_what_it_runs
    see        F018 (the same wrapper, the other face: keep's PATH walk), F013
               (the ldd block's own story), board/README.md §"A check has three
               verdicts" — the two call sites named there were `launch.sh:302`
               and `toolbox.sh:80`; this is the third
