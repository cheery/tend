# F002 — mutate.sh's scratch commit could not sign, so every gate row read "refused" for the wrong reason

    status     resolved — 2026-08-29
    shows      `fatal: failed to write commit object` on every row of tools/mutate.sh (kaizen 1337, item 5: "noise that will one day hide one that does"); every `gate` row's verdict "refused" whatever the break
    seen       reproduced 19:45 in a scratch `git init`: `error: Couldn't load public key ~/.ssh/id_ed25519.pub` — a fresh repository inherits the global ssh signing config, and the fence has no key; the intact commit died, the copy had no HEAD and no hook, the dud commit could not sign either, so `before == after` read as "refused"
    suspected  measured, not suspected: signing.  `$G` now says `-c commit.gpgsign=false`, and a copy with no intact commit or no hook is exit 3 ("the harness could not be trusted"), not a row
    gate       none — the harness's own trust step runs one known break, not a signing failure; kaizen 1934 item 5 owes a row where a global config that signs with a missing key must read as exit 3.  Measured by hand at 19:54: `sh tools/pre-commit.sh --uninstall` → `COMMITTED  suite: red  test_the_hook_is_installed_in_this_clone`, the correct verdict, unreachable before
    see        card:flake.md, card:green.md (the blindness is in the wiring), tools/mutate.sh, 5f7e847
