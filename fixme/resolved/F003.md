# F003 — a fixture's failure was counted on the person's live failure ledger

    status     resolved — 2026-08-29
    shows      five lines of `test_one.py::test_no` in ~/.local/state/tend/failed.log within an hour of the ledger landing — test_precommit.py's scratch tree runs the suite over a test that must fail, and nothing gave it a ledger of its own
    seen       `cat ~/.local/state/tend/failed.log` at 19:4x: lines at 19:33, 19:34 (hand) and 19:36–19:38 (gate), all `test/test_one.py::test_no`
    suspected  measured: the ledger path defaulted to the live file and the fixture inherited the environment — the fixture rule of 2026-08-26 in one more face (the canvas paid it the same afternoon)
    gate       none — the fix is a conftest fixture (`_the_failure_ledger_is_the_fixtures_own`, TEND_FAILED_LOG at tmp for every test), and the measurement is the live file staying absent through the gate's runs, checked by hand at 19:5x and 20:0x.  A test that asserts the live path is never written would have to know the live path, which is the thing the fixture hides
    see        card:flake.md, test/conftest.py, f37c577
