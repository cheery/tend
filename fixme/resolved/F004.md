# F004 — the reach key wrote a name that is not a row onto the hook line, without a word

    status     resolved — 2026-08-30
    shows      `.claude/settings.json`'s fence-hook line read `TEND_REACH_ALLOW=net,tree` — `tree` is a row that is always on, and `tools/sandbox.sh --reach tree` is exit 2 "no such row"; the bound named a reach no session could ask for, and the key that wrote it said nothing.  Henri, 2026-08-30 morning: "Could you modify tend-reach-allow to restrict what you can insert into it, and list available allowances as well?"
    seen       the working tree on 2026-08-30, `git diff .claude/settings.json` (`net` → `net,tree`, uncommitted); the new `tools/reach-allow.sh --rows` on the live tree: `?  tree  is on the line and is not a row`, exit 1
    suspected  measured: `tools/reach-allow.sh` took its argument as written and put it on the line — it had no list of the rows and never asked the fence for one.  The hook checks membership only (`,row,` in the bound), so a name that is not a row is harmless there and wrong at the fence, where the person reads it as a permission granted
    gate       test/test_reach_allow.py::test_a_name_that_is_not_a_row_is_refused_before_the_file_is_touched — `tree`, `net,tree`, `state`, a space, a `;`, an empty name, an upper-case name, each refused with exit 2, the rows named, the file byte-identical; and `::test_the_rows_it_lists_are_the_fences_off_rows` holds that the key's list is read off `tools/sandbox.sh --rows` (the `off` rows) and is not a second copy
    see        card:fence.md, card:self.md (the key in the protected set), tools/reach-allow.sh, tools/fence-hook.sh, tools/sandbox.sh

What the line was reaching for was the other tree: Henri, the same
morning, "isn't there a way to allow you to access other trees?".
There is no reach row for that — the `trees` row is always on,
read-only, and its path is a constant in `tools/sandbox.sh`
(`trees="/home/cheery/gestate"`), which on this laptop names nothing;
so the row binds nothing here, and no key points it elsewhere.  That is
a want and not this defect: this entry is the key writing what the
fence cannot read.  The resolution is the key asking the fence for its
rows and refusing the rest; the live line still carries `tree` until
the person's hand runs `tend-reach-allow net`, which `--rows` says.
