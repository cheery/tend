# F026 — propose through a door said "not a completion" six times and said nothing of why

    status     resolved — 2026-09-04, the same hour: the draft is read in
               both reasoning spellings (`reasoning_content`, OpenRouter's
               `reasoning`) as `tools/deliver.sh` reads it, and a reply
               with no error and no draft is refused with its
               `finish_reason` and the two channels' lengths on the line.
               Gate:
               test_propose.py::test_a_draft_in_the_doors_reasoning_spelling_is_the_draft_and_an_empty_body_says_what_it_had
               — red first against the previous propose.sh on both halves
               (the first wrote no file; the second said nothing after
               the dash).  **Not run live**: the openrouter door calls
               out, which a session cannot; the next hy3 draft from his
               shell says whether the spelling was this failure or the
               line names another
    shows      Henri's shell, 2026-09-04 15:28–15:30, six `lead.sh llm
               --door openrouter --tools --seed` turns at
               `TEND_READCHARS=4000`: every pick in shape and on the
               shelf, and every draft `propose: the openrouter's reply was
               not a completion` followed by an empty line — `outcome
               failed — propose.sh did not draft` on all six accounts
    seen       the same minutes, while the gemma4 arm of the same
               measurement drafted six of six through the llm door
    suspected  measured in the code, not in the body — the body was not
               kept: `tools/propose.sh:109` read the reasoning as
               `.reasoning_content` alone, llama-server's spelling, where
               `deliver.sh:270` has read `.reasoning` too since 2026-08-30;
               `TEND_MAXTOK` is 600 for a draft, and a door mind that
               reasons by default spends it there.  So the likeliest shape
               is content empty, reasoning full, `finish_reason length` —
               F016's neighbour on the door's side of the wire.  And the
               refusal line's second half printed only an `.error`, so a
               body with choices and nothing in them was a dash and a
               newline
    gate       the test above.  Not gated: the live body's shape, which
               only his shell can fetch; the line now says it when it
               comes
    see        F016 (an empty draft under a banner, the node loading);
               F011 (record what came back, not what was asked for);
               tools/deliver.sh's two spellings; card:session-program.md
               §"15:3x" — the twelve seeded turns this broke half of

## What it cost

Six hy3 picks, each read two or three cards and chose among them, and
not one draft to read beside gemma4's six — the comparison the
measurement was for has its pick half and not its draft half.  And a
refusal that says nothing is the F008 shape on a new line: the
courier's own rule since kaizen 1624 is *a door's refusal is one line,
its code and its words*, and this line had the form and not the words.
