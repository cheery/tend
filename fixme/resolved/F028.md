# F028 — the meter said nothing waited for his hand while six marks did

    status     resolved — 2026-09-07, the same hour: a bare `)` after
               `henri:` is not his answer, and the answer is the LAST
               `henri:` in a mark (`tools/meter.py`, EMPTY and the
               `answers` list).  Gate:
               test_meter.py::test_a_mark_placed_with_an_empty_henri_line_is_waiting
               — a mark placed with `henri: )*` and a `henri:` in its
               prose, on the list.  Red first twice: the empty line, then
               the prose mention; two mutate rows
    shows      `tools/meter.py --waiting` on 2026-09-07 12:3x, the minute
               after five `done` drafts went onto the window cards, each
               with its mark: "for him: nothing waiting for his hand".
               `grep -rn "henri: )\*"` the same minute: six — the five and
               `board/done-when.md:36`, placed 2026-09-06.  And the seven
               marks of `spec/canvas.md` were placed in the same form on
               2026-09-06 and never counted; nobody saw it because he
               answered all seven the same sitting
    seen       2026-09-07 ~12:33, this session, at Henri's "start with
               card:done-when.md and equip the window-related cards with
               it first" — the meter run to show him the five, and the
               two counts not agreeing
    suspected  measured in the code: a mark is placed with its answer line
               already in it — `henri: )*`, the form since 2026-09-06
               (`spec/canvas.md`'s seven, by `git log -S`) so that he
               writes into the mark and not beside it — and
               VERDICT's lazy group then holds the bare `)`, which is a
               non-empty answer with no date, so the line is blamed and
               the mark read as struck.  Second face, the same fix: the
               regex took the FIRST `henri:` in the mark, and
               `card:done-when.md`'s prose says "`henri:` line" two lines
               above its empty one — so that mark was struck by its own
               sentence about not being struck
    gate       the test above.  Not gated: a mark whose prose says
               `henri:` AFTER the answer line, which no mark has done
    see        card:done-when.md (its `done` field is the thing the
               waiting count exists to show him; the day-one plan there
               names `--waiting` as the queue for his hand, and the queue
               was empty), F025 (the meter's other typography defect,
               three days earlier, the same shape: a form the tree writes
               that the parser did not read), board/README.md §"The close
               is a call" (his answer goes where the mark reads it — and
               the mark reads its own placeholder as an answer),
               tools/meter.py --waiting

## What it cost

Nothing read wrong yet by a person: the `for him` column is three days
old and the empty-line form of a mark is one day old.  What it would
have cost is the done-when card's whole mechanism: a `done` line waits
for his hand by being on this list, and the list said nothing waited
on the first morning it was used for that.  Found because the number
was computed by two routes before it went into prose — the meter's
footer and a grep of the tree — and they disagreed by six.
