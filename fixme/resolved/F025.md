# F025 — the meter read five ledger rows as never ingested, on the verdict's own typography

    status     resolved — 2026-09-04, the same hour: the verdict is the
               first backticked word of the row's LAST cell, bold or not,
               with or without a where inside the backticks
               (`tools/meter.py`, INGESTED and VERDICT_CELL).  Gate:
               test_meter.py::test_every_form_the_ledger_writes_a_verdict_in_is_read
               — four rows in four forms, one with a bare pipe in its
               phrase, none unread.  Red first in a copy, two mutate rows:
               the third cell read instead of the last, and the bold star
               not allowed
    shows      `python3 tools/meter.py` on 2026-09-04 after batch 11:
               "19 of 124 kaizens have no verdict in doc/ingested.md yet",
               when the ledger had 110 rows for 124 files — 14 unread by
               the rows, 19 by the meter.  Five by the verdict's own
               typography: 2026-08-25-1540 and 2026-08-28-1313 (`open —
               card:…` with the where inside the backticks),
               2026-08-27-0538, 2026-08-28-1017 and 2026-08-29-1934
               (**`promoted` — …**, bold first).  And two more found by
               the first fix, 2026-08-26-1616 and 2026-08-27-0551: a bare
               `|` inside the phrase, so the verdict sits in a fourth cell
    seen       2026-09-04 ~15:00, the 14:2x sitting, batch 11 being
               written: the card's count was computed from the rows by the
               patch script and the meter's footer said five more, and the
               two numbers were not written down until they agreed
    suspected  measured in the code: `INGESTED` wanted the third cell to
               open with a backticked word closed by a backtick — `| \`rule\``
               — so a where inside the backticks and a bold star before them
               were both a non-match, and a non-match is "no verdict".
               The three `promoted` rows are the batches' own promotions,
               the rows the recurs share most needs to read; the two
               four-cell rows are a phrase that used a pipe as prose
    gate       the test above.  Not gated: a fifth typography the ledger
               has not used yet; the meter's own row for this defect is
               `once` until the ledger writes one
    see        card:meter.md §"What would make this card wrong" — a column
               moving because the parser and not the tree changed, which
               this is in the small: the unread count was five too high
               from the day the column existed (2026-09-04 morning), and the
               recurs denominator five too low; doc/ingested.md batches 1,
               3, 4, 5, 8

## What it cost

The `for him` column's neighbour, `recurs`, is the column the card says
answers Henri's sentence, and its denominator was short by seven —
three of them the very rows that record a promotion.  Nobody read a
wrong number off it yet: the column is a day old.  Found because the
count was computed twice, once by the meter and once by the patch
that wrote it into the card, and the difference was chased before
either number was written into prose.
