# F027 — propose.sh says "printf: I/O error" on every draft from a card longer than a pipe

    status     resolved — 2026-09-04, the same hour: printf's stderr is
               closed on the cut line (`tools/propose.sh`, the F010 cut).
               Gate:
               test_propose.py::test_a_cut_of_a_material_past_the_pipes_buffer_says_nothing_but_the_notice
               — a 100k material cut at 300, no "I/O error" on stderr; red
               first by construction, the previous line prints it
    shows      Henri's shell, 2026-09-04 17:03, `lead.sh llm --kept --door
               llm --tools --card board/session-program.md`:
               `/home/henri/tend/tools/propose.sh: 67: printf: I/O error`
               between the keep warning and `proposed: …`; the draft was
               written and the line said an error
    seen       once, the first --card turn that drafted; the card is 1952
               lines, ~90k chars, and TEND_CTXCHARS is 30000
    suspected  measured in the code: line 67 is `printf '%s' "$material" |
               head -c "$ctxchars"`; head exits after its bytes, the pipe
               closes, and a printf still writing past the 64k buffer gets
               EPIPE, which dash reports as "I/O error".  Every material
               under 64k never reaches it, which is why F010's own test
               (40 lines) was green — and it reaches it only where SIGPIPE
               is ignored: python ignores SIGPIPE and keep.py execs the
               program without restoring it, so under keep printf gets
               EPIPE and says so, where under a shell it dies silently.
               Measured: the gate was green against the unfixed line until
               it ran with `restore_signals=False`
    gate       the test above.  Not gated: the same shape elsewhere in
               tools/ — one grep, `printf .* | head -c`, is the sweep, and
               it is not run here
    see        F010 (the cut line this sits on); card:session-program.md
               §"17:0x"

## What it cost

A line that reads as a failure on a draft that succeeded, once — and
the shape the tree names as its own (F008, F010): a mechanism that
says a wrong thing at exit 0.  Small, and the sweep is owed.
