# F024 — the ask node's five tests were written for an ask with no material, and the grant now carries one

    status     resolved — 2026-09-04, the next sitting, at Henri's "tekisitkö
               F024 sillä välin kun lepäilen": the three assertions say what
               an ask with material says — the material's heading first in
               the stub's echo, `---material: README.md (N chars)---` in the
               answer file with N counted by the test from the text it wrote,
               `ASK_TOKENS=2000` on the cut line.  Gate: the five ask tests in
               test/test_launch.py, from
               test_the_ask_node_pulls_the_llm_talks_to_it_over_the_edge_and_lets_go
               on; red first on this tree, the 13:13 run
    shows      `python3 tools/suite.py` at 13:13, five reds in
               `test/test_launch.py`, all the ask node's: the first two and
               the fourth, fifth die at `ask/state/log` not found; the third
               says "the ask node never took the edge".  Alone, the same:
               0.85 s, not a flake.  On the ledger,
               `~/.local/state/tend/failed.log` 2026-09-04 13:13, five
               `hand` lines
    seen       2026-09-04 13:13, the 12:56 sitting, the whole suite run after
               commit 677b971 (the kept loopback door, `tools/lead.sh` — a
               file none of the five touch).  The suite's own record says it
               last passed whole four commits earlier, 06:18, and
               `ask/grant` got its `material ../board/README.md` line in
               ef0b7c5 that same hour (card:material.md) and lost its doubled
               copy in a794087 by Henri's hand
    suspected  measured in the test: `ask_nodes()` copies the tree's
               `ask/` into a scratch tree with an `llm/` beside it and
               nothing else, so `material ../board/README.md` resolves to a
               file that is not there and the node never starts — that is
               the `log` not found, five times.  Giving the scratch tree a
               `board/README.md` (this sitting, in the test) brings the node
               up and leaves three reds that are the *assertions*: the stub
               answers `ANSWER to: === README.md === …` because ask.py now
               hands the material with the question and the test wants
               `ANSWER to: Say hi` verbatim; and the cut line says
               `ASK_TOKENS=2000` — the cap for a node with material — where
               the test wants `800`.  Three expectations written on
               2026-09-02 for an ask with no material, never re-read when the
               grant changed
    gate       the five tests above.  Not gated: a grant edit that changes
               what a node's tests must expect still has nothing tying the
               two — the hook runs the gates and not the program tests, and
               this was caught by the suite's "last passed N commits ago"
               line one sitting later
    see        card:material.md — the grant line and ASK_TOKENS 2000 / 800;
               card:edge.md — the ask node and its two tests;
               test/test_launch.py::ask_nodes; doc/kaizen/2026-09-04-0532.md
               "wrong, mine" (3) — the same family, a number written and not
               read back

## What it cost

A whole suite red for the first time since 06:18 on a change that could
not have caused it, and read as such only because the five names were
all the ask node's and the assertion lines were read from the file, not
the tail.  What it says about the mechanism: a grant edit changes what a
node's tests must expect, and nothing ties the two — the material line
landed with card:material.md's own measurement green and the ask tests
never ran in that sitting's hook, which runs the gates and not the
program tests.  The suite's "last passed N commits ago" line is what
caught it, one sitting later.
