# F001 — under load the launcher stops a busy program as idle at exactly the idle window

    status     open
    shows      `test_a_program_that_is_busy_and_silent_is_not_idle` shaken ten times with eight cores burning: 8 of 10 stopped as `idle: nothing has pulled n for 2s`, wall 7.4–8.7 s; once in a full-suite run with no burners (kaizen 1918)
    seen       ~/.local/state/tend/failed.log, sixteen `shake` lines at 19:47–19:48 and 20:03–20:04, before and after the window rule; the assertion text in card:flake.md §"The first shake"
    suspected  not the per-second granularity — that was fixed (the window rule, `25e1d2e`) and the count did not move.  Two candidates the tick reads will decide between: keep's own startup under contention leaving too few ticks in the first window, or `/proc/$pid/stat` naming a process that is not the one burning.  Both would also explain F000
    see        card:flake.md, card:node-install.md (the rule's origin: llama-server compiling 45 s with no log line), tools/launch.sh

This is the rule that would stop llama-server mid-compile on a loaded
box — the case the rule was written for on 2026-08-28 — measured as a
count.  The shake is the instrument; the tick reads are the next one.
