# F000 — the window rule stops a slow-starting program on the tick its first window slides

    status     open
    shows      `test_a_program_busy_in_bursts_is_busy_over_the_idle_window` fails in the gate at no load and passes on retry: two commits refused once and landed unchanged (kaizen 2048)
    seen       ~/.local/state/tend/failed.log — `2026-08-29 20:40  gate  test/test_launch.py::test_a_program_busy_in_bursts_is_busy_over_the_idle_window  load 0.70  wall 213.9s`, and the same at 20:48, load 0.62; 2 of 4 gate runs; third: 2026-08-30 07:29, load 4.69 (gemma4 loading through the new launcher beside it), the door.sh listing commit refused on no change to the launcher, landed on retry
    suspected  in tools/launch.sh's watch loop, when the program under keep is slow to reach its first burst, the first window's slide (`now - base_at >= idle`) and the idle check (`now - busy >= idle`) fire on the same tick: the base is reset to the few ticks read so far, `busy` is still `began`, and the program is stopped before its first fifty ticks are summed
    see        card:flake.md (the first shake, "do 1"), test/test_launch.py, doc/kaizen/2026-08-29-2048.md

Found by the ledger on its first evening, and it is the test the
ledger's own card landed at "do 1" — written, red then green once,
and not shaken before it rode into `25e1d2e`.  The next sitting shakes
it twenty times without burners and twenty with, reads the launcher's
ticks under it (a `$STATE/ticks` line per second is the instrument
kaizen 1934 asked for), and fixes the rule or the fixture.  Never
marks the test.

**Third sighting, 2026-08-30 07:29, load 4.69** — the gate refused `tools/door.sh
--models` (a commit touching nothing under the launcher) on this test,
the suite's ledger said "seen before, 2 times", and the commit landed
on retry.  Three refusals across two days, each on an unrelated commit;
the shake the entry asks for has still not been run.  It moves up: it
is now costing a gate run per day.
