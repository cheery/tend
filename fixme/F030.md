# F030 — every node start under keep prints a Python deprecation warning into the node's output

    status     open
    shows      two lines before the program's own, in the node's log and
               on the person's terminal: `tools/keep.py:153:
               DeprecationWarning: Due to '_pack_', the
               'path_beneath_attr' Structure will use memory layout
               compatible with MSVC (Windows). If this is intended, set
               _layout_ to 'ms'. The implicit default is deprecated and
               slated to become an error in Python 3.19.`
    seen       first 2026-09-04, noted in F022 ("Also seen in every node
               log on this machine and not this entry's … One sighting,
               noted"); second 2026-09-24 18:13, Henri's paste of
               `tools/launch.sh ask-kude run` against the real llm, the
               warning above the node's `answer = …` line
               (ask-kude/state/log)
    suspected  Python 3.14's ctypes: a Structure with `_pack_` and no
               `_layout_` is warned about now and refused from 3.19.
               keep.py:153's `path_beneath_attr` sets `_pack_ = 1` for
               the kernel's packed landlock struct (u64 then s32, 12
               bytes).  Suspected fix, not measured: `_layout_ = "ms"`
               beside it, which names the layout it has today — unless
               the measurement says "gcc-sysv" packs the same 12 bytes,
               which is the kernel's own.  Either way `ctypes.sizeof` is
               12 and a test says so
    see        fixme/resolved/F022.md (the first sighting), tools/keep.py
               (installed at /usr/local/lib/tend: in force only at his
               `sudo tools/install.sh`), board/README.md §"One sighting is
               a note; the second is a mechanism"

The noise is the smaller half.  The larger is 3.19: the implicit
default becomes an error, and keep — every node's boundary — would fail
to start on the Python that makes it one.  Not fixed by the session that
opened this: keep is the enforcement boundary, and a change to it is his
to call.

**Fixed in the tree, 2026-09-24, at his "fix F030".**  Measured on
/usr/bin/python3 3.14.4 with the warning as an error: no `_layout_`
warns; `"gcc-sysv"` refuses `_pack_` outright ("_pack_ is not
compatible with gcc-sysv layout"); `"ms"` loads, 12 bytes, `parent_fd`
at 8 — the layout keep already had, now named.  keep.py carries
`_layout_ = "ms"`.  Gate, red first on the warning:
`test_keep.py::test_the_landlock_path_struct_is_the_kernels_twelve_bytes_and_loads_without_a_warning`,
and one mutate row (the line deleted) red; test_keep.py's 28 green, the
confinement tests with them.  **Not in force** until his `sudo
tools/install.sh` — the nodes run the installed copy — so this entry
stays open until a node started after it shows no warning.
